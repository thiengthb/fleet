# Knowledge log — <project name>

> Architecture decisions + pitfalls encountered + the reasoning, recorded so the **next session does not repeat mistakes**.
> Append-only, **newest on top**. Recording standard: `nuc-platform/05-documentation-standard.md §5`.
> Record only the **non-obvious** (what code/git does not say on its own). Maintained by the `/session-wrap` skill.

<!--
  Entry template (copy to the top of the list, fill in, then delete this comment block — or keep it as a template, no need to remove):

  ## YYYY-MM-DD — <title of the decision/pitfall, 1 line>

  **Context:** why this decision was needed (1–2 sentences).
  **Decision / Pitfall:** what was chosen, or what the pitfall is.
  **Why:** the reasoning + what was ruled out (the most valuable part — don't omit it).
  **Related:** `file.ts:42` · [[other-name]] · INVENTORY §n.
-->

_(No entries yet — add one when there is a decision/pitfall worth remembering.)_
