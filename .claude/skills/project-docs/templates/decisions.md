# Sổ tri thức — <tên project>

> Quyết định kiến trúc + bẫy đã gặp + lý do, ghi lại để **session sau không lặp lại sai lầm**.
> Append-only, **mới nhất lên đầu**. Chuẩn ghi: `nuc-platform/05-TAI-LIEU-CHUAN.md §5`.
> Chỉ ghi cái **non-obvious** (code/git không tự nói). Bồi đắp bởi skill `/session-wrap`.

<!--
  Mẫu một mục (copy lên đầu danh sách, điền rồi xóa khối comment này KHÔNG cần — giữ làm mẫu cũng được):

  ## YYYY-MM-DD — <tiêu đề quyết định/bẫy, 1 dòng>

  **Bối cảnh:** vì sao phải quyết định này (1–2 câu).
  **Chốt / Bẫy:** đã chọn gì, hoặc bẫy là gì.
  **Vì sao:** lý do + cái đã loại bỏ (phần quý nhất — đừng bỏ).
  **Liên quan:** `file.ts:42` · [[tên-khác]] · INVENTORY §n.
-->

_(Chưa có mục nào — thêm khi có quyết định/bẫy đáng nhớ.)_
