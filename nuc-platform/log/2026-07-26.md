<!-- One file/day; multiple entries = multiple blocks. Schema: see README.md. -->

## sakubun — /guide findability + landing message-flow (plan phases A,B,C,D,E,F2,F3,G) — 2026-07-26

```yaml
id: 2026-07-26-01
type: episodic # episodic | reflection (milestone anchor)
created: 2026-07-26T04:45:00+07:00
last_accessed: 2026-07-26
importance: 7 # 1–10, Park poignancy at write
milestone_id: null
related_ids:
  [
    {
      id: 2026-07-25-01,
      why: "same tree, the parallel skeleton session — its _INDEX.md entanglement resolved (both doc lines committed)",
    },
  ]
embedding: null
```

**What happened**

- Session opened (late 2026-07-25) with a 4-part UI batch on the user's ask: a clear (×) button implemented ONCE in
  `ui/input.tsx` so every text field inherits it; the bulk-action bar rebuilt as a fixed VERTICAL icon dock on the right
  edge (count chip → × → icon-only actions with hover tooltips, all through the new `bulk-action-button.tsx`); DataTable
  keyboard shortcuts (`/ n p a c Esc`) with a ⓘ hint in the header's top-right; and `edge-nav-rail.tsx` — a left-edge grip
  that slides the nav out as icons below `md`, where the sidebar becomes a Sheet. 5 commits, 2 UI patterns locked.
- Then wrote + executed `sakubun/docs/plans/2026-07-25-guide-and-landing-upgrade.md` (researched: Diátaxis + 2026
  landing sources; 5 options ruled out). Shipped, in the user's chosen order: **A** registry-driven, searchable,
  deep-linkable `/guide` feature tour (17 topics → `guide-topic-meta.ts` + a client search shell, bodies stay
  server-rendered) · **C** typed `GuideLink` contextual help at 7 stuck-points + recipe/tab ids · **B** the Bắt đầu
  tutorial showing REAL per-step progress derived from the learner's rows · **E** landing re-ordered to problem →
  features → how-it-works → prerequisites → FAQ → proof → CTA · **F2** one shared `public-top-bar` + guest-viewable
  `/guide` · **F3** single source for the 5 loop steps · **D** "Mới cập nhật" + config copy buttons + a static 360px
  audit · **G** landing metadata + a `next/og`-generated OG card, a11y pass, docs.
- Gates each phase: tsc · eslint · prettier · knip · `npm test` (232 → **259**, 32 files) · `npm run build` · container
  rebuilt healthy. 13 commits in the sakubun repo + 1 ledger commit in the platform repo. Nothing pushed.
- Two traps paid for in real failures: the same URL-vs-sessionStorage precedence bug TWICE (`?topic=`, then `?tab=`), and
  satori's "explicit display:flex" rule (one build failure) plus its no-CSS-vars / no-CJK-font limits.
- Verification was HTML-level, not visual: the Chrome extension stayed offline all session (`list_connected_browsers`
  → `[]`, even after the user installed it and I retried on request). What that DID prove, from the served HTML with no
  cookie: all four new landing blocks present; a guest `/guide` returns 200 with **zero** app nav links / sidebar markup
  / logout (F2 + phase B3 guest-safety); `/opengraph-image` 200 image/png 69 KB — and that PNG I could actually LOOK at
  (read the file), so the card's layout + Vietnamese diacritics are confirmed by eye.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-26 ×5 (guide-as-registry + the RSC metadata/bodies split;
deep-link-must-be-applied-on-mount; derived tutorial progress + `McpKey.lastUsedAt` as the "Claude connected" signal;
landing message order + the ban on fabricated proof; the social card as code + satori's three limits). Cross-project:
`06-knowledge-ledger.md §A` ×3 (URL-seed-vs-deep-link precedence; client-metadata/server-bodies split for docs-heavy UI;
OG card as code). `sakubun/docs/00-map.md` gained a "Public surfaces" bullet.

**Open threads**

- **F1 is the only unfinished plan step** and it is blocked on a human: 3 real screenshots (`/`, `/resources`, a graded
  `/history` turn) at 1440×900, light + dark, WebP ≤250 KB into `sakubun/public/screens/`; then the landing's "Xem thử"
  section is ~30 min. Deliberately NOT faked with mockups. Plan stays `status: active` for this.
- **Nothing client-side has been seen by a human or a browser**: guide search + group hiding, the three deep-link forms,
  the signed-in progress strip, FAQ keyboard, and the 360px pass (D3 was a static grep audit only).
- Browser-review caveat carried from `2026-07-25-01`: even once the extension connects, its Chrome profile has no app
  session, so AUTHED pages bounce to `/login` — the public landing and the now-guest-viewable `/guide` are reviewable
  there, the dashboard/resources/history are not.
- 14 commits unpushed (NUC local-only). `nuc-platform/10-idea-queue.md` was already dirty before this session — untouched.

## sakubun — agentic-tutor / generation-quality / adaptive-calibration planning + MCP runtime lab — 2026-07-26

```yaml
id: 2026-07-26-02
type: episodic
importance: 8
milestone_id: null
related_ids: [2026-07-26-01]
embedding: null
```

**What happened.** A planning pass, no production code. Three plans written (all `status: draft`, nothing
accepted): `2026-07-26-agentic-tutor-loop` (server-computed proposals + one-call `check_library` + enrichment
quality/versioning + a student-model dossier + error mining + teach-back + ingest + card provenance; 9 phases),
`2026-07-26-natural-generation-and-item-relations` (item `slot` attribute + typed relations + compatibility-aware
selection + declared scaffolding + a 24-field settings audit), `2026-07-26-adaptive-calibration` (Elo ability
vector + 85%-rule setpoint + a receptive-to-productive rung ladder + a prior-vs-evidence trust model).

Built a runtime lab (`scripts/mcp-lab-{seed,client}.mjs`) and drove the REAL MCP server as both tutor and
learner on port 3799 against a throwaway DB — prod 3789 untouched, verified after teardown. Ran a full graded
turn; FSRS wrote correctly. Found and verified: `stage_sentence` accepts no `items`; `newGrammarLimitReached`
has no reader; a focus sheet can assert FOCUS entries it did not list (silent FSRS data loss); `enrich_items`
destroys card content on a partial send; a schema-invalid card counts as enriched; 3 brand-new patterns can
land in one sentence on the DEFAULTS. Withdrew one of my own claims (`submit_review` DOES reject a missing
`sentenceId`). Discarded one finding after discovering my seeder's dates don't round-trip through Prisma.

Research (Standard tier, no fan-out): ITS four components (the missing STUDENT and TUTOR models), error-tagged
learner corpora, protégé effect / self-explanation, MCP Resources (ruled out — manual attachment only), lexical
chunks, constrained-generation fluency tradeoff, つまり/要は as one reformulation slot, interleaving vs
in-sentence redundancy (this one CONTRADICTED the user's framing and changed the design), Nation's four strands,
involvement load hypothesis, the 85% rule, desirable difficulty, Elo for adaptive assessment.

**Decisions made.** Four new entries in `sakubun/docs/decisions.md` (prose-contracts-are-not-enforced; the
day-vs-exercise budget; the MCP lab + seeder-fidelity trap) — see that file, not copied here. Design decisions
inside the three plans stay in their "Decisions to distill" until a plan closes.

**Open threads.**
- All three plans are `draft`. Nothing is accepted, no production code written, nothing committed.
- Recommended order if accepted: gen-plan 0.1 (`maxNewPerSentence`) -> agentic Phase 0 (6 live-defect fixes) ->
  gen-plan 0.3/0.4 (split `levels`, trim the per-turn settings payload) -> calibration Phase 2 (the ladder,
  valuable without the controller) -> calibration Phase 1 (shadow mode) -> the engines.
- The blank-chat enforcement assumption (tool descriptions only) is unproven and gated at agentic step 1.7.
- `A-deferred` ("rank next-intro") in `2026-07-11-adaptive-vocab-sensory.md` is subsumed by calibration 2.3;
  close it there when that lands. That plan was NOT otherwise touched.
- Three files were already dirty from a parallel pass (`app/groups/[id]/loading.tsx`,
  `components/shared/fit-skeleton.tsx`, `docs/decisions.md` RSC entry) — deliberately untouched; my decisions
  entries were inserted ABOVE that one.
