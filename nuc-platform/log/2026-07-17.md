<!-- One file/day; multiple entries = multiple blocks. Schema: see README.md. -->

## sakubun — shared time/date picker, nbsp-tag fix, vocab-flagging inbox — 2026-07-17

```yaml
id: 2026-07-17-01
type: episodic
created: 2026-07-17T16:00:00+07:00
last_accessed: 2026-07-17
importance: 6
milestone_id: null
related_ids: [{ id: 2026-07-14-01, why: 'continues sakubun UX/MCP work; builds on the vocab-in-sakubun surface' }]
embedding: null
```

**What happened** — three requests in one pass:

- **Shared date/time picker → sakubun.** Installed `ui-kit` `date-picker` + `time-picker` (copy-in) +
  their base deps (`popover`/`scroll-area`/`calendar` from `todo`, `date-fns`+`react-day-picker`).
  Replaced the raw `<input type="time">` at Settings → "Giờ nhắc học" with `<TimePicker>`. DatePicker
  installed but unused (kept for later, per user). Updated `08-SHARED-ASSETS.md` (sakubun now a consumer).
- **Bug: grammar tag in history showed `～のを&nbsp;&nbsp;…`.** Root cause = DATA not render: exactly 1
  of 509 `Item` rows had HTML entities baked in (from the MAZII import). Fixed at the write boundary
  (`lib/text.ts` `cleanText` in `addItems`/`updateItem`) + a one-off cleanup of the live volume DB
  (backup `sakubun.db.bak-cleanup` first).
- **Feature: flag misused/unclear/unknown vocab into the `/vocab` inbox.** New `Item.captureReason`
  column (migration `add_capture_reason`), `add_vocab` MCP tool gains an optional `reason`, tutor
  protocol now proactively surfaces + asks about the 3 categories, `/vocab` (Từ vựng tab) shows a
  reason badge + filter. No new SRS.
- **Rebuilt Docker twice** (`docker compose up -d --build`); container healthy, migration applied to
  the volume, routes 200. Verified via build+typecheck, 91/91 vitest, dirty-row count 0, column round-trip.
- **3 atomic commits** (`feat(ui)` picker · `fix` nbsp · `feat(vocab)` flagging) — `practice.ts` was
  split by temporarily removing the C hunks for the fix commit, then re-adding.

**Decisions made** — see `sakubun/docs/decisions.md` 2026-07-17 entries: "nbsp from MAZII → sanitize at
write choke point" and "problem-vocab → /vocab inbox, not a revived SRS". Cross-project line in
`06-knowledge-ledger.md` A (sanitize scraped text at the write boundary).

**Open threads** — `nuc-platform/08-SHARED-ASSETS.md` (picker catalog line) still uncommitted in the
parent repo. Badge/asking behaviour not eyeballed in-browser (client-rendered) nor exercised live from
Claude Desktop (protocol adherence is prompt-level, not testable). Backup `sakubun.db.bak-cleanup` left
in the volume — safe to delete once the fix is trusted.

## sakubun — quiz reverted fill-in-blank → multiple-choice A–D (live-test diagnosis) — 2026-07-17

```yaml
id: 2026-07-17-02
type: episodic
created: 2026-07-17T16:30:00+07:00
last_accessed: 2026-07-17
importance: 6
milestone_id: null
related_ids:
  [
    { id: 2026-07-17-01, why: 'same day; quiz edits rode the vocab commit 54b0e86' },
    { id: 2026-07-14-02, why: 'reverses the fill-in-blank quiz rework from that session' },
  ]
embedding: null
```

**What happened** — user live-tested the fill-in-blank quiz in Claude Desktop and it was "lệch hoàn
toàn": Claude ANNOUNCED the target pattern in the question ("Điền với mẫu …も…も…ない"), killing the
recall challenge, and picked a trivial pattern (の possessive).

- **Diagnosis (not a stale container):** the deployed protocol ALREADY forbade revealing the pattern —
  verified the running image (grep bundle) served the fill-in-blank text. Root cause = the "hide the
  pattern" rule was a prose line buried at the bottom of the one long combined `SESSION_PROTOCOL`, no
  concrete counter-example → model ignored it.
- **User's actual want (clarified via a previews question):** they DON'T want to type — they want to
  PICK A/B/C/D. Chose single-blank MC: ONE sentence, ONE blank, EXACTLY 4 grammar-PATTERN options
  (correct due pattern + 3 Claude-built confusable distractors, prefer `enrichment.contrasts`). This is
  the design the original 2026-07-13 quiz plan specified — fill-in-blank was a 1-week detour.
- **Implemented:** rewrote `QUIZ_PROTOCOL` (MC; critical "never reveal / always 4 options" rules moved
  to TOP + invariants), updated `get_quiz_items`/`quiz`/`practice` descs, `catalog.ts` (2), `/guide`
  quiz Topic. Lint clean · build OK · 91/91 vitest · rebuilt Docker (image hash unchanged from the
  verified build) · grep-confirmed MC in the running container.
- **Commit reality:** user committed concurrently — the quiz edits got swept into their `54b0e86
  feat(vocab)` commit (not a separate quiz commit). Verified present in HEAD, nothing lost. HEAD is
  3 commits ahead of origin (unpushed).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-17 "Quiz reverted … to multiple-choice A–D".
`00-map.md` (MCP-server + Settings rows), sakubun `CLAUDE.md`, plan `2026-07-13-quiz-and-enrichment`
(revert note) all realigned. Cross-project line in `06-knowledge-ledger.md` A (prompt-adherence).

**Open threads** — quiz MC not yet exercised live in Claude Desktop (user to re-test: run prompt `quiz`
/ say "luyện quiz"). Trivial-pattern selection (の) is a store-curation issue, not protocol — user may
suspend/remove such items. 2nd quiz-UX reversal in a week — design still churning.
