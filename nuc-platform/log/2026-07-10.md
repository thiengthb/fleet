<!-- Daily recall log — schema in log/README.md -->

## sakubun learning-depth upgrade: focused practice + research-grounded deep loop — 2026-07-10

```yaml
id: 2026-07-10-01
type: reflection
created: 2026-07-10T19:00:00+07:00
last_accessed: 2026-07-10
importance: 7
milestone_id: sakubun-learning-depth
related_ids:
  [
    { id: 2026-07-10-02, why: 'focused-practice episodic of this pass' },
    { id: 2026-07-10-03, why: 'deep-learning-loop episodic of this pass' },
    { id: 2026-07-09-01, why: 'previous milestone sakubun-grammar-pivot this builds on' },
  ]
embedding: null
```

**What happened** — one-day pass adding two UX / learning-effectiveness features on top of the
07-09 grammar pivot, both riding the existing 3-layer display enforcement (no schema migration, no
new DB tables):

- **Focused practice** — `get_due_items` gained `focusLevels` / `focusPatterns`; a session-start
  menu ("Hôm nay luyện gì?" — random / N-level / specific patterns) lets the user steer what to
  drill. Requested patterns jump the queue (incl. early reviews + beyond-levels new), quotas still
  apply, filler marked `bổ sung`. Commit `c4d0d27`.
- **Deep learning loop** (research-grounded, A + B-lite at the brainstorm gate) — 🔁 rewrite offer
  on a failed sentence (conversational-only, never re-graded), direct-correction + one-rule-line
  feedback, weak-spot–targeted composition (⚔️ top-2 error tags + per-item "lần trước sai"), daily
  `sessionGoal` progress "câu X/N". Plan `sakubun/docs/plans/2026-07-10-deep-learning-loop.md`
  (done). Commit `1b60364`.
- Committed the leftover planning-sheet knobs from the 07-09 wrap (vocab ceiling + char length) as
  its own commit `7059179` — the pending diff was split into **3 feature-atomic commits** by
  reconstructing each intermediate state (backup → revert → stage → restore).
- Verified at 3 layers each: 54/54 unit, live read-only cases against the container, model-in-the-
  loop eval (scenarios D + E, both PASS). Deep-loop live-verify used a sacrificial item, cleaned to
  zero residue.

**Decisions made** — `sakubun/docs/decisions.md` 2 new entries (2026-07-10 focused-practice;
2026-07-10 deep-learning-loop). Eval doc gained scenarios D + E. `00-map` flow + module map +
settings updated. 1 ledger line (feature-atomic commit split technique).

**Open threads** —

- User should run a full session in a NEW Claude Desktop conversation to confirm the focus menu +
  retry + weak-spot targeting behave in the real client (evals passed, Desktop is ground truth).
- Deferred (research-backed, out of scope this round): pretest-first mini-lesson flip; web
  review/history UI (option C). Queue as ideas if the loop sticks.
- Platform-repo edits still uncommitted for user review (this log, ledger line, INVENTORY, memory).
- sakubun still has no GitHub remote; NUC deploy deferred.

## focused practice: explicit ask overrides schedule gates, not quotas — 2026-07-10

```yaml
id: 2026-07-10-02
type: episodic
created: 2026-07-10T19:00:00+07:00
last_accessed: 2026-07-10
importance: 6
milestone_id: sakubun-learning-depth
related_ids: []
embedding: null
```

**What happened** —

- `pickBatch` gained a `focus` arg; `getDueBatch(now, maxItems, focus)` merges matching rows into
  the pools by id-dedupe (the general `take: 50` query could truncate them). Focus items jump the
  queue: due → learned-but-ahead (early review) → new; then the general FSRS pool tops the sentence
  up to the length budget.
- Legibility echoed back: `focus.matched` / `unmatchedPatterns` + sheet warnings distinguish
  "pattern not in store" (offer add_grammar) from "quota-blocked" — same lesson as `newBeyondLevels`.
- Session-start menu + mid-session switch live in `PRACTICE_PROTOCOL`; eval scenario D confirmed the
  model asks the menu, stops, and maps 4 → `focusLevels:["N3"]`, 6+patterns → `focusPatterns`.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-10 focused-practice entry.

**Open threads** — none beyond the reflection's.

## deep learning loop: rewrite is conversational-only, weak spots feed composition — 2026-07-10

```yaml
id: 2026-07-10-03
type: episodic
created: 2026-07-10T19:00:00+07:00
last_accessed: 2026-07-10
importance: 7
milestone_id: sakubun-learning-depth
related_ids: []
embedding: null
```

**What happened** —

- Standard-tier research (1 haiku scout → 5 sonnet fetch/distill → opus synth): testing-effect
  review, written-corrective-feedback study, Duolingo method, LLM-tutor position paper, CEFR
  alignment-drift paper. Findings → plan `2026-07-10-deep-learning-loop.md` (≥2 sources, ≥2 ruled-out
  options), accepted at the brainstorm gate as A + B-lite.
- Built: `renderGradeResult` conditional 🔁 retry line + "câu X/goal" progress + goal-reached
  celebration; `PRACTICE_PROTOCOL` step-4 direct-correction+rule mandate and step-7 retry rule (NO
  second submit_review); `recentWeakSpots` (top-2 tags, 14 days + per-item misses) rendered as ⚔️
  on the planning sheet; `sessionGoal` setting + form field.
- Key design calls: rewrite lives purely in conversation (rating finality is an invariant — no DB
  column, YAGNI held); weak-spot steering capped at 2 tags with "bẫy công bằng" wording to avoid
  over-steering; `sessionGoal` is a DAILY goal (reuses `todayCounts`, not per-chat tracking).
- Alignment-drift paper validated NOT changing anything: per-turn planning-sheet re-injection is
  already the mitigation the literature calls for — don't move constraints to a session-start prompt.
- Ops note: row-deletes for sacrificial cleanup work via `docker exec sakubun node -e` on the LIVE
  db; the stop→cp→chown dance (07-07) is only for whole-file replacement.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-10 deep-learning-loop entry; eval
scenario E.

**Open threads** — watch retry-skip usage; if the model over-steers to the same trap every
sentence, tighten the weak-spot wording (the cap is the guard).

## sakubun UX + integrity pass: pagination reuse · items 200-cap · pre-commit reference · menu order — 2026-07-10

```yaml
id: 2026-07-10-04
type: episodic
created: 2026-07-10T23:30:00+07:00
last_accessed: 2026-07-10
importance: 6
milestone_id: sakubun-learning-depth
related_ids: []
embedding: null
```

**What happened** — a second same-day pass, four user-reported items:

- **Reusable pagination** — new `data-pagination` in `ui-kit` (shadcn registry: controlled
  page/pageCount/onPageChange + `…` window), built + `shadcn build`, copied into sakubun, registered
  in `08-SHARED-ASSETS.md`. First build lives in ui-kit by user directive (platform-wide reuse).
- **`/items` 200-cap bug** — `listItems({ limit: 200 })` silently hid 300+ of 500+ patterns while
  the dashboard counted all. Fixed: fetch whole store (cap 10 000 guard) + client-side paginate
  50/page in a new `ItemsTable`. Clamp page during render, not in an effect (React 19 lint).
- **Pre-commit reference (`stage_sentence`)** — the anti-contamination fix the user asked for. Loop
  is now get_due_items → stage_sentence (persists question + reference BEFORE showing, returns
  sentenceId) → submit_review (sentenceId only, reuses staged reference, rejects re-submit). No
  schema migration (reuse `Sentence` row, `userAnswer=''` = pending marker).
- **Session-start menu** — was buried at the BOTTOM of `PRACTICE_PROTOCOL`; the model ran the loop
  first and skipped it. Moved to the top ("LÀM ĐẦU TIÊN"). Section order is load-bearing.

**Verified** — tsc + lint + build clean, 54/54 unit; staging live-verified with 4 cases on a
throwaway dev.db COPY (reference reused, double-submit + bogus-id rejected — real dev.db untouched);
model-in-the-loop eval scenarios F (menu order) + G (staging) both 3/3.

**Decisions made** — `sakubun/docs/decisions.md` 2 new entries (2026-07-10 pre-commit reference;
2026-07-10 pagination + 200-cap + protocol order). `00-map` flow updated. 2 ledger lines (pagination
reuse; section-order lesson). `08-SHARED-ASSETS` data-pagination row.

**Open threads** —

- Real Claude Desktop run still the ground truth (evals passed; Desktop confirms the 3-tool loop +
  menu behave live). The running sakubun container has the OLD image — a rebuild is needed before a
  live Desktop test picks up staging/menu changes.
- Abandoned sessions leave pending `Sentence` rows (userAnswer '') — harmless; add cleanup only if
  they pile up.
- ui-kit `data-pagination` reused only 1× (sakubun) — REGISTRY row, watch for the 2nd consumer.
- Platform-repo edits (this log, ledger, 08-SHARED-ASSETS) uncommitted for user review, as usual.
