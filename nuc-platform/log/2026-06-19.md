<!-- Recall tier (temporal). Schema: log/README.md. NEVER auto-loaded; durable lessons are distilled UP into the ledger. -->

## Closed-loop driver — Phases 1+2 wired (idea→plan→arm bridge + reflection) — 2026-06-19

```yaml
id: 2026-06-19-01
type: episodic
created: 2026-06-19T07:20:00+07:00
last_accessed: 2026-06-19
importance: 8
milestone_id: closed-loop-driver
related_ids: [{id: 2026-06-18-01, why: "Phase 0 + capstone plan this continues"}]
embedding: null
```

**What happened**
- Continued the capstone `plans/2026-06-18-closed-loop-driver.md` after a compact. Built + committed **Phase 1** and **Phase 2**, all in the scheduled wrapper `auto-pilot-scheduled.ps1` (interactive, supervisor-approved direct edits per #69):
  - **S1.1** (`fac1242`) — graduation: an accepted-but-ungraduated idea (`outcome: accept` ABOVE `## Done`) → one bounded batch writes a **draft** plan (`auto_pilot: false`), idempotent, parks for the enrol gate. `Test-HasUngraduatedAccept` + `Invoke-AutonomousClaude` helper.
  - **S1.2** (`33928e2`) — planning Q&A: gate-repo pull/push wrap so the graduation batch can ask via `ask-cli` and resume with the answer next cycle; step-0 RESUME + pending-ask guard (no duplicate asks/plans).
  - **S1.3** (`4368104`) — enrol gate: graduation marks the draft `enrol: pending`; a new enrol phase asks Discord (`enrol|not yet|reject`) and applies the SIGNED answer (enrol⇒`active`+`auto_pilot:true`). Chose `ask-cli` (no hook side-effect, no bot change) over `gate-cli`. Hook-level enforcement of arming PROPOSED for human commit (`proposals/2026-06-19-enrol-gate-hook-hardening.md`).
  - **Phase 2** (`7f94c8a`) — one end-of-cycle `reflect` batch (only when the cycle did work): `/session-wrap` distil + EXTERNALLY-grounded retro filing propose-only inbox ideas + `/idea sort` + one Discord digest with the next-action.
- Also committed `ab9f05a`: the overnight C3 autonomous sort (2026-06-19, no new gaps) — the loop is alive.

**Decisions made** — distilled up into `06-knowledge-ledger.md` §A (#70 the idea→plan→arm bridge design + ask-cli/gate-cli split; #71 wiring-verified ≠ runtime-verified + line-anchor regex over a markdown queue).

**Runtime verification (added end of session)** — ran a LIVE local graduation smoke test (synthetic accepted idea-9999, sonnet worker, throwaway branch). Result: graduation produced a correct draft plan (`status: draft` / `auto_pilot: false` / `enrol: pending` / `idea_ref`), with NO spurious planning question (scope was clear, so S1.2 correctly stayed silent); the enrol batch then minted `ASK-enrol-*` + PARKED, leaving the plan **un-armed** (no signed answer ⇒ no `auto_pilot: true`) — the core safety invariant held. **Bug caught + fixed (commit `1dd928c`):** `Test-HasUngraduatedAccept` matched the Rules-prose `## Done` substring (added in S1.1 docs) → scan region collapsed to the header → graduation would NEVER have fired in production. Anchored the divider to `^## Done`. The S1.1 dry-run "skipped correctly" had been a FALSE NEGATIVE; the unit-test missed it (synthetic line arrays lacked the prose). Test branches + synthetic idea + stray ask-state all cleaned. ⇒ **Phase 1 is now runtime-verified**, not just wired.

**S3.3 enrol round-trip — PROVEN LIVE THROUGH DISCORD (end of session)** — provisioning was already in place (gate-clone → private `nuc-agent-gates`; bot `nuc-ops-bot` Up+healthy, polls every 25s via GitHub API). Ran a synthetic demo idea on a throwaway branch: graduation → draft plan → enrol batch minted `ASK-enrol-b3e7d2` + pushed → bot posted the Discord card → supervisor clicked **enrol** → bot wrote the RS256-signed answer → `ask-cli check` verified it → the next cycle ARMED the plan (`status: active` + `auto_pilot: true`) + consumed the ask. Full chain `accept → graduate → enrol-ask → Discord click → signed answer → arm` works end-to-end with cryptographic authorization. Cleaned up; authoritative dry-run confirms opted-in=0, zero residue. **Reliability finding (now plan S3.4):** `Invoke-GatePush` silently swallowed a failed `git push` once — the ask was committed locally but didn't reach the remote until pushed by hand; in production a worker would park forever. The bot's old 2026-06-17 `401` was transient (the round-trip proves the token works now).

**S3.4(a) push-reliability — FIXED 2026-06-19.** Vá `Invoke-GatePush`. Root cause sâu hơn `catch {}`: trong PowerShell `git push` FAIL KHÔNG THROW (chỉ set `$LASTEXITCODE`), nên catch chẳng bao giờ bắt → push fail luôn bị nuốt im. Fix: kiểm `$LASTEXITCODE`, retry 1 lần sau `pull --ff-only` (case non-ff khi bot push song song), fail cuối → hét `WARNING: gate push FAILED` + trả `$false` (commit local giữ → cycle sau retry). 3 caller hứng `$null = Invoke-GatePush`. Runtime-verified trong temp-git sandbox (clean→no-op True · good remote→True+reached · broken remote→False+warning+commit preserved). Plan S3.4 → [~] (phần (a) xong; (b) verify-landed còn mở, gộp với S3.2 watchdog).

**S3.2 watchdog — DONE 2026-06-19** + **S3.4(b) — DONE**. Phase-4 block trong `auto-pilot-scheduled.ps1`: progress fingerprint = multiset sorted của TẤT CẢ branch tip (`git for-each-ref refs/heads`); cycle "did work" mà không nhúc nhích tip nào = no-progress. Phân biệt 2 ca KHÔNG phải stall để không báo nhầm: (a) parked đúng trên 1 quyết định human (`current-ask`/`current-gate` tồn tại + `*-cli check`==none) → reset; (b) human ĐÃ trả lời/duyệt mà worker chưa consume (answered ask / `approve` gate chưa cross) → coi là stall mạnh (đúng cái AC gọi "gate approved but not crossed"). Escalate qua `ask-cli report`→Discord chỉ sau `StallThreshold=2` cycle no-progress-không-parked liên tiếp, và CHỈ 1 lần (`escalated` flag, chỉ set nếu publish push thành công → không thì retry cycle sau). State ở `~/.claude/auto-pilot-logs/watchdog-state.json` (machine-bound). S3.4(b): `Invoke-GatePush` giờ phân biệt "chưa provision" (dir vắng → no-op đúng) vs "provision-nhưng-hỏng" (dir có asks/reports chờ nhưng không `.git` → WARN + `$false`, không nuốt im). Cũng sửa 1 lỗi tự gây: `2>&1` trên native git (trap #60 dưới Start-Transcript) → `| Out-Null` + `$LASTEXITCODE`. Verify: unit-test 10 scenario của decision-fn (PASS) + git-for-each-ref/JSON-roundtrip real-I/O + S3.4b glob + dry-run wiring. **Plan: S3.2 [x], S3.4 [x]. Phase 3 còn lại: chỉ S3.1 (retry-cap=3 + escalate) + full-S3.3.** Lưu ý: escalation-publish path (report→GatePush) là cùng plumbing đã proven live ở S3.3, nhưng watchdog với `didWork=true` thật chưa chạy live (cùng hạng wiring≠runtime #71 — sẽ tự kiểm khi cycle thật đầu tiên có việc).

**Open threads**
- **Phase 2 reflect batch not yet runtime-run** (graduation+enrol now are, incl. the live Discord round-trip). Full S3.3 still wants the planning-Q&A round-trip (demo had no ambiguity) + a single-transcript execute→wrap→retro→next-ask.
- Phase 3 remaining: S3.1 retry-cap, S3.2 watchdog, **S3.4 gate-push reliability** (silent push-failure).
- Phase 3 remaining: S3.1 retry-cap=3 + escalate (the full-cycle ORDER is already wired), S3.2 watchdog (stall detection), S3.3 live demo.
- `proposals/2026-06-19-enrol-gate-hook-hardening.md` awaits human commit (governance — agent must not self-edit `autonomy-gate.mjs`).
- Branch-state coherence across parallel `auto/*` branches still unresolved (Phase 3 risk, noted in plan).
- Still pending from before: Phase-5 skill-law `proposals/*.proposed` unapplied; idea-0013/0014 await the supervisor gate.
