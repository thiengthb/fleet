# 2026-06-16

<!-- Recall tier — dated digests. NEVER auto-loaded; read on demand. Schema + rules: README.md -->

## B5 full unattended auto-pilot loop PASSED (supervised, approve-from-phone) — Layer B complete

```yaml
id: 2026-06-16-01
type: episodic
created: 2026-06-16T11:00:00+07:00
last_accessed: 2026-06-16
importance: 8
milestone_id: agent-os-evolution
related_ids:
  - { id: 2026-06-15-02, why: 'B4b.3 proved the gate protocol once; this is the first full auto-pilot LOOP end-to-end + where finding #4 was set to be watched' }
  - { id: 2026-06-14-00, why: 'rolls up into the agent-os anchor' }
embedding: null
```

**What happened**

- Ran **B5** — the full unattended auto-pilot loop on a throwaway low-risk plan (`plans/2026-06-16-b5-loop-smoke.md`,
  cleaned up after), supervised, approve-from-phone. **Result: PASSED.** 3 fresh-context Sonnet batches via
  `auto-pilot-run.ps1`: batch-1 advanced 4 safe-zone steps incl. a **subagent-delegated wide read** (Explore/sonnet read
  36 `SKILL.md` → index) + parked at the PR gate + published request `GATE-b5-loop-smoke-4f9e2a`; **Duyệt from phone** →
  bot-signed APPROVE token verified; batch-3 crossed **exactly** `git push origin auto/b5-loop-smoke` + `gh pr create`
  (PR #2) → `consume`. **Independently verified (NOT the worker digest): `main` untouched local+origin (`d9c1cd8`), token
  single-use consumed, only the `auto/` branch pushed → zero T4 crossed.**
- **Finding #4 REPRODUCED** on the cross step: the 1st fresh Sonnet worker received a valid `approve` from
  `gate-cli check` but hand-re-verified against the WRONG relative path (`agent-gates/gates/` vs `~/.claude/agent-gates/`),
  overrode the correct result to `none`, and parked; a 2nd fresh worker crossed correctly. Hook held throughout — the
  weak link is the Sonnet worker's judgment over-thinking deterministic tool output, not the gate.
- **Re-hit the documented 2026-06-15 trap** (ledger #60): piping the orchestrator through `2>&1 | Tee-Object` killed the
  first launch (inner `& claude` native-exe stderr + the script's `$ErrorActionPreference=Stop` → terminating error,
  silent worker death). Fixed by running plainly (no `2>&1`), foreground, `-MaxBatches 1` per call.

**Decisions made** (distilled upward — don't duplicate here)

- `plans/2026-06-14-autonomous-agent.md`: B5 → `[x]` (PASSED, independently verified); finding #4 reproduction + the
  proposed SKILL fix; the operator trap note.
- Ledger §A line to add at `/session-wrap`: "offline-passing + one-click-passing still ≠ loop-reliable — the model's
  judgment on deterministic tool output is the residual risk; the fix is to FORBID hand-re-verification, not add checks."

**Open threads**

- **Finding #4 SKILL fix (governance — human commits):** `/auto-pilot` Step 1.5 should tell the worker to TRUST
  `gate-cli check`'s one-word output verbatim and NOT hand-re-verify the gates dir; and/or run the single crossing batch
  on Opus (R3). Candidate for `/skill-proposer` or a direct human edit.
- **Layer C3** (unattended proposer integration) = the remaining optional residue of the autonomy plan, gated on lived-in B5.
- This session's recording commit (plan + this day-log) not yet pushed to origin/main — user's call.
