# 2026-07-20

## sakubun — product-completion pass: guide rebuilt, emoji banned, item page built then reverted — 2026-07-20

```yaml
id: 2026-07-20-01
type: episodic
created: 2026-07-20T08:45:00+07:00
last_accessed: 2026-07-20
importance: 9
milestone_id: null
related_ids:
  [
    { id: 2026-07-19-05, why: 'the previous pass; its "run the thing" lesson recurred here in a sharper form' },
  ]
embedding: null
```

**What happened** — a long pass against `docs/plans/2026-07-20-product-completion-guide-lesson.md` (accepted at the start of the session). ~31 commits on `main` in sakubun + 1 in ui-kit. F0-F3 landed and stuck; F4 landed and was then **reverted at the user's request**.

- **F0 (quick wins), F1 (vocab bug), F2 (audio/furigana everywhere), F3 (guide rebuild) — all shipped and kept.** The guide fix was the reported defect: the "Bắt đầu" tutorial ended by telling the reader to run `/practice` while the MCP configuration that creates it lived on a sibling tab the tutorial never sent them to. Diátaxis names this exactly — a tutorial must be self-sufficient and linear. Fixed by moving MCP setup to step 2 and adding required "Cần trước đó"/"Xong chưa?" blocks, enforced by a `TutorialStep` type rather than a convention.
- **The reported vocab bug had a different root cause than the plan predicted.** The plan asserted Claude was storing polluted terms (`「言葉」(chưa hiểu)`). Checking the data first found the library holds ONE vocab row and it is clean. The real defect was presentational: the capture-reason badge rendered INSIDE the "Từ" cell, glued to the word, so "Chưa hiểu" read as part of the vocabulary's name. Gave the reason its own column. The write-boundary sanitizer was kept as defence-in-depth but correctly labelled as NOT the reported bug.
- **Emoji banned project-wide at the user's direction, and enforced by a test.** ~165 emoji removed from code, UI and the MCP protocol; verified live (21 tools and the 21,427-char practice protocol both return zero). Replacements were meaning-preserving, not deletions: markdown headings for block markers, bold labels for section markers, plain `1.`-`7.` for the focus menu, "Đúng/Gần đúng/Sai" for grading symbols. `lib/no-emoji.test.ts` was verified to actually fail by planting a tripwire.
- **F4 (item page as a lesson) was built, critiqued by the user, redesigned with research, rebuilt — then reverted to the original two-tab layout.** Three separate corrections from the user in one session, all the same underlying mistake on my side: building before reading/searching what already existed.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-20, four entries: "A tutorial surface must be self-sufficient" · "Check the data before building the fix the theory implies" · "Emoji were decoration posing as structure — banned and gated by a test" · "Rearranging a UI is not a feature: the F4 build-and-revert". Cross-project lines -> `06-knowledge-ledger.md` A.

**Open threads** — **F5 not started**: backup/restore (still the real production gap — no export path exists for the SQLite volume), a11y/responsive sweep, the 602-tok `get_due_items` JSON trim carried over from 2026-07-19. **17 commits unpushed** in sakubun, 1 in ui-kit. **The user has still not run a real study session**, so anything that renders review history remains unverified against real data. **The user ended the session dissatisfied** and was asked what specifically fell short (real learning features vs UI quality vs rework rate) — that answer should shape F5 and is not yet given.
