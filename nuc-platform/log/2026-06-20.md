<!-- One file/day; multiple entries = multiple blocks. Schema: see README.md. -->

## Harness self-review: research migrate-vs-keep, trim drift — 2026-06-20

```yaml
id: 2026-06-20-01
type: episodic # episodic | reflection (milestone anchor)
created: 2026-06-20T07:40:00+07:00
last_accessed: 2026-06-20
importance: 6 # 1–10, Park poignancy at write
milestone_id: null # point UP to a reflection entry's id once a harness-maturity milestone is anchored
related_ids: []
embedding: null
```

**What happened**

- User asked (strategically): is there a more mature agent-"harness" / context-engineering model to MIGRATE to so the
  hand-built platform (skills, CLAUDE.md rules, git-synced memory, autonomy gate, idea-queue, self-improvement loop)
  isn't wasted effort — and will Anthropic natively solve this, making skills/rules redundant?
- Ran the `deep-research` workflow. It was interrupted twice by session-suspend (spanned 2 days); recovered by
  **re-passing `args` on resume** then **salvaging the journal via one Sonnet subagent** instead of re-running the
  expensive synthesis. Net finding: **don't migrate** — the hand-built parts already sit on Anthropic-native primitives;
  no single "better harness" exists (frameworks like LangGraph are a *different* thing = programmatic orchestration).
- Structural drift audit: harness is healthy (pointers intact, 37 skills aligned, memory clean). Only real finding =
  CLAUDE.md size budget. Trimmed `todo/CLAUDE.md` 335→274 (§12 UI-shell → `docs/04-features-spec.md`; §4 stale
  hand-copied schema → pointer to `prisma/schema.prisma`, **fixed real drift: 2 vs 16 models**).
- Recorded the Agent-SDK-rewrite REJECTION into `plans/2026-06-14-autonomous-agent.md` Out-of-scope (research-grounded).
- Shipped both as PRs: `todo#1` (trim) + `miniserver-platform#14` (plan decision). Not merged (user merges).

**Decisions made** (distilled upward — pointers, not copies)

- Agent-SDK executor rewrite REJECTED → `plans/2026-06-14-autonomous-agent.md` "Out of scope" (full why there).
- §4 schema-duplication drift pitfall → `todo/docs/decisions.md` (2026-06-20 entry).
- Cross-project lessons → `06-knowledge-ledger.md §A` (2 lines: keep-don't-migrate; Workflow resume/salvage).

**Open threads**

- PRs `todo#1` + `platform#14` awaiting human review + merge (merging `todo#1` → CI build + Watchtower, docs-only/harmless).
- `.claude/workflows/` left untracked — offered to gitignore, awaiting user.
- Optional: trim `todo/CLAUDE.md` further below 200 by moving §6 JSON / §16 table (declined — they're load-bearing invariants).
- Autonomous-agent plan still `active` (B4 Discord control-plane, C3 unattended proposer open).
