<!-- One file/day; multiple entries = multiple blocks. Schema: see README.md. -->

## sakubun — resources merge + edit/delete + analytics + Claude-orange theme — 2026-07-13

```yaml
id: 2026-07-13-01
type: episodic
created: 2026-07-13T14:30:00+07:00
last_accessed: 2026-07-13
importance: 6
milestone_id: null
related_ids: [{ id: 2026-07-12-03, why: 'same continued session, next batch of sakubun UI work' }]
embedding: null
```

**What happened**

- Merged `/items` + `/vocab` → one `/resources` page (tabs **beside** the title); old routes redirect. Per-row `⋮` menu (Sửa / Tạm ngưng / Xóa) via `row-actions` + `item-edit-dialog`.
- Item **edit** = content-only (FSRS schedule locked at the service layer + action Zod). **Delete keeps history**: `ReviewLog.itemId` → nullable + `onDelete: SetNull` (migration `reviewlog_setnull`, copies rows first) so invariant #2 holds; added null-item guards in `getWeakReport`/`listSentences`/`recentContexts`/`weakSpots`.
- Dashboard `/` → **analytics** (`lib/analytics.ts`): 12-week heatmap, 8-week accuracy trend, JLPT × FSRS-state distribution, 14-day forecast, 30-day totals (pure CSS, no chart dep).
- **Claude-orange theme** (`globals.css`, terracotta compiles to ~`#ca653c`/`#e7885d`; warm-biased neutrals; `--warn` nudged off the brand orange) + 作文 orange-chip logo.

**Decisions made** — `docs/decisions.md` 2026-07-13 "Resources merge + item edit/delete (ReviewLog SetNull) + analytics dashboard + Claude-orange theme". Invariant #2 reworded in `docs/00-map.md` + `sakubun/CLAUDE.md`.

**Open threads** — delete not click-tested through the UI (server action, hard via curl) → user to verify history survives a delete. Nothing pushed.

## sakubun — quiz mode + enrichment structure + settings-by-mode — 2026-07-13

```yaml
id: 2026-07-13-02
type: episodic
created: 2026-07-13T17:00:00+07:00
last_accessed: 2026-07-13
importance: 7
milestone_id: null
related_ids: [{ id: 2026-07-13-01, why: 'same session, continues sakubun feature work' }]
embedding: null
```

**What happened**

- Added a multiple-choice cloze **QUIZ** mode alongside translation. `settings.mode` (translate | quiz_grammar | quiz_vocab) + **adaptive settings form** (mode selector; translate-only fields hide in quiz) + `quizItemsPerQuestion`.
- Quiz MCP: `getQuizBatch` + `get_quiz_items` tool + `quiz` prompt (`QUIZ_PROTOCOL`): one sentence, one `(___)` blank per batch item, 4 fill-SETS A–D, answer HIDDEN (frozen in `stage_sentence.suggested`), **per-item nuanced rating** (a set right for item 1 / wrong for item 2 → Good vs Again; wrong pick ≠ everything fails).
- Enrichment gained **`structure`** (cấu trúc/cách chia — Bunpro "GrammarStructure") + **`examples[]`**. Grammar/vocab **detail dialog on click-the-term** (`item-detail-dialog`, shows FSRS status + full card; replaced + deleted the sparkles `EnrichmentDialog`).
- **Zero DB migration** for the whole batch — `Settings` + `Item.enrichment` are JSON blobs, quiz reuses existing items + FSRS + stage/submit. Research-grounded (Bunpro cloze cards; FSRS 4-rating maps onto MCQ).

**Decisions made** — `docs/decisions.md` 2026-07-13 "Quiz mode + settings-by-mode + enrichment structure (all schema-free)".

**Open threads** — quiz conversational loop verified only at the data layer (`get_quiz_items` returns due items); user to test the full prompt flow in Claude Desktop (does Claude hide the answer + rate per-item?). If the multi-blank/4-set format confuses, fall back to one focal blank/question. `structure` needs backfill via `enrich_backlog` on old cards. Plan `docs/plans/2026-07-13-quiz-and-enrichment.md` active.

**Follow-up (same day, 2026-07-13)** — user reported two real bugs from a live run: (1) with mode=quiz_grammar, saying "bắt đầu" ran the TRANSLATION loop / Claude even flipped the mode; (2) quiz emitted 3 separate questions instead of one sentence. Root cause: `mode` was an app setting but the tutor behaviour was driven purely by WHICH prompt ran, and `PRACTICE_PROTOCOL` never read `mode`. Fix: unified both `practice`+`quiz` prompts under a new `MODE_DISPATCH` (reads `settings.mode` first, routes to dịch/quiz, FORBIDS auto-switching the user's mode) → single `SESSION_PROTOCOL`; `QUIZ_PROTOCOL` hardened to "EXACTLY ONE sentence, 2–3 numbered blanks, distractors permute the correct fillers"; `get_due_items` desc now says translate-only. Verified on the running container (practice prompt now carries the dispatch + get_quiz_items markers). Still needs a live Claude Desktop confirmation. Committed + pushed (sakubun 3eaa296).
