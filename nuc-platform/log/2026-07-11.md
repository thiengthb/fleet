<!-- Daily recall log — schema in log/README.md -->

## sakubun 2026-07-11: sentence-length level, UX polish, and MCP id-robustness (two live failures) — 2026-07-11

```yaml
id: 2026-07-11-01
type: reflection
created: 2026-07-11T11:00:00+07:00
last_accessed: 2026-07-11
importance: 7
milestone_id: sakubun-learning-depth
related_ids:
  [
    { id: 2026-07-11-02, why: 'MCP id-discipline episodic (both live failures + fixes)' },
    { id: 2026-07-11-03, why: 'UX polish episodic (length level, truncate, grammar/history)' },
    { id: 2026-07-10-01, why: 'the learning-depth milestone this continues' },
  ]
embedding: null
```

**What happened** — a refinement day on top of the 07-10 learning-depth work: settings/UX polish
plus TWO distinct live MCP model-behavior failures caught from the running container and hardened.
No schema migration; all changes ride the existing 3-tool loop + display contract.

- **Sentence length is a LEVEL again** (short/medium/long → per-language char targets via
  `SENTENCE_CHARS`, density-aware: 文字 denser than VN letters), decoupled from
  `patternsPerSentence` so the "set 5 get 3" bug can't return. User picked this from 3 options.
  Commits `b1ebb18` + `b7ef9c3`.
- **Grammar list truncation** — `table-fixed` + per-column widths + the existing ui-kit `Truncate`
  (ellipsis + tooltip only on real overflow); no more horizontal scroll / dead whitespace. `4e8ee59`.
- **id-discipline hardening #1 (fabricated placeholder)** — Claude Desktop invented
  `sentenceId "pending-manual-review"`; hardened via the escalation ladder (in-result nudge + tool/
  schema descriptions + protocol + error message). `9d260fd`.
- **Grammar/History UI polish** — import button moved into the filter bar (rightmost) + wider modal
  (`bd7e436`); history card de-duplicated (rating badges carry the pattern name, dropped the plain
  collapsed chips) (`fb27622`).
- **id-discipline hardening #2 (missing id, self-heal)** — Claude skipped `stage_sentence` entirely,
  so `submit_review` got NO id and the SDK rejected it before the handler; made `sentenceId` optional
  in the schema + handler returns a self-healing recovery message. Uncommitted at wrap.

**Decisions made** — `sakubun/docs/decisions.md`: three 2026-07-11 entries (length level + truncate;
id fabricated-placeholder hardening; submit_review self-heal on missing id). Eval doc gained scenario
H (+ scenario G criterion tightened for #1). 1 ledger line (required-field dead-end).

**Open threads** —

- Real Claude Desktop run is still ground truth — user to open a NEW conversation, run `practice`,
  and confirm both the fabricated-id and missing-id failures no longer surface (evals passed 3/3 each,
  container rebuilt with both fixes).
- #2 fix (self-heal) is committed-pending: code + decisions + eval doc staged for the user's commit.
- Recurring shape (4× now): live MCP model-behavior bug → escalation-ladder fix → model-in-the-loop
  eval. Candidate for `/skill-proposer` if it recurs again; the eval doc already serves as the runbook.
- sakubun still has no GitHub remote; NUC deploy deferred (local-only by design).

## sakubun MCP id-robustness: fabricated id, then MISSING id — two exits, two fixes — 2026-07-11

```yaml
id: 2026-07-11-02
type: episodic
created: 2026-07-11T11:00:00+07:00
last_accessed: 2026-07-11
importance: 8
milestone_id: sakubun-learning-depth
related_ids: []
embedding: null
```

**What happened** —

- **Failure #1 (fabricated).** Logs: `submit_review error: sentenceId không tồn tại:
  "pending-manual-review"`. Claude copied a "meaningful" placeholder instead of the staged UUID. The
  server rejected it (no garbage written) but the round was lost. Fixed by the escalation ladder,
  strongest layer first: `stage_sentence` now echoes `sentenceId = "<uuid>"` with a copy-verbatim
  instruction in its RESULT text (where the model reads the id), plus tool/schema descriptions +
  protocol steps 3/6 + an invariant + a recovery-worded error. Eval scenario G re-run 3/3 PASS.
- **Failure #2 (missing).** Logs for the round showed ONLY `tool=get_due_items` — Claude skipped
  `stage_sentence`, so it had no id and called `submit_review` with none. Because `sentenceId` was a
  REQUIRED zod field, the **MCP SDK rejected the call at arg-validation BEFORE the handler ran** — the
  bare "missing sentenceId" reached Claude and ALL the Vietnamese recovery guidance (in handler/error)
  never showed; Claude gave up. Fix: `sentenceId` made **optional in the advertised schema** + the
  handler returns a self-healing `mcpError` ("never staged → call stage_sentence with the shown đề →
  get id → re-submit"). Eval scenario H 3/3 PASS — all retro-staged and re-submitted, no fabrication.
- The two are DIFFERENT exits: #1 = a bad value (caught in `submitReview`, our message shows); #2 = no
  value (caught by the SDK, our message DIDN'T show until we made the field optional). Fixing #1 did
  not fix #2 — the model just swapped "invent an id" for "omit the id".

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-11 (both id entries).

**Open threads** — see reflection; Desktop ground-truth confirmation pending.

## sakubun UX polish: length level, grammar truncation, import bar, history de-dup — 2026-07-11

```yaml
id: 2026-07-11-03
type: episodic
created: 2026-07-11T11:00:00+07:00
last_accessed: 2026-07-11
importance: 5
milestone_id: sakubun-learning-depth
related_ids: []
embedding: null
```

**What happened** —

- **Length level** — `sentenceLength` (short/medium/long) resolves to `SENTENCE_CHARS`
  `{ ja, vi }` per direction in `renderDueBatch`; length ONLY, `patternsPerSentence` is its own knob.
  Settings form shows a Ngắn/Vừa/Dài group with a live "Nhật ~N 文字 · Việt ~N ký tự" hint.
- **Grammar list** — `ItemsTable` uses `table-fixed` + per-column % widths; JA/VI cells wrap in the
  ui-kit `Truncate` (reused, not rebuilt). Import trigger moved from the PageHeader action into the
  filter bar (rightmost, `sm:ml-auto`) + `ImportDialog` widened to `max-w-4xl`; empty-store state
  keeps its own import button.
- **History card** — the collapsed summary now shows the rating badges (pattern + Good/Easy, colour-
  coded) that used to sit only in the expanded detail; the plain pattern chips were dropped (they
  duplicated the pattern name). Expanded detail keeps bạn dịch / bản dịch tham khảo / feedback.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-11 length+truncate entry; the two UI-polish
commits (`bd7e436`, `fb27622`) are self-explanatory (no decisions entry — git log covers them).

**Open threads** — UI verified via a static Artifact preview + approved; live container rebuilt.

## sakubun 2026-07-11: practice-flexibility batch + adaptive engine (đợt-1→3) — 2026-07-11

```yaml
id: 2026-07-11-04
type: episodic
created: 2026-07-11T18:55:00+07:00
last_accessed: 2026-07-11
importance: 7
milestone_id: null
related_ids: [{id: 2026-07-11-03, why: "supersedes it — sentenceLength + patternsPerSentence merged into one sentenceSize knob"}]
embedding: null
```

**What happened** —

- **đợt-1** — whole-level focus keeps SCHEDULED slots pure (`fillFromFocusOnly`) + vocab targets the
  study/focus level (fixes "chọn N3 nhưng ra từ vựng N5"); tutor scaffolds with lower UNRATED grammar.
- **sentenceSize** — merged `patternsPerSentence` + `sentenceLength` → ONE derived knob
  (`SENTENCE_SIZE_SPEC`); later extended with `huge`/`paragraph` MULTI-sentence tiers (6/8 patterns).
  Reverses the 2026-07 decouple SAFELY (derived, not conflated → old "set 5 get 3" can't return).
- **History UI** — 4-color FILLED rating chips (word dropped; new blue `--info` token), colored /100,
  "Bản dịch tham khảo"→"Tham khảo". Furigana-on-hover: Claude sends `漢字[かな]`, parsed by
  `lib/furigana.ts` (no JA tokenizer by design).
- **Staged-reference audit** — history shows PENDING cards with the frozen reference; "chỉ xem →
  all-Again" (`skipSentence`); `stage_sentence` gained `items[]` → `Sentence.stagedItems`/`status`;
  migrations `add_sentence_staging` (+status backfill) & `add_source_ruby`.
- **Richness** — protocol licenses varied particles/adverbs/connectives; `difficulty` redefined =
  STRUCTURAL (not vocab); `TOPIC_PRESETS` enriched beginner→advanced.
- **Adaptive engine (level-agnostic, no N3 hardcode)** — `autoLearnSupport` + `submit_review.observedGrammar`
  → `addItems` as New (item creation, NOT a review write → FSRS invariant safe); `set_below_target_suspended`
  hides below-target backlog (reversible).
- 9 feature-atomic commits on branch `feat/practice-flexibility-batch`; container rebuilt (2 migrations
  applied to the volume); 67 tests · lint · build green at each commit.

**Decisions made** — 8 new `sakubun/docs/decisions.md` 2026-07-11 entries (level-focus/vocab ·
sentenceSize merge · cỡ-câu huge/paragraph · history 4-color · staged-audit · furigana ·
richness/difficulty/topics · hide-below-target · auto-learn). Cross-project → `06-knowledge-ledger.md`
(commit-as-you-go). Plan: `sakubun/docs/plans/2026-07-11-practice-flexibility-batch.md`.

**Open threads** —

- Branch NOT pushed — awaiting user's push/PR decision.
- **Runtime behavior NOT yet verified** in a live Claude Desktop session (observedGrammar reporting,
  paragraph composition, richer sentences, skip→Again, furigana render) — code + gate + deploy only.
- Deferred: đợt-3 item-3 DEEP adaptivity (rank next-intro by demonstrated mastery / auto-suspend a low
  pattern after N easy hits) — revisit after real use.
- 2 pre-existing dialog files (`enrichment-dialog`, `import-dialog`) left uncommitted (user's WIP).
