<!-- One file/day; multiple entries = multiple blocks. Schema: see README.md. -->

## sakubun — vocab page + sidebar + lucide-only rule — 2026-07-12

```yaml
id: 2026-07-12-01
type: episodic
created: 2026-07-12T20:00:00+07:00
last_accessed: 2026-07-12
importance: 5
milestone_id: null
related_ids: []
embedding: null
```

**What happened**

- Added `/vocab` page + `VocabTable` to sakubun so looked-up words (`add_vocab`, `kind='VOCAB'`) are finally visible — they were stored since 2026-07-11 but had no browse UI (`/items` is grammar-only).
- Replaced sakubun's top navbar with a collapsible sidebar on the shadcn `sidebar` primitive; extracted the reusable nav as `AppSidebar` (config-driven) into `ui-kit` registry (`shadcn add app-sidebar`).
- Audited icons across all 8 repos → already 100% lucide-only (the one inline `<svg>`, yakudoku `ScoreRing`, is a data-viz gauge, not an icon). Codified the lucide-only rule + its data-viz exception into platform `CLAUDE.md` and `coding-convention/ui-rules.md`.
- Hit + fixed the shadcn-CLI `use-mobile.ts` lint failure (setState-in-effect → `useSyncExternalStore`).

**Decisions made** — sakubun `docs/decisions.md` 2026-07-12 "Vocab gets its own `/vocab` page; navbar → collapsible AppSidebar"; ledger `06 §A` line (AppSidebar registry + lucide-only rule).

**Open threads** — none blocking. Nothing pushed (committed to `main` locally on the 3 repos); vocab page not yet runtime-verified against a live DB with real VOCAB rows (build + lint green only).
