<!-- One file/day; multiple entries = multiple blocks. Schema: see README.md. -->

## sakubun — vocab page + sidebar + lucide-only rule — 2026-07-12

```yaml
id: 2026-07-12-01
type: episodic
created: 2026-07-12T20:00:00+07:00
last_accessed: 2026-07-12
importance: 5
milestone_id: null
related_ids: []
embedding: null
```

**What happened**

- Added `/vocab` page + `VocabTable` to sakubun so looked-up words (`add_vocab`, `kind='VOCAB'`) are finally visible — they were stored since 2026-07-11 but had no browse UI (`/items` is grammar-only).
- Replaced sakubun's top navbar with a collapsible sidebar on the shadcn `sidebar` primitive; extracted the reusable nav as `AppSidebar` (config-driven) into `ui-kit` registry (`shadcn add app-sidebar`).
- Audited icons across all 8 repos → already 100% lucide-only (the one inline `<svg>`, yakudoku `ScoreRing`, is a data-viz gauge, not an icon). Codified the lucide-only rule + its data-viz exception into platform `CLAUDE.md` and `coding-convention/ui-rules.md`.
- Hit + fixed the shadcn-CLI `use-mobile.ts` lint failure (setState-in-effect → `useSyncExternalStore`).

**Decisions made** — sakubun `docs/decisions.md` 2026-07-12 "Vocab gets its own `/vocab` page; navbar → collapsible AppSidebar"; ledger `06 §A` line (AppSidebar registry + lucide-only rule).

**Open threads** — none blocking. Nothing pushed (committed to `main` locally on the 3 repos); vocab page not yet runtime-verified against a live DB with real VOCAB rows (build + lint green only).

## sakubun — UX pass (emoji→lucide, settings tabs, sortable tables) — 2026-07-12

```yaml
id: 2026-07-12-02
type: episodic
created: 2026-07-12T21:30:00+07:00
last_accessed: 2026-07-12
importance: 5
milestone_id: null
related_ids: [{ id: 2026-07-12-01, why: 'same session, continues the sakubun UI work' }]
embedding: null
```

**What happened**

- User caught that "lucide-only" was violated by EMOJI-as-icons in /guide (my first audit only checked icon libraries + `<svg>`). Replaced 🎓/📖/🔊/🈁/✨/⏳ with lucide; kept MCP display-contract emoji.
- Settings page → 3 tabs; shadcn tabs active state made prominent (`bg-primary`); guide Tính năng/Cách hoạt động → accordions with icon+label+summary.
- History card: rating/pattern tags moved up beside the score (saves a row). Items/Vocab tables: sortable columns + relative↔exact due-format toggle + page-size selector (DataPagination extended, ui-kit + copy). Import button "Nhập ngữ pháp" → "Nhập". Filter selects widened (popper + min-width).
- Rebuilt Docker; all routes 200; user approved; committed as 5 sakubun + 1 ui-kit feature commits.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-12 "UX pass…"; ledger `06 §A` emoji-lucide line; `08-SHARED-ASSETS.md` (data-pagination page-size + ItemsTable≈VocabTable extract-candidate).

**Open threads** — ItemsTable/VocabTable are near-dupes → extract a generic `<DataTable>` to ui-kit at the 3rd shape. Docs (this wrap) committed separately; nothing pushed.

## sakubun — study-plan / roadmap (tutor-owned advisory) — 2026-07-12

```yaml
id: 2026-07-12-03
type: episodic
created: 2026-07-12T23:30:00+07:00
last_accessed: 2026-07-12
importance: 6
milestone_id: null
related_ids: [{ id: 2026-07-12-02, why: 'same session, continues sakubun feature work' }]
embedding: null
```

**What happened**

- User articulated sakubun's true shape from a fresh-machine session: **Claude = tutor/companion, app = the shared toolset** (external memory + FSRS + practice harness), NOT a content repository.
- Built a **StudyPlan** (tutor-owned roadmap): `StudyPlan` singleton model + migration, `lib/study-plan{,-schema}.ts`, MCP `get_study_plan`/`update_study_plan` + `onboarding` prompt, `renderStudyPlan` 🗺️ briefing block, read-only `/plan` page + nav + guide topic.
- Design line: **advisory NOTEPAD, not a curriculum engine** — app never auto-introduces, FSRS still owns review scheduling, empty plan = freestyle. Milestones = whole-list replace.

**Decisions made** — `docs/decisions.md` 2026-07-12 "Study plan / roadmap = tutor-owned NOTEPAD, not a curriculum engine".

**Open threads** — cross-machine cold-start NOT solved (local SQLite; NUC sync deferred). Watch whether Claude calls `get_study_plan` at session start unprompted → maybe nudge `PRACTICE_PROTOCOL`. Plan `docs/plans/2026-07-12-study-plan-roadmap.md` active. Nothing pushed.
