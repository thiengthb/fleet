<!-- One file/day; multiple entries = multiple blocks. Schema: see README.md. -->

## sakubun — study-groups fully shipped + multi-user go-live completed (Settings fix, open sign-up, backup, CF Access removed) — 2026-07-22

```yaml
id: 2026-07-22-01
type: episodic
created: 2026-07-22T01:40:00+07:00
last_accessed: 2026-07-22
importance: 8
milestone_id: 2026-07-21-M-multiuser # completes the go-live of the multi-user milestone
related_ids:
  [
    { id: 2026-07-21-04, why: "continues study-groups Slice 1-2 → 3-4" },
    { id: 2026-07-21-M-multiuser, why: "finishes this milestone's go-live open-up (G6)" },
  ]
embedding: null
```

**What happened**

- Study-groups **Slice 3 (MCP) + Slice 4 (guide/docs)** shipped (commits `95e8eec`, `9c9bd93`): 5 tools
  (`list_groups`/`create_group`/`add_items_to_group`/`remove_items_from_group`/`suggest_group`) + `get_due_items{groupId}`
  scoping; guide features Topic + how-to recipe; 26 tools total. 151 tests green. Plan flipped to `done`.
- **Rebuilt the local container twice** (docker compose up --build) to actually run the new image — `migrate deploy`
  applied the group migrations + confirmed health; the "run the thing, don't trust the green build" discipline.
- **Fixed `Settings.id` → autoincrement** (`241106c`): the old `@default(1)` made a 2nd user's settings save collide on
  the PK (blocked multi-user go-live). Migration is a data-preserving SQLite table-rebuild; regression test added.
- **G6 go-live open-up:** open sign-up kept (no invite gate — admin ban + rate-limit are the controls); built a
  sakubun-specific **backup cron** (`scripts/backup-sakubun.sh`, online `.backup()` + integrity + retention, `611c7c1`),
  verified locally; operator **removed Cloudflare Access entirely** (deleted BOTH the host gate AND the api/mcp bypass —
  harmless), verified from outside (`/login`→200, `/api/mcp`→401 JSON). Docs updated (`579f9e8`, trap §14 `f1d4c36`).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-22: (1) Settings.id autoincrement + the migrate-dev-doesn't-regen
gotcha; (2) study-group MCP surface (suggest_group dry-run, groupId focus); (3) G6 open sign-up + dedicated backup cron;
(4) CF Access removed (supersedes the 2026-07-21 api/mcp-bypass entry). Ledger: `06` prisma-generate gotcha + SQLite
online-backup. Trap: `02-known-traps §14` Update 2026-07-22 (CF Access removed from sakubun).

**Open threads** — OPERATOR-ONLY (not agent): install the backup cron on the NUC (script header has the cron.d line);
smoke-test a real friend `/register`. This machine's SSH does NOT resolve `thien25@thienminiserver` (config has
`thientn@{ssh.hungnq.online,sv.hungnq.online,192.168.1.36}`) → agent can't reach the NUC to verify prod DB / install cron
until the correct alias is provided. Plan multi-user stays `active` (Phase 3 public-library + Phase 4 comments pending).

## sakubun — sharing shipped: Phase 3 (public library + fork) + Phase 4 (feed + comments); multi-user plan DONE — 2026-07-22

```yaml
id: 2026-07-22-02
type: episodic
created: 2026-07-22T16:00:00+07:00
last_accessed: 2026-07-22
importance: 8
milestone_id: 2026-07-21-M-multiuser # completes the sharing phases of the multi-user milestone
related_ids:
  [
    { id: 2026-07-22-01, why: "same day: go-live completion → then the sharing phases" },
    { id: 2026-07-21-M-multiuser, why: "Phases 3-4 finish this milestone's plan (now status: done)" },
  ]
embedding: null
```

**What happened**

- **Phase 3 — public library + fork** (commits `fc5ea6f` data, `b7b7caf` UI, `61ce74c` docs): `Item.visibility` +
  `forkedFromId`; `@@unique` moved to `[userId,kind,expression]` (global one blocked forks); `lib/library.ts`
  (RAW-SQL cross-tenant reads, content-only; publish/unpublish/fork); `/library` card gallery + share toggle on the
  item detail. Dropped the plan's redundant `publishedById`.
- **Phase 4 — shared feed + comments** (`6b0fae1` backend, `a4bc2bb` UI, `9fe357b` docs): `Sentence.visibility`/
  `sharedAt` + `Comment` (NOT a tenant model); `lib/feed.ts` (feed raw-SQL, comment authz); a "Cộng đồng" tab on
  `/history`, share toggle in the detail, lazy-loaded comments (add/delete author-or-owner, XSS-safe).
- **Conformed `/library` to the user's new `PageShell` standard** (`b35d8f9`) — the user refactored the frontend in
  PARALLEL this session (PageShell, public landing, groups→a /resources tab, Motion, docker cache); my new page had
  to adopt PageShell + drop its hand-rolled container/title. Left every WIP file of theirs untouched (staged only mine).
- The **multi-user deploy+sharing plan is now `status: done`** (Phases 1–4 complete). 166 tests green throughout.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-22: "Public library (Phase 3)" + "Shared feed + comments
(Phase 4)". Cross-project: `06-knowledge-ledger` — the cross-tenant public/shared read pattern (raw SQL to bypass a
fail-closed tenancy guard, content-only). No new memory, no skill proposal.

**Open threads** — Production applies the Phase-3/4 migrations via CI + Watchtower (mechanism proven; not re-verified
in-container this session because **Docker Desktop's daemon broke locally** mid-rebuild — restart it to get the local
container back). Operator eyeball of /library + the Cộng đồng tab (auth-gated, agent can't screenshot). Two older
plans stay `active` and USER-gated: `adaptive-vocab-sensory` (C0 voice spike) + `quiz-and-enrichment` (observe).

## sakubun — UI layout system: PageShell, landing, sidebar, tabs-beside-breadcrumb + GATE-enforced — 2026-07-22

```yaml
id: 2026-07-22-02
type: episodic
created: 2026-07-22T18:15:00+07:00
last_accessed: 2026-07-22
importance: 7
milestone_id: 2026-07-21-M-multiuser
related_ids:
  [{ id: 2026-07-22-01, why: "same day; this is the frontend the -01 backend session saw as a parallel refactor" }]
embedding: null
```

**What happened** (this was the agent-side of the "parallel frontend refactor" -01 referenced)

- **Public landing page** at `/` for guests (`getOptionalSessionUser` non-redirecting; shell renders `/` chromeless
  from a server-resolved `authed` prop). Added **Motion** as the animation standard (`MotionConfig reducedMotion="user"`
  global + `motion-primitives`).
- **`PageShell`** = the one page wrapper (rhythm + width tier + breadcrumb). Adopted on every page; later made STRICT.
  Sidebar footer → two collapsible action buttons (row expanded / icon column collapsed), logout destructive, logo
  above the collapse toggle. `/groups` list merged into a `/resources` "Nhóm" tab (detail stays `/groups/[id]`).
- **Round 2:** full-width account/admin/library (dropped narrow tiers); AuthNav on login/register; token-modal overflow
  fixed (grid child needed break-all); **`min-w-0` on SidebarInset** fixed the wide-content page "phình" (both scrollbars).
- **Round 3:** `PageShell.headerAside` so tabs sit BESIDE the breadcrumb; fixed history/library/guide (were rendering
  TabsList as content → tabs below); item-analytics onto PageShell; groups tab search+kind filter; in-app add-vocab.
- **The mechanism** (user asked for one so he stops repeating rules): `lib/layout-standard.test.ts` gates the layout
  standard (every page → PageShell; page-level TabsList → headerAside), + `CLAUDE.md` invariant #8. 168 tests green.
- **Docker build sped up**: BuildKit cache mounts (npm + Next) + npm fetch retries. Root-caused two failed rebuilds:
  flaky network (ECONNRESET) then **Docker disk full → read-only FS (EROFS)**; freed ~5.5GB (`builder prune`).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-22: the three "UI layout" entries + "UI layout is GATE-ENFORCED".
Platform: `nuc-platform/12-ui-layout-standard.md` (new standard doc). Memory: `[[enforce-rules-with-gates]]`,
`[[nuc-down-deploy-local-only]]`.

**Open threads** — Agent could NOT screenshot (Claude-in-Chrome extension disconnected all session) → the visual
pieces (tabs-beside, footer collapse, full-width pages, token modal) are operator-eyeball-pending on `localhost:3789`.
NUC still down + no VPS → deploy is LOCAL rebuild only; nothing pushed went live. Docker build cache regrows (~5.9GB) —
`docker builder prune -f` when it fills.

## sakubun — UI polish batch: auto-fit pagination, glow hint, one SearchInput, save-as-action — 2026-07-22

```yaml
id: 2026-07-22-03
type: episodic
created: 2026-07-22T20:40:00+07:00
last_accessed: 2026-07-22
importance: 6
milestone_id: 2026-07-21-M-multiuser
related_ids: [{ id: 2026-07-22-02, why: "continues the same-day UI-consistency work" }]
embedding: null
```

**What happened**
- Settings **Save** → an `ExpandingIconButton` (Save icon, hover label) matching the resources add button.
- **Auto-fit pagination**: `useFitRows` + DataTable default page size fills the viewport (no scroll), overridable.
- **Glow-until-read lightbulb** (`intro-hint`) at the guide tab row; intro moved off the page into its tooltip.
- **One `SearchInput`** everywhere (uniform size); removed the 3 hand-rolled search boxes.
- **Library filter** (level + in-collection, badge + clear). Detail page header/grid/spacing tightened (no rearrange).
- Commits: `250bf9b` (save + auto-fit), `355060f` (library filter), `b2debd2` (lightbulb), `f706a74` (search), `50b5e9c` (detail).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-22 "UI polish pass". Ledger: auto-fit pagination + localStorage-via-useSyncExternalStore.

**Open threads (NOT done — carry forward)** — the user's big batch has 3 items left, all deep in the phase3/4 data layer:
(#5) **share** bulk + per-row on the grammar/vocab tables — needs `listItems` to also return `visibility`+`forkedFromId`,
a per-row + bulk publish gated on `forkedFromId == null` (share only what YOU created), and `publishItem` gated the same;
(#2) history **card icons** — Của tôi (share/lock + comment-count badge), Cộng đồng (comment + user icon w/ name tooltip);
(#3-rest) a filter on the **Cộng đồng** tab. Paused deliberately: the user is committing phase3/4 (feed/auth/sidebar) in
parallel, so these edits to `practice.ts`/`items-table`/`community-feed` risk colliding — confirm before diving in.

## sakubun — auth hardening: email verification, password reset, Google sign-in + sidebar footer — 2026-07-22

```yaml
id: 2026-07-22-04
type: episodic
created: 2026-07-22T22:10:00+07:00
last_accessed: 2026-07-22
importance: 8
milestone_id: 2026-07-21-M-multiuser # auth hardening on top of the multi-user go-live
related_ids:
  [
    { id: 2026-07-22-01, why: "builds on the multi-user go-live (open sign-up → now verified)" },
    { id: 2026-07-22-03, why: "same day; continues the settings/auth surface" },
  ]
embedding: null
```

**What happened**

- **Email verification on sign-up** (`8551db2`): better-auth `requireEmailVerification` + SMTP send via new
  `lib/email.ts` (nodemailer; logs instead of throwing when unconfigured). A **grandfather migration**
  (`20260722090000`) marks pre-existing accounts verified so nobody is locked out. Verified end-to-end on a
  throwaway (sign-up→blocked login→verify link→login).
- **Settings made multi-user-correct** (`47cd752`): dropped the hardcoded `notificationEmail` → Nhịp học shows
  the account email; Save = React 19 form action labelled "Lưu"; Data tab = per-user JSON export
  (`/api/export/mine`, own rows only) for everyone + admin-only whole-DB backup/restore.
- **Password reset + Google sign-in** (`9446a0b`): `sendResetPassword` (1h link) → `/forgot-password` +
  `/reset-password`; Google OAuth opt-in (only when `GOOGLE_CLIENT_ID/SECRET` set) + account-linking by email +
  a "Tiếp tục với Google" button (brand-SVG named exception to lucide-only).
- **Sidebar footer redesign** (`b6fd76f`): theme toggle → a Switch ABOVE an identity card (avatar+name+email)
  with a logout icon button ON THE SAME ROW (per the user's follow-up); logout no longer `destructive`.
- **Google button NEXT_PUBLIC-in-Docker fix** (`106be2e`): `NEXT_PUBLIC_GOOGLE_ENABLED` baked false at
  `next build` (env_file is runtime-only) → SSR showed the button, hydration removed it. Fixed by resolving
  availability SERVER-side (`lib/google-enabled.ts`) and passing a prop; login/register split into
  server-page + client-form.
- **Corrected the deploy model** (`5adf379`): there is NO NUC/VPS — sakubun deploys LOCAL (`sakubun:local` from
  the gitignored `docker-compose.yml`, port 3789, secrets in `.env.docker`) exposed via Cloudflare; a `git push`
  does NOT go live, `docker compose up -d --build` does. Rebuilt + verified live via Cloudflare after each batch.
- **Cleaned prod DB**: removed 2 throwaway test users + 1 typo account (`...@gmal.com`) I'd let slip into the
  PROD volume DB by test-signing-up against the prod container on 3789 (thought it was a dev server). 4 real
  users remain, all verified.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-22: email verification; settings multi-user; auth
reset+google; sidebar footer; "NEXT_PUBLIC baked at build" + deploy-context correction. Ledger: the
NEXT_PUBLIC-in-Docker line. Memory: `verify-target-db-before-test-writes` (new, `95b6ea8`).

**Open threads** — Google needs the user's real login test (redirect-URI correctness is theirs to confirm); an
unused `NEXT_PUBLIC_GOOGLE_ENABLED` line lingers harmlessly in `.env.docker`; the -03 threads (share bulk on
tables, history card icons, Cộng đồng filter) still pending.
