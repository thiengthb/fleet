<!-- One file/day; multiple entries = multiple blocks. Schema: see README.md. -->

## sakubun — study-groups fully shipped + multi-user go-live completed (Settings fix, open sign-up, backup, CF Access removed) — 2026-07-22

```yaml
id: 2026-07-22-01
type: episodic
created: 2026-07-22T01:40:00+07:00
last_accessed: 2026-07-22
importance: 8
milestone_id: 2026-07-21-M-multiuser # completes the go-live of the multi-user milestone
related_ids:
  [
    { id: 2026-07-21-04, why: "continues study-groups Slice 1-2 → 3-4" },
    { id: 2026-07-21-M-multiuser, why: "finishes this milestone's go-live open-up (G6)" },
  ]
embedding: null
```

**What happened**

- Study-groups **Slice 3 (MCP) + Slice 4 (guide/docs)** shipped (commits `95e8eec`, `9c9bd93`): 5 tools
  (`list_groups`/`create_group`/`add_items_to_group`/`remove_items_from_group`/`suggest_group`) + `get_due_items{groupId}`
  scoping; guide features Topic + how-to recipe; 26 tools total. 151 tests green. Plan flipped to `done`.
- **Rebuilt the local container twice** (docker compose up --build) to actually run the new image — `migrate deploy`
  applied the group migrations + confirmed health; the "run the thing, don't trust the green build" discipline.
- **Fixed `Settings.id` → autoincrement** (`241106c`): the old `@default(1)` made a 2nd user's settings save collide on
  the PK (blocked multi-user go-live). Migration is a data-preserving SQLite table-rebuild; regression test added.
- **G6 go-live open-up:** open sign-up kept (no invite gate — admin ban + rate-limit are the controls); built a
  sakubun-specific **backup cron** (`scripts/backup-sakubun.sh`, online `.backup()` + integrity + retention, `611c7c1`),
  verified locally; operator **removed Cloudflare Access entirely** (deleted BOTH the host gate AND the api/mcp bypass —
  harmless), verified from outside (`/login`→200, `/api/mcp`→401 JSON). Docs updated (`579f9e8`, trap §14 `f1d4c36`).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-22: (1) Settings.id autoincrement + the migrate-dev-doesn't-regen
gotcha; (2) study-group MCP surface (suggest_group dry-run, groupId focus); (3) G6 open sign-up + dedicated backup cron;
(4) CF Access removed (supersedes the 2026-07-21 api/mcp-bypass entry). Ledger: `06` prisma-generate gotcha + SQLite
online-backup. Trap: `02-known-traps §14` Update 2026-07-22 (CF Access removed from sakubun).

**Open threads** — OPERATOR-ONLY (not agent): install the backup cron on the NUC (script header has the cron.d line);
smoke-test a real friend `/register`. This machine's SSH does NOT resolve `thien25@thienminiserver` (config has
`thientn@{ssh.hungnq.online,sv.hungnq.online,192.168.1.36}`) → agent can't reach the NUC to verify prod DB / install cron
until the correct alias is provided. Plan multi-user stays `active` (Phase 3 public-library + Phase 4 comments pending).
