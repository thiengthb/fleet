# 2026-07-24

## sakubun — community/library/resources polish + app-wide streak fire + furigana fix — 2026-07-24

```yaml
id: 2026-07-24-01
type: episodic
created: 2026-07-24T00:40:00+07:00
last_accessed: 2026-07-24
importance: 5
milestone_id: null
related_ids: []
embedding: null
```

**What happened** — a long UI-polish pass on sakubun (attended, local-only deploy; NUC still down):

- **Grammar furigana fix** (committed `9bf7767`): `buildFuriganaMarkup` now strips a 〜/～ the reading itself carries; diagnosed the admin account (517 patterns) — 431 kana-only correctly show none, backfilled `reading` for 14 kanji patterns (15 rows across users) directly in the prod-volume DB (backup `/data/sakubun.db.bak-furigana`). Admin furigana render 56→72.
- **Community feed (/history Cộng đồng) rework:** auto-share toggle moved to the header row (community tab only), grid x1/x2 toggle + enhanced filters (level/topic/liked/score) mirroring "Của tôi", author avatars, aligned like/comment/avatar cluster + shared date, removed a duplicate divider in the detail modal, redesigned comment layout (avatar + horizontal vote rail + composer avatar).
- **Shared `UserAvatar`** extracted (photo→initials→icon) — used by sidebar + community + library owner + comments; added `authorImage`/`ownerImage` to the feed + library raw-SQL reads.
- **Library:** owner column avatar, bulk "Thu hồi chia sẻ" for own rows, a "Nguồn chia sẻ" (mine/others) filter; **Resources:** grammar +share/date filters, vocab +level/share/date, groups → FilterPopover (loại/số mục/sắp xếp). Shared `withinDatePreset` helper in `lib/format`.
- **App-wide streak fire:** flame redesigned (layered SVG + Motion celebration with AnimatePresence fade); a new `StreakBeacon` in the app shell polls `getStreakAction` so the fire fires on ANY page; dashboard card is now preview-only (`auto={false}`).
- **History "Của tôi":** newly-graded card auto-opens its modal while on the tab; the PENDING card lost its "Bỏ qua" button (skip is Claude-Desktop-only now, with a notice). **Mobile header:** app icon + inline sidebar trigger + divider at the start of the breadcrumb row (`MobileTopBar`), replacing the floating trigger.
- **MCP one-paste auto-config (#4):** explained only — not buildable via chat; recommended a token-prefilled config block later.

**Decisions made** — see `sakubun/docs/decisions.md` 2026-07-24 entries: app-wide streak beacon; library share-status filter is source-based; grammar furigana depends on Claude-sent reading; web skip removed from the pending card.

**Open threads** — none of this session's UI work is committed yet (awaiting the user's go; must stage ONLY my files — `group-detail-view.tsx`, `lib/mcp/catalog.ts`, `scripts/verify-image.sh` are the user's concurrent work). Optional next builds the user parked: MCP token-prefilled config block (#4 option ①). Still local-only (NUC down) — no push.

## sakubun — viewport-fit pagination + UI polish (buttons/settings/enrich/flashcard/breadcrumb/groups) — 2026-07-24

```yaml
id: 2026-07-24-02
type: episodic
created: 2026-07-24T01:10:00+07:00
last_accessed: 2026-07-24
importance: 5
milestone_id: null
related_ids: [{ id: 2026-07-24-01, why: 'same-day parallel sakubun UI session on different surfaces' }]
embedding: null
```

**What happened** — a multi-request UI-polish pass on sakubun (attended, local-only; NUC down), committed in small clusters, interleaved with the user's heavy parallel editing of ~25 files:

- **Reusable viewport-fit pagination** (`6619a57`, then `53be6d4`): `lib/fit-rows.ts` (pure, unit-tested) + `components/ui/use-fit-rows.ts` (DOM measure) + `components/use-paged-list.ts` (client card lists); wired into `DataTable` + history/community/groups lists. First attempt still overflowed → root-caused two bugs (see decisions).
- **Page rhythm** `py-8 space-y-6` → `py-6 space-y-4` (`PageShell` + doc `12-ui-layout-standard.md`); **uniform 48px table rows** (`33e9a64`).
- **Toolbar buttons = search height h-8** (`f7bc4eb`, `ExpandingIconButton` default); **Settings Save disabled until dirty** (`60eb5fb`); **enrich modal "Mở trang chi tiết" link + flashcard shortcuts info tooltip** (`51c260d`).
- **Group detail** (`c715bbb`, `81dee05`, + latest): paginate member list, action-button toolbar, member SearchInput, equal `h-16` rows, native-scroll add-modal, lightbulb "idea" button (hover=description, click=copy `luyện nhóm <tên>`).
- **Breadcrumb** (`05b26e1`): detail crumb now the entity NAME (was "Chi tiết"), "Nhóm học"→"Nhóm"; **rule created + locked** (CLAUDE.md #8 + `ui-patterns.json` `breadcrumb-detail-is-data-name`, forbid gate).
- **groups-view** empty-description → `__` placeholder + `min-h` (equal-height cards) — edited but LEFT UNCOMMITTED (user's WIP file).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-24 (viewport-fit: measure don't guess; uniform row height = invariant #9; Radix ScrollArea dialog scroll; breadcrumb = data name). Cross-project: `06-knowledge-ledger.md §A`.

**Open threads** — groups-view task C left uncommitted (user commits it with his WIP); deferred: move the filter button left of "Thêm nhóm" in the groups tab (WIP file). Couldn't visually verify (Claude-in-Chrome extension not connected) — verified by rebuild + route-sweep only.

## sakubun — local deploy + Cloudflare tunnel + cross-machine sync identity fix — 2026-07-24

```yaml
id: 2026-07-24-03
type: episodic
created: 2026-07-24T18:45:00+07:00
last_accessed: 2026-07-24
importance: 7
milestone_id: null
related_ids: [{ id: 2026-07-24-01, why: 'same-day sakubun; this session deployed it + fixed cross-machine sync' }]
embedding: null
```

**What happened** — deployed sakubun locally + exposed it publicly, then diagnosed & fixed why cross-machine data sync crashed (attended; NUC still down):

- **Local deploy + public tunnel.** Pulled sakubun (was 39 behind), `docker compose up -d --build` (migrations up-to-date, HTTP 200). Ran `cloudflared` (the user's token) via a **gitignored `docker-compose.tunnel.yml` + `.env.tunnel` (chmod 600)**, project `sakubun-tunnel`, `--add-host host.docker.internal:host-gateway` because the dashboard ingress routes `sakubun.thientnse.site → http://host.docker.internal:3789` (NOT `sakubun:3789` as the deploy README assumes). Verified end-to-end: `sakubun.thientnse.site` → HTTP 200 through Cloudflare. So a LOCAL deploy is publicly reachable without the NUC.
- **Sync crash diagnosis.** The user's "Đồng bộ hệ thống" (`/api/import/system`) failed with `SQLITE_CONSTRAINT_UNIQUE: user.email`. Root cause: better-auth mints a random user id per signup, so the same email has a different id on each machine; the merge keyed users by `id` → tried to INSERT → tripped `@@unique(email)`. Discovered the real data (510 items/189 logs/93 sentences) belongs to `tranngocthien628@gmail.com` (admin on machine A), while machine B's admin was a *different* email (`tranngocthien861`).
- **Phase 1 quick fix (done, verified).** Copied 628's domain rows from the machine-A `.db` snapshot into this machine's live DB with a one-off `better-sqlite3` script, remapping `userId` source→local (raw same-schema copy = dates/FSRS verbatim; FK-checked; guarded; pre-backup at `~/Downloads/sakubun-live-before-restore.db`). Promoted local 628 → sole admin (861 demoted).
- **Phase 2 durable fix (done, committed `2cd548e`).** `importSystemBackup` now reconciles users by email + translates every user FK (userId AND authorId, via `foreign_key_list`) through a `uidmap`, merging with `INSERT OR IGNORE`. `ADMIN_EMAIL` → admin on create on any machine. 228 tests pass, lint+build green, integration-verified on a copy of the live DB against the crashing snapshot (no crash, data preserved).
- **Gotcha caught by running it:** targetless `ON CONFLICT DO NOTHING` is a syntax error on the driver-adapter's SQLite → used `INSERT OR IGNORE`. Unit tests + build did NOT catch this; the integration run did.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-24 "Cross-machine sync collides on user.email" (root cause + Phase 1 + Phase 2 + INSERT OR IGNORE gotcha + better-sqlite3 date trap). Plan `docs/plans/2026-07-24-cross-machine-identity-sync.md` (status: done). `00-map` Sync + Auth rows updated. Cross-project: `06-knowledge-ledger.md §A` (2 lines).

**Open threads** — Phase 2 is committed but **NOT yet live on the running container** (built from pre-Phase-2 code); recommended deferring the rebuild until the user commits his in-flight UI edits (a rebuild bakes the working tree). `ADMIN_EMAIL` not set in `.env.docker` yet (628 already admin, so no effect until a fresh machine). 64 orphan `userId=NULL` items (pre-existing, invisible) pending optional cleanup. User's concurrent UI edits (`loading.tsx`, `login-form`, `register-form`, `data-table.tsx`, `landing-skeleton.tsx`, a new plan file) left untouched/uncommitted.

## sakubun — fix chromeless-route crash + account/admin UI simplification — 2026-07-24

```yaml
id: 2026-07-24-05
type: episodic
created: 2026-07-24T17:10:00+07:00
last_accessed: 2026-07-24
importance: 6
milestone_id: null
related_ids: [{ id: 2026-07-24-04, why: 'both machines re-hit + rebuilt the stale-container useSidebar 500 this day' }]
embedding: null
```

**What happened** — attended, local-only (NUC down); two pieces:

- **Diagnosed a production crash** ("Có lỗi xảy ra" navigating home↔login↔register). Real error from `docker logs
  sakubun`: `useSidebar must be used within a SidebarProvider`. Root cause: the root `loading.tsx` (→ `PageSkeleton`) is
  the Suspense fallback for EVERY route incl. chromeless auth pages + guest `/`, where `AppShell` mounts no
  `SidebarProvider`, yet `PageSkeleton` rendered `MobileTopBar`→`SidebarTrigger`→`useSidebar`. Fix (already in the user's
  concurrent commit `88a90b1`) drops `MobileTopBar` from the skeleton; the **running container was a stale build**, so I
  rebuilt (`docker compose up -d --build`) and verified via a full-route GET+RSC sweep with clean logs.
- **Account/admin UI** (`6ab13a9`): `/account` split into "Thông tin" / "Token MCP" tabs (beside the breadcrumb), dropped
  the oversized card titles, display name edits inline via a pencil button, password form dropped the confirm field (kept
  current-password), and both `/account` + `/admin` now show "Đăng nhập gần nhất" (`session.createdAt`) instead of
  last-activity. 221 tests + lint + build green; committed and **pushed** (remote `09ea9e4..6ab13a9`, first push this thread).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-24: "A root `loading.tsx` renders in EVERY route's chrome context
— keep it provider-free"; "Changing an EXISTING password without the current one has no supported better-auth path".
Cross-project: `06-knowledge-ledger.md §A`.

**Open threads** — none open from this session. Visual check still done by rebuild + route-sweep (Claude-in-Chrome
extension not connected). Push went to GitHub only; NUC down so it does not auto-deploy — local container is the live copy.

## sakubun — expose to internet via cloudflared-Docker + tighten #12 truncate into a gate — 2026-07-24

```yaml
id: 2026-07-24-04
type: episodic
created: 2026-07-24T21:40:00+07:00
last_accessed: 2026-07-24
importance: 6
milestone_id: null
related_ids: [{ id: 2026-07-24-03, why: 'same-day sakubun thread; re-hit + rebuilt the useSidebar 500 recorded there' }]
embedding: null
```

**What happened** — attended, local-only (NUC down); four pieces:

- **Committed the pending UI-backbone WIP** (5 atomic commits `88a90b1..5be5e3a`): retire PageHeader, settings card-blurb
  → title-ⓘ + Save dirty-state fix, type-scale gate (`lib/type-scale.test.ts`, invariant #11), MCP `conversation` prompt
  in catalog, image-verify script. `tsc` + 221 tests green.
- **Put sakubun on the internet** at `https://sakubun.thientnse.site` via a **cloudflared Docker container**
  (`cloudflared-sakubun`, token tunnel, on `sakubun_default`). Dashboard ingress already routes to
  `host.docker.internal:3789`. Chose Docker over the Windows service (needs admin; a different tunnel already runs there).
- **Re-hit the `useSidebar` 500** from `2026-07-24-03`: the running container was a **stale image** again, serving the
  old crashing skeleton. Rebuilt (`docker compose up -d --build`) → origin + public + `/login` all 200, logs clean.
- **Whole-project UI-rule audit + tightened #12 (truncate) into a gate.** Confirmed 6 UI gates + spoken-line green
  (all rules apply project-wide because the gates walk every file). #12 was `manual`-only and had shipped BROKEN in 3
  spots — added `lib/truncate.test.ts`, fixed 5 sites (`min-w-0`), proved the gate has teeth. 2 commits `0fd5894` (fix)
  + `7d691b5` (gate). Working tree clean.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-24: "Invariant #12 (truncate) is now GATE-enforced; two
regex-lint-gate pitfalls" · "Serving sakubun to the internet via a cloudflared Docker container (NUC down)". Cross-project:
`06-knowledge-ledger.md §A` (regex-gate hygiene).

**Open threads** — 7 sakubun commits UNPUSHED (5 UI-backbone + 2 truncate); running container has the 500-fix but NOT the
2 truncate fixes (rebuilt before those commits). Operator to decide push + rebuild. The `cloudflared-sakubun` container has
since **Exited** (Docker Desktop restart; `unless-stopped` didn't revive it) — public site still 200 via another machine's
connector on the same token; recreate as a `docker-compose.tunnel.yml` for durability. `.env.docker` secrets (SMTP/Google/
better-auth) surfaced in-chat via an IDE selection — rotate if this transcript is shared.

## sakubun — unify grammar+vocab add/import into one 3-tab ResourceAddDialog — 2026-07-24

```yaml
id: 2026-07-24-05
type: episodic
created: 2026-07-24T22:10:00+07:00
last_accessed: 2026-07-24
importance: 5
milestone_id: null
related_ids: [{ id: 2026-07-24-04, why: 'same-day sakubun thread; built on the truncate/type-scale gates tightened there' }]
embedding: null
```

**What happened** — attended, local-only (NUC down). Fixed the asymmetric add UIs on `/resources`:

- Grammar could only bulk-import (paste/Excel), vocab could only add one word. Unified both into ONE
  `components/resource-add-dialog.tsx` parameterized by `kind: 'grammar'|'vocab'` — a modal with three tabs shared by both:
  **Theo trường** (one entry, field by field), **Dán danh sách** (paste → preview → bulk), **Nhập Excel** (.xlsx → preview
  → bulk). Deleted the 3 old files (`import-dialog`, `import-form`, `vocab-add-dialog`).
- Server: `importItemsAction` + new `addOneItemAction` gained an optional `kind` param (validated server-side, mapped to
  `VOCAB_KIND` / grammar default); `addVocabAction` removed. Parse layer (`import-parse`/`excel-import`) reused unchanged —
  it was always content-agnostic.
- Two gate exceptions needed (added WITH reason, not weakened): the bulk preview renders `{expression}` bare →
  `spoken-line.test.ts` ALLOW; the modal's `<TabsList>` is not page-level → `layout-standard.test.ts` ALLOW.
- Updated `/guide` (grammar step + "Quản lý kho" feature card now narrate all 3 methods for both stores), `_INDEX.md`,
  `00-map.md`, `decisions.md`. 228 tests + lint + build green; rebuilt container (image `6d5c642`, healthy) → origin +
  `/resources` + public all 200, logs clean.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-24: "One `ResourceAddDialog` for both stores; 3 add paths
symmetrical (grammar + vocab)".

**Open threads** — NOT committed (held for the operator's preview-before-commit approval on the interactive modal). The
working tree ALSO carries the operator's OWN concurrent IDE edits, unrelated to this feature (`items-table`, `vocab-table`,
`row-actions`, `share-toggle`, `enrichment-card`, `item-analytics`, `item-groups`, `app/resources/[id]/page.tsx`, new
`components/row-share-button.tsx`) — when committing, stage ONLY the ResourceAddDialog fileset, never those.

## sakubun — resources/detail UI polish + discovered a PARALLEL Claude session sharing the working tree — 2026-07-24

```yaml
id: 2026-07-24-06
type: episodic
created: 2026-07-24T22:30:00+07:00
last_accessed: 2026-07-24
importance: 6
milestone_id: null
related_ids: [{ id: 2026-07-24-05, why: 'that ResourceAddDialog entry was written by the PARALLEL session sharing this exact working tree' }]
embedding: null
```

**What happened** — attended, local-only (NUC down). An 8-part resources/grammar-detail UI refinement:

- **`/resources` tables (both tabs):** per-row **share promoted OUT of the ⋮ menu** into a standalone `row-share-button.tsx`
  (icon + short verb + full tooltip; forked = disabled); **vocab "Cách đọc" column removed** (furigana covers it).
- **Grammar detail (`item-analytics`):** share (new `share-toggle variant="icon"`) + star moved onto the breadcrumb row via
  `PageShell.headerAside` (far right, the history-card [share][star] pair); **definition inline with the pattern**
  (ngữ pháp → định nghĩa), badges to their own row; **"Thêm vào nhóm"** → `ExpandingIconButton` (+ icon, label on hover);
  **"Thuộc nhóm" title dropped**, tag hover shows "Thuộc nhóm …"; **enrichment accordion now sticky** (`persist` →
  shared `useSessionState`).
- **Gates:** 228 tests + lint + `tsc --noEmit` green. Rebuilt the container (image `6d5c642`, healthy) → `/`, `/resources`,
  `/api/health` all 200, logs clean. (Host `next build` lock was held by the parallel session → used `tsc` for types.)

**The notable meta-fact.** A SECOND Claude session was running on this SAME repo/working tree, building the `ResourceAddDialog`
unification ([[2026-07-24-05]]). Each session perceived the OTHER's uncommitted files as "the operator's concurrent IDE
edits." Confirmed by: the 22:10 `2026-07-24-05` wrap is in Claude session-wrap voice, describes my fileset as the human's WIP,
and cites the same rebuilt image hash `6d5c642` my rebuild produced. A gate also TRANSIENTLY failed once
(`resource-add-dialog.tsx` flagged by `layout-standard.test.ts`) because the other session was mid-saving that test's ALLOW
entry — a re-run was green. So: on this machine, "concurrent edits" can be another agent, not the human; verify authorship +
re-run before attributing a gate failure.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-24 "Row sharing left the ⋮ menu; detail share+star on the breadcrumb
row; sticky enrichment accordion" (share relocation + `variant="icon"` + the sticky-accordion hook-before-early-return gotcha).
`_INDEX.md` gained `row-share-button`. Memory: extended [[user-edits-files-concurrently]] with the parallel-agent facet.

**Open threads** — my fileset (the 8 files + `row-share-button.tsx`) is NOT committed; the running container already bakes it
(rebuilt at the operator's request). Two filesets now coexist uncommitted in one tree — mine and the parallel session's
ResourceAddDialog set; each must be staged separately. Log id `2026-07-24-05` is used TWICE (my earlier chromeless/account
entry + the parallel ResourceAddDialog entry) — an id collision from the two sessions; left as-is, this entry is `-06`.

## sakubun — view-state persistence + empty-state unification + streak trim — 2026-07-24

```yaml
id: 2026-07-24-07
type: episodic
created: 2026-07-24T20:19:00+07:00
last_accessed: 2026-07-24
importance: 6
milestone_id: null
related_ids:
  [
    { id: 2026-07-24-01, why: 'same-day sakubun UI-polish; this continues it (persist state, empties, streak)' },
    { id: 2026-07-24-03, why: 'the loading.tsx/data-table/auth-card edits that session saw as WIP were this session, now committed' },
  ]
embedding: null
```

**What happened** — a multi-request UI pass on sakubun (attended, local-only; NUC down), committed in feature-split clusters, alongside the user's parallel editing of ~7 files (settings tabs, slider, group-flashcards, lib/library.ts — left untouched):

- **Bulk-action bar floats** (`8186ab4`): an in-flow bar above `DataTable` shifted the tbody top → `useFitRows` recomputed fewer rows on select (grew back on clear). Made it a `fixed` bottom-centre overlay (Motion slide-up) → out of flow → row count stays put. Diverges the vendored `data-table.tsx`.
- **Persist all page view-state** (`7ce9140`, `09ea9e4`): filled the gaps left by the existing `useSessionState`/DataTable persistence — controlled Tabs on library/settings/item-detail/group-detail + the guide tab (Server Component → tiny client wrapper `guide-tabs.tsx`; same for the guide accordion) + group member-search keyed per-id. `ScrollRestorer` in `PageShell` restores window scroll per route (rAF-retry until content tall enough). Plan `2026-07-24-persist-all-page-state.md` → done.
- **Guest "/" loader** (`b03003a`): `app/loading.tsx` was async-branched on auth → guests get `LandingSkeleton` (no sidebar), not the dashboard skeleton. **Auth cards** (`76d0ae8`): dropped the redundant 作文 brand mark (nav already shows it).
- **Empty states unified** (`bd8e197`): one `EmptyState` everywhere on a two-category rule — no-data ⇒ page-level EmptyState, toolbar hidden (extended to library + groups, which used to show a live toolbar over an empty table); filtered ⇒ toolbar stays + `SearchX` EmptyState (DataTable empty row now renders EmptyState via a new `emptyIcon` prop). Icons: Inbox=no-data, SearchX=filtered.
- **Streak celebration trimmed** (`dc2b529`): removed the milestone banner + caption — flame + day count only; deleted dead `milestoneFor`/`STREAK_MILESTONES` + `.streak-banner`/`.streak-sub` CSS.
- **New rule**: memory `rebuild-container-to-review` (miniserver-platform `617ba05`) — after app-visible edits, rebuild+restart the local container so the user reviews the running result. Also caught the Docker-Desktop-vs-native-engine context trap (stack lives on `default`; Desktop's daemon showed empty).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-24: "Persist view-state by default; float the bulk bar; auth-branch the / loader" (committed) + "One EmptyState for every 'nothing here'; strip the streak celebration" (this wrap). Cross-project: `06-knowledge-ledger.md §A` (2 lines: empty-state two-category, floating-overlay-avoids-reflow).

**Open threads** — empty-state + streak code committed (`bd8e197`, `dc2b529`); container already rebuilt + live at :3789. 13 files the user is editing in parallel left untouched — must stage only my files when committing. Scroll-restore FEEL unverified (no authed browser session) — build + route-sweep only.

## sakubun/platform — pull+resolve both repos, retire a UI lock, migrate the stack to Docker Desktop — 2026-07-24

```yaml
id: 2026-07-24-08
type: episodic
created: 2026-07-24T22:55:00+07:00
last_accessed: 2026-07-24
importance: 6
milestone_id: null
related_ids:
  [
    { id: 2026-07-24-07, why: 'same session-wrap thread; this continues it (commit + pull + ops)' },
    { id: 2026-07-24-06, why: 'retired the card-explanation lock the parallel session installed' },
  ]
embedding: null
```

**What happened** — attended, local-only (NUC down). An ops + reconcile pass, no feature code:

- **Diagnosed "container missing" in Docker Desktop.** The sakubun stack ran on the NATIVE engine (context
  `default`); Docker Desktop shows only its OWN VM daemon (`desktop-linux`), and launching Desktop hijacks the
  CLI context — so the GUI looked empty while `docker ps` (default) still showed the stack. See `02-known-traps.md` #16.
- **Committed the pending session-wrap batch**: sakubun `dc2b529` (streak) + `bd8e197` (empty-state) + docs;
  miniserver `876447a` (digest) + `83277ec` (normalized the orphan `git-fetch-before-work` memory).
- **Pulled + resolved BOTH repos** (diverged: local feature commits vs remote sessions). Hand-resolved conflicts:
  ledger + day-log (kept every entry, RENUMBERED my colliding episodic id `-04`→`-07`), `resources-view.tsx`
  (combined my EmptyState format + remote's new `ResourceAddDialog`), `00-map`/`decisions` (kept my newer version,
  patched the Import row, dropped a duplicate entry). Stash-popped the WIP with no conflict; `tsc` + 227 tests green.
- **Retired the `card-explanation-in-title-info` UI lock** (`a3394cf`) after reading the user's WIP redesign of
  `settings-data-tab.tsx`: the real fix is equal-height cards (grid-stretch + `CardFooter mt-auto`), not banning
  CardDescription; the lock's own canonical example had outgrown it.
- **Migrated the whole stack (app + cloudflared + voicevox + the `sakubun_data` volume) from the native engine to
  Docker Desktop** at the user's request — data-safe (`sakubun.db` sha256 matched byte-for-byte across daemons;
  tar backup at `~/sakubun-volume-backup-2026-07-24.tar.gz`). Rebuilt on `desktop-linux`, verified healthy + local
  200 + public 200, then removed the old native volume.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-24 "Retired the card-explanation-in-title-info lock".
Cross-project: `06-knowledge-ledger.md §A` (premature-1x-lock; parallel-session episodic-id collision). Infra:
`02-known-traps.md` #16 (Docker Desktop daemon split). Machine-specific: local memory `docker-desktop-context-trap`.

**Open threads** — the stack is now tied to Docker Desktop RUNNING (recommended enabling "start on login"); it will
NOT auto-start on boot like the old systemd engine. Both repos synced with origin (user / a parallel session pushed).
sakubun carries 14 WIP files (the user's active edits) — untouched.
