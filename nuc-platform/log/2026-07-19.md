<!-- Daily recall log. One file/day; multiple batches = multiple blocks. Schema: see README.md. -->

## sakubun — audio/furigana polish, streak fire, Thống kê page (Cụm A/B/C) — 2026-07-19

```yaml
id: 2026-07-19-01
type: episodic
created: 2026-07-19T02:50:00+07:00
last_accessed: 2026-07-19
importance: 6
milestone_id: null
related_ids: [{ id: 2026-07-18-01, why: 'continues the same sakubun UI polish push (DataTable, audio, history)' }]
embedding: null
```

**What happened** — one long session across three request batches ("cụm"), each committed atomically + rebuilt/redeployed locally (`docker compose build --no-cache sakubun` + `--force-recreate`), container healthy each time:

- **Cụm A (audio/furigana/search/MCP fixes):** adjustable TTS speed (default `speechRate` setting + a per-play control fused to the play button as a **button-group** whose speed side opens a dropdown; reused on EVERY `SpeakButton` — tables, settings, history). Fixed a real **half-speed bug** (absent `?rate` → `Number(null)=0` → clamped to `speedScale 0.5`). Furigana: replaced the `furiganaHint` boolean with a 4-mode `furiganaMode` (app/claude/both/none, default `app`), made readings **hover-only** in the karaoke line, and fixed the `[]` empty-bracket leak in `parseFurigana`. Karaoke sweep now renders furigana ruby. Added kana→romaji (`lib/romaji.ts`) folded into table search. Split MCP `list_items`/`update_item` → **`list_grammar`/`list_vocab` + `update_grammar`/`update_vocab`** (root cause of "Claude says no vocab": `list_items` defaulted to GRAMMAR). Topic grid 4→5 cols + truncate.
- **Cụm C (streak):** gradient SVG flame that heats up with the streak (`lib/streak-tiers.ts`); a once-per-increase full-screen fire celebration (localStorage baseline). Previewed as an Artifact + user-approved before wiring in.
- **Cụm B (analytics):** new **`/thong-ke`** page with **recharts** (added dep, 3.9) — accuracy + answer-time time-series (line + count bars + dashed window-average), daily progress, score/time by difficulty & length; client windows (30/90/365) + groups (day/week/month/weekday) a single server-side 365-day series. Moved the perf charts OFF the dashboard. Verified the existing analytics/item-stats metrics are correct.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-19 entries (TTS rate-param trap · furiganaMode+hover+`[]` · MCP grammar/vocab split · React-Compiler render-purity gotchas · recharts/`/thong-ke` analytics · streak flame). Docker `--build` cache trap → `02-known-traps`. Cross-project lines → `06-knowledge-ledger.md` A.

**Open threads** — **Cụm D not started:** rewrite `/guide` fully + a shadcn **animation walkthrough** (user chose the interactive option) for Bắt đầu / Cách hoạt động / Setup Anki / Setup MCP. Two narrowed-scope items deferred: karaoke/furigana on the resources-table expression cell (dialog-trigger coupling), and letting Claude update grammar `enrichment` (only set at import today). recharts pulled 7 moderate transitive vulns (accepted for a local app; no `audit fix --force`). Interactive bits (celebration, charts, speed dropdown, hover-furigana) verified only via curl markup + bundle grep — need a browser eyeball. MCP tool rename requires the user to reconnect the server in Claude Desktop.

## sakubun — code-optimization & maintainability pass (P0 + P3 partial) — 2026-07-19

```yaml
id: 2026-07-19-02
type: episodic
created: 2026-07-19T15:00:00+07:00
last_accessed: 2026-07-19
importance: 6
milestone_id: null
related_ids: [{ id: 2026-07-19-01, why: 'same day; this is the maintainability/optimization pass over the UI-polish work' }]
embedding: null
```

**What happened** — planned + executed a broad optimization pass (plan `docs/plans/2026-07-19-code-optimization-maintainability.md`, active). Pushed back on the user's "INDEX.md in every folder" idea → agreed a **hybrid** (thin primer + co-located `_INDEX.md` in a few folders). 5 commits on `main` (not pushed):

- **P0 bug-scan (reviewer subagent) → 7 fixes in `lib/practice.ts` + a migration**: B1 post-grade `dueNow` was the only due-count NOT grammar-scoped; B2 `submitReview` guarded on `userAnswer` truthiness so an `autoSkipStalePending` race lost a late review with a wrong "đã chấm rồi" error → now guards on `status==='GRADED'`; B3 `addItems` was per-row findUnique+create with no txn → batched + `$transaction`; B4 added `ratings ⊆ stagedItems` integrity check; B5 `today.sentences` counted PENDING/SKIPPED/QUIZ → GRADED-only; B6 `recentContextsFor` per-item fetch; B7 `@@index([SrsState.due])`. Gates green (test 104/104 · lint · build).
- **Docs legibility:** `00-map.md` **78KB→15.5KB (−80%)** (kept current-state facts + all 7 invariants + flow, dropped the dated changelog narrative that lives in `decisions.md`); added `_INDEX.md` to `lib/`, `lib/mcp/`, `components/`; anti-drift line in `CLAUDE.md`.
- **P3 MCP audit:** terminology is SOUND (Claude routes correctly). Token findings: both `practice`+`quiz` prompts return the FULL `SESSION_PROTOCOL` (~6.7k tok); 3-layer redundancy on the anti-fabrication/anti-contamination guidance. Did **Bậc 1** only (trimmed the redundant `stage_sentence`/`submit_review` tool descriptions, kept guidance in the nudge + protocol).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-19 entries (count-scoping + submit guard + atomic import · doc-legibility hybrid index · MCP token/terminology). Cross-project lines → `06-knowledge-ledger.md` A.

**Open threads** — P1 (split `server.ts`/`practice.ts` + 5 large components) NOT started; P2 (UI reuse) NOT started; **P3 Bậc 2** (split prompts per mode) + **Bậc 3** (VN→EN instructions, keep VN output) DEFERRED — behavior-sensitive, need the model-in-the-loop `eval/display-contract-eval.md` + user gate. The 5 commits are local, not pushed. The P3 Bậc 1 trim is compile-verified only — behavior unconfirmed until the user runs an eval session.

## sakubun — P1 refactor pass (file-size reduction) — 2026-07-19

```yaml
id: 2026-07-19-03
type: episodic
created: 2026-07-19T15:45:00+07:00
last_accessed: 2026-07-19
importance: 6
milestone_id: null
related_ids: [{ id: 2026-07-19-02, why: 'continues the same optimization plan (P0/P3 → now P1)' }]
embedding: null
```

**What happened** — executed **P1** of plan `docs/plans/2026-07-19-code-optimization-maintainability.md`. 5 behavior-preserving refactor commits on `main` (not pushed), each gated by lint+build+test 104/104:

- **P1.1** `lib/mcp/server.ts` 917→796 — the tuned tutor protocol (`MODE_DISPATCH`/`PRACTICE`/`QUIZ`/`SESSION_PROTOCOL`) moved to new `lib/mcp/prompts.ts` by a **script that slices+re-exports** (no retype), then a **SHA256 proved the assembled string identical** (len 22665, `03e148be…`). server.ts is now pure wiring.
- **P1.2 SKIPPED** — I pushed back on splitting `practice.ts` (1323, sole DB layer): shared internal helpers ⇒ import-cycle risk in the critical layer > benefit; user agreed. Kept the facade whole.
- **P1.3** 4 leaf views split (orchestrator + presentational parts): `history-list` 816→142 (+`history-cards`/`history-filters`), `guide-walkthrough` 777→116 (+`guide-primitives`/`guide-steps`), `settings-form` 594→70 (+3 per-tab files), `item-analytics` 445→273 (+`item-forgetting-curve`/`item-review-log`). All `_INDEX.md` updated per drift rule.
- **`data-table` (reuse anchor) deferred to P2** (split with the reuse work); **P1.4 (lib re-org) skip-recommended** (churn > value, like the practice.ts call).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-19 top entry (byte-identical move via SHA256 · split leaves not the cohesive core · component-split shape · Radix Tabs context through child files). `00-map.md` MCP row now points to `prompts.ts` too. Cross-project line → `06-knowledge-ledger.md` A.

**Open threads** — P2 (UI reuse audit + `data-table` split + UX sync — needs visual previews, user in loop) is the next pillar (fresh session). P3 Bậc 2/3 still deferred (eval-gated). 10 commits total local, none pushed. All refactors compile-verified only — user to eyeball `/history` `/guide` `/settings` `/resources/[id]` renders unchanged.

## sakubun — P2: UI reuse, the missing error tier, MCP tool split (plan CLOSED) — 2026-07-19

```yaml
id: 2026-07-19-04
type: episodic
created: 2026-07-19T16:40:00+07:00
last_accessed: 2026-07-19
importance: 7
milestone_id: null
related_ids: [{ id: 2026-07-19-03, why: 'same plan; P1 refactor → now P2, and closes the plan' }]
embedding: null
```

**What happened** — ran P2 of `docs/plans/2026-07-19-code-optimization-maintainability.md` and **closed the plan (`status: done`)**. 5 more commits on `main` (not pushed), each gated by lint+build+test 104/104:

- **P2.1 audit (grep-first, after reading `08-SHARED-ASSETS` per `/code-reuse`)**: empty-state box hand-rolled **9×/8 files**, `if (r.ok) toast.success else toast.error` **8×/6 files**. Below rule-of-three (2× icon+h2 header) → logged, not extracted. `loading.tsx` skeletons are deliberately page-shaped → PATTERN.
- **P2.2 extractions**: `components/empty-state.tsx` (7 call sites) + `lib/action-toast.ts` `toastResult()` (**type predicate** so the ok-branch narrows; guarantees no action failure is silently swallowed). Catalog updated; EmptyState flagged **proposed for ui-kit, needs user approval** (agent must not install).
- **P2.3**: colors + icons audited **clean** (0 literal Tailwind colors; the only hex is deliberate flame art; the 4 `⚠️` hits are code comments). But the app had **NO error tier at all** → added `app/{error,global-error,not-found}.tsx` + `loading.tsx` for `/items`+`/vocab`. **Digest only, never the error message.** Previewed as an Artifact → user approved before commit.
- **P2.6/P2.7 file-size debt**: `app/guide/page.tsx` 622→284 (features tab → `guide-features.tsx`, shared `Topic/Code/RefHeading` → `guide-reference.tsx`); **`lib/mcp/server.ts` 796→128** — 20 tools → `lib/mcp/tools/{core,content,practice,manage,quiz}.ts` + `tool-kit.ts`.
- **P2.5 SKIPPED with evidence**: sakubun's `data-table.tsx` differs from the ui-kit canonical by **exactly 3 comment lines** → vendored code, not ours to split.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-19 top entry (scope the LOC target to app logic · extract on grep evidence · the error-tier traps · verify compiler-invisible failure modes). Goal criterion in the plan revised honestly. Cross-project line → `06-knowledge-ledger.md` A.

**Open threads** — **P3 Bậc 2/3 remain deferred** (split prompts per mode; VN→EN instructions) — behavior-sensitive, need `eval/display-contract-eval.md` + a user gate. **EmptyState → ui-kit is an open proposal awaiting user approval.** 15 commits local, **none pushed**. Everything is compile/verified-by-script only: the user still needs to eyeball `/history` `/guide` `/settings` `/resources/[id]` and force a real error to confirm `reset()` recovers.
