<!-- One file/day; multiple entries = multiple blocks. Schema: see README.md. -->

## sakubun — UX batch: mode dispatch, propose-supporting-grammar, sticky state, live refresh, audio, mobile-research — 2026-07-14

```yaml
id: 2026-07-14-01
type: episodic
created: 2026-07-14T01:30:00+07:00
last_accessed: 2026-07-14
importance: 7
milestone_id: null
related_ids: [{ id: 2026-07-13-02, why: 'continues the sakubun quiz/UX work from the prior session' }]
embedding: null
```

**What happened** — 8 friction items from a live-use session:

- **MCP mode dispatch (bug fix):** `mode=quiz_grammar` + "bắt đầu" ran the TRANSLATION loop and Claude
  flipped the user's mode. Unified both `practice`+`quiz` prompts under `SESSION_PROTOCOL` = `MODE_DISPATCH`
  (reads `settings.mode`, routes, FORBIDS auto-switching) + the two loops. Quiz hardened to EXACTLY ONE
  sentence (was 3), distractors permute the correct fillers.
- **Supporting grammar → propose-then-approve, default ON.** Removed the silent `observedGrammar`
  auto-add in `submit_review`; step 6b now proposes → `add_grammar` on approval. Was OFF+silent = why the
  user never saw a proposal.
- **Sticky UI state** via `useSessionState` (sessionStorage, `useSyncExternalStore`) — NOT URL (bare
  sidebar links + `force-dynamic` refetch). **Live refresh** `live-refresh.tsx` (focus + ~10s poll,
  `router.refresh()`) on `/`,`/history`,`/resources`. **`update_item`** MCP tool (content only, never SRS).
  Page-size selector hidden when ≤25. Grammar level badge → `secondary`. Furigana moved beside auto-learn.
- **Audio (laptop OK, Ubuntu silent):** environmental — Linux browsers ship no Japanese voice. `speak-button`
  now pins a `ja` voice and toasts an install-hint when missing. VOICEVOX deferred.
- **Mobile (option) — researched only:** Claude iOS/Android support MCP as a REMOTE custom connector added
  on claude.ai (cloud → public HTTPS + OAuth). = the deferred NUC-onboarding project + centralized data.

**Decisions made** — `docs/decisions.md` 2026-07-14 "UX batch: mode dispatch, propose-supporting-grammar,
sticky state, live refresh, audio". `00-map.md` updated (19 tools, `SESSION_PROTOCOL`, audio, sticky state,
live refresh, autoLearnSupport). Plan `docs/plans/2026-07-13-ux-live-edit-audio.md` → `done`.

**Open threads** — propose-grammar flow + `update_item` verified only at protocol/data layer → user to
test in Claude Desktop. Audio hint to confirm on the Ubuntu box. Live refresh: watch it doesn't fight table
state (it shouldn't — `router.refresh()` keeps client state). Mobile = separate P3 go/no-go. URL-sync for
shareable filtered links offered but not built. Committed + pushed.

## sakubun — URL-sync filters + added-date + guide-wrap + dialog headings; USER quiz rework; study-plan REMOVED — 2026-07-14

```yaml
id: 2026-07-14-02
type: episodic
created: 2026-07-14T15:30:00+07:00
last_accessed: 2026-07-14
importance: 7
milestone_id: null
related_ids: [{ id: 2026-07-14-01, why: 'same session, later batch + a feature removal' }]
embedding: null
```

**What happened**

- **UX polish (4):** table filters (search/level/state) + resources tab now ALSO sync to the URL
  (`?q/?lvl/?st/?vq/?vst/?tab`) on top of sessionStorage → shareable + restored from a link
  (`useSessionState` gained an optional `urlKey`, written via `history.replaceState` = no server refetch;
  seeds from the URL on a fresh load). Added a **"Ngày thêm"** (`createdAt`) sortable column to the grammar
  + vocab tables (`listItems` now returns `createdAt`). Guide MCP tool **table wraps** (`table-fixed` +
  `whitespace-normal`) instead of overflowing. Detail-dialog section headings styled as **secondary-badge
  pills** (app bg) so they don't fade.
- **USER reworked quiz + settings IN PARALLEL (their editor), authored by them:** quiz MC A–D → **fill-in-the-blank**
  (1 pattern, 1 blank, no options), **removed the `mode` setting**, added **`skip_sentence`** tool + **reminder
  settings** (`reminderTime`/`notificationEmail`) + `Sentence.answeredAt` migration. Their concurrent edit
  to `settings-schema.ts` (removing `MODES` while adding reminder fields) broke the build mid-session — I
  STOPPED and surfaced the conflict instead of auto-merging; they finished, build went green.
- **Study-plan / roadmap feature REMOVED** (user: "no longer fits"). Full teardown — see decisions.md.

**Decisions made** — `docs/decisions.md` 2026-07-14 "Removed the study-plan / roadmap feature". Study-plan
bits stripped from `00-map.md` (18 tools + 5 prompts); plan `2026-07-12-study-plan-roadmap` → `abandoned`.
Migration `20260717150954_drop_study_plan`. (UX-polish tweaks are self-evident — no decisions entry.)

**Open threads** — ⚠️ `00-map`/`decisions`/guide QUIZ docs still describe the OLD MC + `mode` design, NOT the
user's fill-in-blank rework — a full docs reconciliation is DEFERRED until the user says quiz+reminder are
settled (their reminder UI: settings-form + date/time-picker components + package deps still UNCOMMITTED in
the working tree — left for them). Study-plan removal committed (252d918); UX polish rode commit 20074da.
