<!-- Recall tier (raw, never auto-loaded). Schema: log/README.md. Durable lessons are distilled UP into
     sakubun/docs/decisions.md + nuc-platform/06-knowledge-ledger.md — don't duplicate, link. -->

## sakubun — executing the three accepted plans: Phase 0 repair → settings re-scope → production ladder → calibration shadow → agentic check_library — 2026-07-27

```yaml
id: 2026-07-27-01
type: episodic
created: 2026-07-27T01:20:00+07:00
last_accessed: 2026-07-27
importance: 9 # first execution pass on the 3 plans; 5 commits, a live migration, the flagship shipped
milestone_id: null
related_ids:
  - {
      id: 2026-07-26-02,
      why: "the planning session this executes — the 3 plans + the MCP lab it built",
    }
embedding: null
```

**What happened**

- Executed in the recommended order, batching + reporting between waves. **5 commits, all deployed to the local
  container; nothing pushed yet.** Gates at the end: 373 tests, lint clean, build clean.
  - `8e5887b` — Phase 0 of both plans: `maxNewPerSentence` (default 1) + the 6 lab-found defects (focus bypasses
    the daily new cap · the sheet never claims FOCUS entries it did not list · `limits.*` stated with their fix ·
    `stage_sentence` refuses an unratable item-less row · `update_settings` takes flat args · the lab is now
    `npm run mcp:lab:up|down` on port **3798**).
  - `c0d4426` — `levels` split into `grammarLevels` + `vocabCeiling` (null = follow the grammar levels);
    `difficulty` anchored to countable structure (`DIFFICULTY_ANCHORS`, same text to Claude and to the slider);
    `toCompositionSettings` trims the per-turn payload 22 → 16 fields.
  - `bb2341f` — the production ladder: `CLOZE_PRODUCTION` + `GUIDED_PRODUCTION` as one compact
    `PRODUCTION_LADDER` section inside `PRACTICE_PROTOCOL`; an assisted success caps at rating 3.
  - `4adcde6` — `lib/calibration.ts` (Elo ability vector + per-item `challenge`) in **shadow mode**: it proposes,
    nothing applies. New `LearnerAbility` model + `Item.challenge`.
  - `02b3e7e` — the flagship: `check_library` + `fork_shared_items` + `lib/coach.ts` (16-code proposal engine) +
    `lib/enrichment-quality.ts` (6 objective card states).
- **A live migration on real user data**, done only after verifying it on a COPY: adding `Item.challenge` makes
  Prisma DROP + recreate the table (SQLite + FKs), so all 581 items were rewritten. Verified 0 drift across 12
  table counts on the copy, took a pre-migration backup, then applied it to the live volume — 0 drift again.
  The agent's first `docker compose up -d --build` was correctly BLOCKED by the permission classifier; the
  supervisor authorised it explicitly afterwards.
- **The flagship scenario was measured before/after on the same lab.** Asking about `てしかたがない` (a pattern
  another member published) used to return `[]` from every tool on the surface; it now returns the owner + one
  offer, forking works, and the SAME lookup afterwards switches to two different offers. Codes exercised live:
  FORK_SHARED, ADD_MISSING, PRACTICE_NEW, ENRICH_OUTDATED, ENRICH_THIN, WIDEN_LEVELS.
- **Bugs the harness caught, in both directions.** The lab found a real defect of the agent's own making (a
  quoted query `「〜ざるを得ない」` reported "not in your library" for an item the learner owned, which would have
  produced a DUPLICATE); the unit tests caught two more before the lab did (normalizer stripped tildes before
  quotes; item `kind` was inferred from "only VOCAB has hanviet", wrong for a kana word). The lab ALSO lied twice
  and both lies are now refusal gates (integer date cells; a stray server on the port).
- Token cost tracked as it grew: tool descriptions 15,678 → 15,935 → 16,518 → **18,085 chars** across the waves
  (25 → 27 tools), all of it always-in-context.

**Decisions made** (distilled upward — see the entries, not this list)

- `sakubun/docs/decisions.md` 2026-07-27: the settings re-scope + why the split was free · the assisted-rating cap
  and why an Elo support offset was needed the moment the ladder shipped · why calibration is a passenger on the
  FSRS transaction · the normalizer's search-vs-compare split · why the card is structurally unrenderable.
- `nuc-platform/06-knowledge-ledger.md`: 3 cross-project lines (a schema default is silent data loss during a
  rename; a scaffolded success must not be scored like an unaided one; a projection beats a deletion when a
  payload rides every turn).
- Execution notes (deviations, measurements, what could NOT be done) live inside each plan file's phase, not here.

**Open threads**

- **5 commits unpushed.** Multi-machine: push protects this more than any summary.
- **Two gates only the supervisor can pass**, and both are named in the plans:
  1. `agentic-tutor-loop` step **1.7** — does `check_library` fire UNPROMPTED in a blank Claude Desktop chat? The
     lab can prove the tool works, never that Claude reaches for it. If it does not fire, the enforcement
     assumption of the whole plan fails and it changes shape (fallback: a Claude Desktop project instruction).
  2. `adaptive-calibration` step **1.4** — review ~a week of real shadow proposals. If they read as wrong, the
     model is wrong and Phase 3 must not ship. The supervisor's own comfort is explicitly NOT the test (Bjork:
     correct calibration feels worse).
- **Blocked by a hard dependency:** coach codes 14/15 (`RATCHET_CONTRAST`, `CARD_SUSPECT`) are filtered out until
  `patch_enrichment` exists (plan step 2.1) — with only `enrich_items`, firing them would overwrite whole cards.
  Enforced by a test, not a note. This is the recommended next piece of work.
- `natural-generation` Phase 1 (baseline the generation quality) is untouched and is the other candidate — that
  plan forbids its own feature work until the baseline exists, and it is allowed to shrink if collisions are rare.
- 3 files stayed dirty from a parallel pass all session (`app/groups/[id]/loading.tsx`,
  `components/shared/fit-skeleton.tsx`, `components/shell/page-skeleton.tsx`) — never staged, never touched.
- `backup-prod-premigration-20260727.db` sits in the sakubun repo root (gitignored). Delete once the migration is
  trusted in daily use.

## sakubun — Phase 2 of the tutor loop: patch_enrichment, the ratchet, and three bugs only a running server showed — 2026-07-27

```yaml
id: 2026-07-27-02
type: episodic
created: 2026-07-27T06:30:00+07:00
last_accessed: 2026-07-27
importance: 8 # the dependency that unblocked the compounding half; 3 commits, all lab-verified
milestone_id: null
related_ids:
  - {
      id: 2026-07-27-01,
      why: "the first execution pass; this continues the same plan into Phase 2",
    }
```

**What happened**

- **3 commits, deployed to the local container, nothing pushed.** Gates each time: 396 tests, lint clean,
  build clean. No schema change, so no migration.
  - `8007d85` — `patch_enrichment`: `lib/enrichment-merge.ts` (pure merge — append+dedup+cap for the 5
    list fields, replace for the 3 scalars, every skip REPORTED, `v` stamped only when the card meets
    the bar) + `patchEnrichment` in `practice.ts` (compare-and-set write) + `lib/normalize.ts` (the
    shared key, extracted to break a cycle). 27 → 28 tools.
  - `082bf66` — a SUSPENDED pattern is reported as paused, not missing (both renderer exits) +
    `ASIDE_HANDLING` in all three protocols + the stale-pending regression test.
  - `74a8bcb` — a never-studied pattern is no longer offered as "due for review".
- **Plan steps closed: 2.1, 2.3, 1.3b, 2.4, 2.5** (and plan risk 4). Remaining in Phase 2: 2.2
  (`autoCapture` settings + UI), deliberately left because it is UI work the supervisor previews.
- **2.3 deviated and the plan was wrong.** Codes 14/15 cannot live in a pure engine — see the
  decisions entry. They ship as an imperative paragraph in the `check_library` description.
- **The MCP lab found three real bugs again**, none of which a passing unit suite would have shown:
  `ENRICH_FROM_SHARED` offered a patch for a card that does not exist; three proposals pre-filled
  arguments their tool does not accept (Zod strips them silently); and a never-studied pattern was
  offered as "đang đến hạn ôn". A fourth was found by a test failing against the branch I had not
  thought about (the empty-batch renderer exit).
- **2.5 needed no code**: the guard already existed (`status === 'GRADED'`), it just had no test.
- Token cost: tool descriptions 18,085 → **19,263 chars** (+6.5%), partly repaid by shortening
  `enrich_items` now that partial edits have their own tool.

**Decisions made** (distilled upward — see the entries, not this list)

- `sakubun/docs/decisions.md` 2026-07-27: only-the-model-knows (where a rule belongs) · a merge is only
  non-destructive if every drop is reported (+ CAS, + the donor-must-match guard) · one state, two
  messages (+ comment-only invariants).
- `nuc-platform/06-knowledge-ledger.md`: 3 cross-project rows matching those entries.

**Open threads**

- **9 commits unpushed** in sakubun, 2 in the root repo.
- The two supervisor-only gates from `2026-07-27-01` are still open (blank-chat `check_library`
  firing; a week of calibration shadow proposals).
- Next candidates, in the order recommended: Phase 3 steps **3.6 + 3.7** (widen the error annotation to a
  confusion PAIR, then mine it — one additive nullable column, no backfill) · then **2.2** autoCapture.
- `backup-prod-premigration-20260727.db` still in the sakubun repo root (gitignored) — delete once the
  migration is trusted.

## sakubun — Phase 3 proactive half + Phase 4 close-out: the loop is done and proven — 2026-07-27

```yaml
id: 2026-07-27-03
type: episodic
created: 2026-07-27T11:00:00+07:00
last_accessed: 2026-07-27
importance: 8 # reaches the plan's own stop-here checkpoint; 5 commits, all lab- or preview-verified
milestone_id: null
related_ids:
  - { id: 2026-07-27-02, why: "the Phase 2 pass this continues; same plan" }
```

**What happened**

- **5 commits**, each deployed to the local container and verified in the RUNNING image (not just a
  green build). Gates at the end: 430 tests, lint clean, build clean.
  - `28545be` — error-signature mining (`ReviewLog.errorDetail` + `lib/mining.ts` + `MINED_CONTRAST`).
  - `8767b4e` — `get_library_health` + the library-wide sweeps + the `quality` filter + RAISE_NEW_CAP
    moved to the lookup path.
  - `5a68d98` — the session opening carries ONE unnumbered clean-up offer (`get_stats` + MODE_DISPATCH).
  - `246b377` — web parity: card quality visible on the web (previewed and APPROVED before commit).
  - `8d7c28c` — `autoCapture`, card provenance, the behavioural eval.
- **Plan reached step 4.4**, its own declared stop-here point: the proposal engine is done. Phases 5-8
  (student model, ingest-from-anything, teach-back) are untouched and can be started or not.
- **The UI change was previewed, not just diffed** — a static artifact with sakubun's real tokens, and
  the one number that CHANGES for the supervisor (the dashboard gap count goes up on the same library)
  was called out explicitly rather than shipped as a silent redefinition.
- Deviations, both recorded in the plan: `autoCapture` does NOT make the lookup write (the flag marks
  the proposal; Claude calls the tool), and 3.4's scheduler signal was unbuildable so it moved to the
  lookup path.
- Token cost across the day: tool descriptions 18,085 → **20,736 chars**, 27 → 29 tools.

**Decisions made** (distilled upward)

- `sakubun/docs/decisions.md`: pre-filled call must be the NEXT call · a whole-kho aggregate is not the
  dashboard · pre-authorisation must not make a read tool write · an optional field must be optional in
  failure · a stale eval is worse than none.
- `nuc-platform/06-knowledge-ledger.md`: 3 more cross-project rows.

**Open threads**

- **14 commits unpushed** in sakubun, 4 in the root repo. This is the largest single-machine exposure of
  the week and includes a live schema migration.
- The two supervisor-only gates remain: blank-chat `check_library` firing (now Scenario A of
  `eval/agentic-lookup-eval.md`) and a week of calibration shadow proposals (calibration plan 1.4).
- `backup-prod-premigration-20260727.db` and `backup-prod-preerrordetail-20260727.db` sit gitignored in
  the sakubun repo root. Delete once both migrations are trusted in daily use.
- 3 files stayed dirty from a parallel pass all day and were never staged.

## sakubun — Phases 5, 6 and 8: the app now holds a model of the LEARNER — 2026-07-27

```yaml
id: 2026-07-27-04
type: episodic
created: 2026-07-27T16:00:00+07:00
last_accessed: 2026-07-27
importance: 9 # the knowledge-compounding half; 5 commits, a live migration, one step deliberately deleted
milestone_id: null
related_ids:
  - { id: 2026-07-27-03, why: "continues past the plan's own 4.4 stop-here checkpoint" }
```

**What happened**

- **5 commits**, each deployed to the local container and verified in the RUNNING image. Gates at the
  end: **492 tests**, lint clean, build clean, `tsc --noEmit` clean.
  - `cf80570` — `lib/dossier.ts`: the derived student model, pushed onto the composition sheet.
  - `40da6e6` — `LearnerNote` + `lib/learner-notes.ts`: the evidence-bound asserted annex (live migration).
  - `8b79e3d` — the dashboard section that shows the learner what the app concluded about them.
  - `a96de5f` — the session recap, DERIVED (the stored digest was deleted).
  - `94ac79e` — `preview_import`: paste real material, diffed against the library before anything is added.
- **Phase 5 and Phase 6 are complete; Phase 7 (teach-back) was deliberately NOT started** — it is the
  only remaining item whose cost is per-session and permanent (a fourth protocol body in context every
  session), so it stays a supervisor decision rather than a checklist item.
- **A live migration, third of the week:** `LearnerNote` is a pure `CREATE TABLE` — rehearsed on a copy
  of production (17 row counts identical, tables 19 → 20, indexes 53 → 55), backed up, applied on
  container boot, 0 drift re-verified against the same fingerprint.
- **Three defects found by RUNNING it, none of which a diff would show:** the digest told Claude to aim a
  sentence at a pattern that was not in the batch (silent data loss, the 2026-07-26 focus bug in new
  clothes); the fail-closed tenancy guard rejected an unscoped `LearnerNote.update`; and building the
  dashboard revealed the notes had shipped in English, i.e. unreadable by the person they describe.
- **One step shipped by DELETING its storage.** §9's session digest needed a session-end event; there
  isn't one. Deriving it satisfied all three of its self-destruct conditions by construction.
- **The invariant-9 gate refused a citation rendered as dead text** and was not widened — the quote
  became a real `SpokenLine`.
- Token cost across this pass: tool descriptions 20,736 → **22,699 chars**, 29 → **30 tools**. Exactly ONE
  new tool for two whole phases; the student model deliberately added none.

**Decisions made** (distilled upward)

- `sakubun/docs/decisions.md`, 6 entries: a model of the learner must not aim at a pattern outside the
  batch · the `verbatim` flag decides where the digest may appear · an aggregate about a PERSON needs a
  support floor and "improving" must not mean "practised less" · the asserted tier is a TABLE because
  the Settings blob has no CAS · building the surface changed the data (a claim the subject cannot read
  is not falsifiable) · a citation is not an exemption from the playable rule · the session digest
  shipped with its storage deleted · a bulk import path makes the normaliser load-bearing, and NFKC does
  less than assumed.
- `nuc-platform/06-knowledge-ledger.md`: new cross-project rows (see section A).

**Open threads**

- **Phase 7 (teach-back) is an open supervisor decision**, with the cost argument written into the plan.
- The two supervisor-only gates still stand: blank-chat `check_library` firing (Scenario A) and a week of
  calibration shadow proposals. Scenarios G-K now exist but have never been RUN — the eval table still
  reads `(pending first run)`.
- `sakubun/CLAUDE.md` says "the 21 tool definitions" (actually 30). Governance file — flagged, not edited.
- Three prod backups now sit gitignored in the sakubun repo root (`premigration`, `preerrordetail`,
  `prelearnernote`). Delete once the migrations are trusted.
- `app/groups/[id]/loading.tsx` stayed dirty from a parallel pass all day and was never staged.
