# 2026-06-15

<!-- Recall tier — dated digests. NEVER auto-loaded; read on demand. Schema + rules: README.md -->

## B4 control-plane DEPLOYED (gate-approval live) + /nuc-set-env skill + key-leak rotation

```yaml
id: 2026-06-15-01
type: episodic
created: 2026-06-15T00:30:00+07:00
last_accessed: 2026-06-15
importance: 7
milestone_id: agent-os-evolution
related_ids:
  - { id: 2026-06-14-00, why: 'rolls up into the agent-os anchor — this is B4 going from code-complete to LIVE' }
  - { id: 2026-06-14-01, why: 'continues the same autonomy / Knowledge-OS thread' }
embedding: null
```

**What happened**

- Installed B4 governance to live from the sandbox (`autonomy-gate.mjs` token-release + `gate-cli.mjs` + orchestrators),
  re-verified 20/20 + 24/24 from the live locations, committed (human-installs-governance).
- Provisioned the two-way control plane end-to-end: RSA keypair, private `nuc-agent-gates` gates repo (created via the
  newly-installed `gh`), fine-grained PAT, Discord channel id; deployed `nuc-ops-bot` (`gate_approval.py`) →
  **bot logs `gate-approval ON`** (feature live, polling the gates repo, ready to post Duyệt/Từ chối).
- Built a NEW skill **`/nuc-set-env`** (the user found manual `.env` editing too fiddly + under-specified): local mirror
  `~/.nuc-env/<app>.env` → ssh STDIN → idempotent upsert into `/opt/apps/<app>/.env` (atomic · chmod 600 · `.env.bak` ·
  force-recreate). Auto-generated the signing key file→file (never printed). Merge tested 22/22.
- **Incident:** a diagnostic `grep -oE '^KEY='` over a malformed `.env` **dumped the private signing key into the
  transcript** → ROTATED the key (fresh keypair, new committed pubkey), hardened the merge to **auto-heal** (drop
  orphan / non-`KEY=` lines), and switched to inspecting secret files by **count/length only**.
- Added `.gitattributes` (`*.sh eol=lf`) — autocrlf was rewriting committed shell scripts to CRLF (breaks under bash).

**Decisions made** (distilled upward — don't duplicate here)

- Ledger §A 2026-06-15 row: secure env delivery + the secret-leak pitfall + auto-heal (`06-knowledge-ledger.md`).
- Memory `[[never-print-secret-file-contents]]` (feedback) — inspect secrets by count, never `grep -o`/`cat` the value.
- B4 realizes ledger #56 (the signed gate-token design — now DEPLOYED). Plan `2026-06-14-autonomous-agent.md` B4a.4 → done.

**Open threads**

- **e2e B4b.3 NOT yet run** (the payoff): park at a real gate → bot posts buttons → approve from PHONE → agent crosses
  exactly one `auto/*` push + PR → assert zero T4. Needs a joint session (user + phone); recommended fresh-session.
- Doc-sync remaining: `INVENTORY.md` + `auth-apps.md` should record the `nuc-agent-gates` repo + the bot's new gate
  env keys (NAMES only) — gateway, no endpoint.
- Human edit suggested (`CLAUDE.md` is governance, agent doesn't self-edit it): add `/nuc-set-env` to its skill catalog.
- B5 (real unattended window) + Layer C C3 still pending per `plans/2026-06-14-autonomous-agent.md`.

## B4b.3 LIVE e2e PASSED (gate released exactly 1 push+PR) — 4 defects surfaced + fixed/proposed

```yaml
id: 2026-06-15-02
type: episodic
created: 2026-06-15T06:45:00+07:00
last_accessed: 2026-06-15
importance: 8
milestone_id: agent-os-evolution
related_ids:
  - { id: 2026-06-15-01, why: 'B4 went DEPLOYED there; this is the first time the approval path was actually exercised' }
  - { id: 2026-06-14-00, why: 'rolls up into the agent-os anchor' }
embedding: null
```

**What happened**

- Ran the live B4b.3 e2e (supervised, approve-from-phone). **Result: PASSED** — under `CLAUDE_AUTONOMOUS=1` the hook
  ALLOWED exactly `git push origin auto/b4b3-e2e` + `gh pr create` (PR #1) with the live bot-signed token, then after
  `consume` a 2nd `auto/*` push was BLOCKED (single-use) and `git push origin main` BLOCKED. **Zero T4.** Live keypair
  consistent (signer pub == committed `gate-approval.pub.pem`). Throwaway plan + branch + PR cleaned up.
- The first real click surfaced **4 defects** (B4 was "DEPLOYED" but the button had never been pressed):
  1. **channel-authz** — `gate_approval._decide` reuses `guards.user_allowed` (hard-binds `OPS_CHANNEL_ID`); the card
     posts to a *separate* `GATE_APPROVAL_CHANNEL_ID` → every click rejected. **Fixed live**: collapsed the env to ops.
  2. **worker permission gap** — no allowlist for `node gate-cli` / `gh pr create` → headless worker auto-denied, could
     never run the control-plane. **Fixed live**: added 2 entries to `.claude/settings.local.json`.
  3. **(GATE)-count gap** — orchestrator `count_unchecked` excludes `(GATE)` lines → an approved gate that's the last
     step never gets a crossing batch (auto-pilot can't autonomously cross). **Sandbox fix proposed.**
  4. **worker reliability** — Sonnet worker mis-diagnosed permissions (didn't retry after the fix), re-marked `(GATE)`,
     dropped PR-title quotes (bash parse error). Drove the cross with a constrained `CLAUDE_AUTONOMOUS=1` invocation.
- **Secondary leak (benign):** the `nuc-set-env-remote.sh` "names-only" listing printed a base64 *private key* line from
  a malformed `.env` — verified DEAD (orphan pubkey 86f5 ≠ committed a546 → no rotation), removed the orphan, proposed a
  bounded-key-length fix. Reinforces #59 + `[[never-print-secret-file-contents]]`.
- Mid-session: my `2>&1 | Tee-Object` on the orchestrator's `& claude` (a NATIVE exe) under `$ErrorActionPreference=Stop`
  turned a benign stdin warning into a terminating error → silently killed the worker (PowerShell 5.1 gotcha). Re-ran
  without `2>&1`. Also hit + waited out a usage session-limit (resets 3am Saigon).

**Decisions made** (distilled upward — don't duplicate here)

- Ledger §A 2026-06-15 row #60: "first live exercise finds what offline tests + 'deployed' miss" + the 4 defects.
- `nuc-ops-bot/docs/decisions.md` 2026-06-15: channel-authz bug + stopgap + proper fix.
- `plans/2026-06-14-autonomous-agent.md`: B4b.3 → `[x]` + the 4 findings; 3 fixes proposed in `plans/b4b3-fixes-sandbox/`.

**Open threads**

- 3 sandbox fixes await human install+commit (`plans/b4b3-fixes-sandbox/INSTALL.md`): #1 nuc-set-env leak (governance),
  #2 auto-pilot (GATE)-count (governance), #3 gate-approval channel-authz code (bot, optional). **B5 is blocked until #2.**
- 3 local commits NOT pushed (root `b58590a`+`7076415`; bot `49a8a69`) — user to push (bundle bot push w/ a redeploy).
- Still pending from -01: INVENTORY/auth-apps doc-sync; add `/nuc-set-env` to `CLAUDE.md` skill catalog (human edit).
