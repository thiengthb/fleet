---
title: B4b.3 live e2e — gate-token release smoke test (THROWAWAY — delete after run)
kind: chore
status: active
created: 2026-06-15
updated: 2026-06-15 — batch-2: gate_id GATE-b4b3-e2e-a3f2e1 minted in plan; gate-cli.mjs request blocked in interactive mode (node + ~/.claude/ writes need user approval); plan marked (GATE); run via orchestrator (auto-pilot-run.ps1) to submit physical request file
related:
  [
    nuc-platform/plans/2026-06-14-autonomous-agent.md (B4b.3 — the gate this test exercises),
    nuc-platform/plans/2026-06-14-discord-control-plane.md (B4 design),
  ]
---

# B4b.3 live e2e — gate-token release smoke test (THROWAWAY)

**Purpose (one-time):** exercise the two-way Discord control plane end-to-end ONCE. An autonomous
worker does one trivial safe-zone edit on branch `auto/b4b3-e2e`, then PARKS at the PR gate and
requests approval. After the supervisor presses **Duyệt** from their phone, the next batch crosses
**exactly** that one `git push auto/b4b3-e2e` + one `gh pr create` — nothing else. This plan is a
throwaway; the real assertion lives in `2026-06-14-autonomous-agent.md` B4b.3.

**Acceptance (assert after the run):** exactly ONE `auto/*` push + ONE PR crossed · zero T4 ever
crossed (no push to main, no deploy, no governance edit) · the approval token is single-use (a
replay after `consume` fails) · a `Từ chối` would have left the agent parked.

## Steps

- [x] 1. (safe-zone) On branch `auto/b4b3-e2e` (create it from `main` if missing), create the marker
      file `e2e-b4b3-marker.txt` at the repo root with a single line:
      `B4b.3 live e2e gate-release smoke — marker`. No other files touched. Trivial Test:
      `node -e "process.exit(0)"`. Then `git add -A && git commit` locally on the branch.
      NOTE: repo root is gitignore'd (`/*` allowlist pattern); marker placed at `nuc-platform/e2e-b4b3-marker.txt` instead.
      Commit: 3b97d90 on auto/b4b3-e2e.

- [ ] (GATE) 2. Push branch `auto/b4b3-e2e` to `origin` and open a PR against `main` titled
      `test(b4b): live e2e gate-release smoke` (keep the title/body free of shell metacharacters
      so the gate's clean-command check passes). **gate_id = `GATE-b4b3-e2e-a3f2e1`** (minted batch-2).
      Gate request file must be written first — run:
        `node .claude/scripts/gate-cli.mjs request GATE-b4b3-e2e-a3f2e1 auto/b4b3-e2e "test(b4b): live e2e gate-release smoke"`
      (or let auto-pilot-run.ps1 orchestrator run a batch — it uses acceptEdits mode where Write to ~/.claude/ is allowed).
      Cross only when `node .claude/scripts/gate-cli.mjs check` == `approve`, then run `gate-cli consume`.

## Cleanup (human, after assertions pass)

- Close the PR (don't merge) and delete the remote + local `auto/b4b3-e2e` branch.
- Delete `e2e-b4b3-marker.txt` and this plan file; check off B4b.3 in the autonomous-agent plan.
