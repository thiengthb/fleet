# 06 — Cross-project knowledge log (index)

> **Index** of the platform's memorable lessons — does NOT copy content, only **one line per lesson + a pointer**
> to the full place. Two sources: (a) lessons **spanning multiple projects** (recorded right here), (b) lessons **for a single
> project** (living in `<project>/docs/decisions.md`, only pointed to here).
>
> Established 2026-06-12. Built up by the `/session-wrap` skill. Recording standard: see `05-documentation-standard.md §5`.

---

## How to use

- Starting something new and want to know "have we tripped on anything related?" → scan the table below, follow the pointer to the detail.
- Learned something **cross-project** (applies to ≥2 projects, or to the platform itself) → add one line to section A.
- Learned something **for a single project** → record it fully in `<project>/docs/decisions.md`, then (if worth others
  knowing) add a pointer line in section B.
- **System/infrastructure**-level traps (Docker/Traefik/Watchtower/Authentik) do NOT go here — their place is
  `02-known-traps.md`. This file handles **development/product/decision** knowledge.

---

## A. Cross-project lessons (content here)

| Date | Lesson (one line) | Applies to | Detail |
|------|------------------|--------|----------|
| 2026-06-12 | **Compute DYNAMICALLY instead of storing derived columns** (streak, delay, progress…) to avoid stale data when the source changes. | every app with stats | `todo/docs/02-technical.md §2` (the "Dynamically computed values" table) |
| 2026-06-12 | **`CLAUDE.md` thin + heavy spec split into `docs/`** — don't let the auto-loaded file bloat, it costs context every turn. | every project | `05-documentation-standard.md §2`; `todo/CLAUDE.md` (slimmed sample 641→327 lines) |
| 2026-06-12 | **Endpoints called by client-machines (MCP/OAuth/webhook/health) must NOT sit behind forward-auth** — split into their own router, auth at the app layer. | web-app with Authentik | `CLAUDE.md` invariant #8; `coding-convention §9`; `authentik/docs/auth-apps.md` |
| 2026-06-13 | **Adopt a community skill by ADAPTING, not copying** — read scripts line-by-line (drop if non-essential), strip invariant-conflicts, kill dangling refs, re-scope the description, run the conflict grep-guard. | every skill added to `.claude/skills/` | `/skill-authoring`; `nuc-platform/07-SKILL-CANDIDATES.md` |
| 2026-06-13 | **Don't pre-install a skill before its triggering need** — it's clutter + a context tax; defer "situational" ones with a note instead. | every skill add | `07-SKILL-CANDIDATES.md §1b` |
| 2026-06-13 | **A frozen security/IOC list gives false confidence** ("scan passed" against a stale list) — prefer live advisories + `npm/pip audit` over a baked-in scanner. | supply-chain hygiene | `/supply-chain-guard` |
| 2026-06-13 | **Multi-user MCP: gate ONLY `/api/oauth/authorize` behind Authentik** (the browser step) so it learns the email and binds it into the OAuth token (`sub=email`); keep `/api/mcp`+`/token` exempt. Drop shared static bearers — they can't identify a user. Thread per-request identity into the core client with `AsyncLocalStorage` (mcp-handler tools don't get the request). No token in the Claude JSON → works for free tier. | web-app with MCP + Authentik + multi-user | `yakudoku/docs/decisions.md` (2026-06-13 MCP entry); `authentik/docs/auth-apps.md` (yakudoku §) |
| 2026-06-13 | **"Internal-only" ≠ trusted on the shared `edge` network**: an app that forwards an identity header (`X-User-Email`) to a sibling backend must gate it with a shared **service token** (backend 401s without it) + resolve the user server-side + verify row ownership (IDOR). Any edge container could otherwise spoof it. | any app with a separate backend on `edge` | `yakudoku/docs/decisions.md` (2026-06-13 service-token entry); `core/app/auth.py` |
| 2026-06-13 | **A local project checkout can lag the remote by dozens of commits — `git fetch` before analyzing/editing it.** A stale `todo` checkout nearly caused redoing work (slim CLAUDE.md + docs split) already shipped on remote. | every project not always open locally | this session; remote `todo` was 40+ commits ahead |
| 2026-06-13 | **Add `paths-ignore: ['\*\*.md','docs/\*\*']` to each app's `deploy.yml`** so docs-only pushes don't trigger a wasted CI build + Watchtower redeploy. | every deployed app | `todo`/`yakudoku` `deploy.yml` |
| 2026-06-13 | **A project may already satisfy the doc-set via an equivalent file — don't force `01/02/03` if `INSTRUCTION.md` + `00-map` already cover it** (a 2nd source of truth just drifts). | per-project docs | `yakudoku` (`INSTRUCTION.md` = authoritative, 571 lines) |
| 2026-06-13 | **Remove a skill with no triggering need** (anti-clutter): `/saas-multi-tenant` was adopted then removed — its description loaded every session for zero use. | every skill add | `07-SKILL-CANDIDATES.md §1`; counterpart to "don't pre-install" |
| 2026-06-13 | **Organize projects by metadata on a flat table, NOT nested directories** — two orthogonal axes (`domain`=purpose for browsing, `kind`=archetype for ops) can't both fit a 1-D dir tree; a flat `INVENTORY §0` with two columns can. Nesting would break ~58 hardcoded paths across 16 docs/skills + add a 3rd dev≠NUC≠GitHub layout, for ~10 *independent* git repos (no monorepo gain). A metadata label scales better than a dir bucket (no bucketing-at-creation, no boundary problem, multi-label OK). Reconsider physical nesting only past ~25–30 rows, and even then prefer a VS Code multi-root workspace. | every project / platform org | `INVENTORY §0` (domain+kind axes) |
| 2026-06-13 | **Model choice is session-level, NOT per-task** — switching mid-session re-reads full history + drops the prompt cache (*costs* tokens), and `/model` Enter persists a GLOBAL default (`s` = session-only). Real token lever = Opus orchestrates+reviews while cheaper **subagents** do isolated bulk work under review (no contamination). Rejected a switch-per-task skill (agent can't self-switch). **Announce every subagent downgrade** (`label: task → model`, notify-don't-gate) = the user's staffing control surface. Discipline = the *right* amount, never the minimum. | every session / token discipline | `CLAUDE.md §"Model routing"`; `07-SKILL-CANDIDATES.md §5f` |
| 2026-06-14 | **A cross-project audit MUST `git fetch` every repo FIRST** (recurrence of #34, now as a hard rule): a stale local `todo` (8 behind) made a compliance audit report docs already shipped on remote as "missing" — nearly recreating 00-map/decisions/CLAUDE.md. Re-baseline (fetch + compare ahead/behind, check remote tree) before trusting any "gap". | every multi-project sweep/audit | this session (compliance-sync); `nuc-platform/plans/2026-06-13-project-compliance-sync.md` (Phase 0.5) |
| 2026-06-14 | **Python availability is MACHINE-SPECIFIC — run `python --version` before claiming local checks are impossible.** Some dev boxes have only the Windows-Store stub (then `ruff`/`mypy`/`py_compile` can't run → verify in **CI**); but the current box has real **Python 3.13.0** (`py_compile` + `pip install --target` + running scripts all work — used here to interop-test the gate-token signer). Don't assert "no local Python" as a blanket fact. | every `python-worker` / monorepo-python | this session (used real Python 3.13.0); `nuc-monitor`/`yakudoku`/`nuc-ops-bot` pyproject |
| 2026-06-14 | **A generic compliance checklist OVER-reports gaps — verify each flag against the project's `decisions.md`/ledger before "fixing".** Deliberate states look like debt: `todo`'s 327-line CLAUDE.md (a closed knowledge-os plan) and `yakudoku`'s `INSTRUCTION.md`-as-SoT were intentional; `nuc-monitor` running as root is correct (privileged monitor). Don't churn/regress what someone shaped on purpose. | per-project docs / any audit | this session; ties to #36; `todo`/`yakudoku`/`nuc-monitor` decisions.md |
| 2026-06-14 | **Semantic color = CSS tokens via `@theme inline`, NOT per-file `text-amber-*`/`emerald-*`.** Define `--ok/--warn/--alert/--free` (+ a restrained brand accent) in `globals.css` `:root`/`.dark`, map to `--color-*` so `text-warn`/`bg-warn/10` work. Kills the same "behind/overdue" amber hardcoded across ≥5 files and lets one edit re-theme state colors. A single accent on focus-ring + links adds life without breaking a neutral look. | every web-app UI | `todo/docs/decisions.md` (2026-06-14); `todo/app/globals.css` |
| 2026-06-14 | **Wrap identical first-party queries in React `cache()` to dedupe within one render** (e.g. a layout streak-chip + the page both scanning done-tasks) → one DB round-trip, not N. Cheap win for any `force-dynamic` Next app. Also: the most-queried table often has **zero indexes** — check `@@index` on the date/status/parent columns pages filter by. | every Next web-app + Prisma | `todo/docs/decisions.md` (2026-06-14); `todo/lib/streaks-query.ts` |
| 2026-06-14 | **Pitfall: bounding a "history" list query to last-N-days will silently corrupt a streak/longest/all-time aggregate computed from it.** Bound the heavy *list* fetch for display, but compute the aggregate from the FULL (cheap, narrow-select) dataset — keep the two sources separate. | any app with a history list + all-time stat | `todo/app/history/page.tsx` (`HISTORY_WINDOW_DAYS` vs `getActiveDoneDates`) |
| 2026-06-14 | **Gate a monorepo's per-image tests INSIDE the matrix lane, not via a job-level `needs:`.** yakudoku builds 3 images (core/web/bot) in one matrix job; adding the web Vitest step with `if: matrix.context == 'web'` *before* web's build-push gates the web image alone — a red web test never blocks a core hotfix. A separate `test` job + `build needs: test` (correct for a single-image app like todo/journal) would couple all three. Also: `prettier --write` on root `.github/**` YAML flips quotes when the prettier config lives in a subdir (`web/`) — hand-edit workflow YAML to keep diffs minimal. Test the thin-but-real **security glue** (PKCE via the RFC-7636 vector, JWT round-trip, the MCP auth gate) when the app's logic lives elsewhere (core owns it). | any monorepo CI / web-app with auth glue | this session; `yakudoku/.github/workflows/deploy.yml`; `yakudoku/docs/decisions.md` (2026-06-14); plan `2026-06-14-test-rollout.md` |
| 2026-06-13 | **Evaluated OpenClaw (viral autonomous-agent daemon, marketed as an n8n replacement) → DECLINED to adopt.** "Replaces n8n" is a false binary: n8n = deterministic workflow (keep it for high-volume/zero-variance), OpenClaw = autonomous reasoning agent — different categories, many teams run both. We already own its "brain" (Claude Code = skills + MCP + file-memory) and its headline *chat-to-control-the-NUC* feature as **`nuc-ops-bot`** — built SAFER: verb-bound socket-proxies (no raw `docker.sock`) + LLM-suggests/human-confirms, vs OpenClaw's full-shell *self-execute* + a skill marketplace with flagged supply-chain abuse (Cisco/Gartner/Trend Micro). Upgrade path for agent power = deepen `nuc-ops-bot` / widen the MCP tool layer / scheduled runs — NOT a 3rd always-on daemon. If ever trialed: `kind:node-worker`, PRIVATE-only (never public Traefik), pin version (no Watchtower), sandbox + vet every skill. | platform org / automation / agent capability | this session; `nuc-ops-bot` (`INVENTORY §1`); `CLAUDE.md` invariants #4/#7/#8 |
| 2026-06-14 | **Build autonomous agency as GOVERNED layers (Governance → Executor → Proposer), human stays supervisor — the realization of #43 ("deepen native, no OpenClaw daemon").** Decision tiers T1–T4 (reversibility × blast-radius) routed by a deterministic PreToolUse gate (`autonomy-gate.mjs`), NOT prompts; the hardest invariant = **self-modification prohibition** (the agent NEVER edits its own settings/hooks/skills/memory/CLAUDE.md/CI/orchestrator — propose, a human commits; the CVE-2025-53773 + Replit lessons). Unattended = branch-only, never push-main/deploy/destructive; **propose-don't-execute** (pure self-critique is empirically harmful → ground gap-analysis in external standards). | platform / automation / agent capability | `nuc-platform/09-autonomy-contract.md`; `plans/2026-06-14-autonomous-agent.md`; ties to #43 |
| 2026-06-14 | **A long-running agent must be a STATELESS worker with a FRESH context per batch (durable state on disk = the plan file + git), NOT one long session** — auto-compact (~70–80%, not disableable) degrades fidelity + burns re-derive tokens; no native auto-clear. A dumb external orchestrator loops `claude -p` (never `--continue`/`--resume`); self-contained plan steps kill re-exploration; subagents read/answer only with single-threaded reviewed writes (flat "bag of agents" amplifies error ~17× vs ~4.4× with a gatekeeper). | any long / unattended agent run | `09-autonomy-contract.md`; `plans/2026-06-14-autonomous-agent.md` (Context & token discipline) |
| 2026-06-14 | **Research-before-design as an ENFORCED gate, not a value** (anti-bias): a `kind: feature`/`system-change` plan can't go `active` without ≥2 external sources + ≥2 ruled-out options (RFC-lite `proposal.md` template + advisory `prior-art-check.mjs` hook). Survey external prior art FIRST; don't design from opinion. | every non-trivial design / proposal | `/project-plan` skill; `.claude/hooks/prior-art-check.mjs`; `.claude/skills/project-plan/templates/proposal.md` |
| 2026-07-06 | **Prisma 7 + npm 11 break every sibling's copy-paste recipe.** Prisma 7: generator `prisma-client` (generated client imported from `lib/generated/prisma/client`, NOT `@prisma/client`), CLI config via `prisma.config.ts` (+`dotenv` dev-dep — `.env` is no longer auto-loaded), runtime REQUIRES a driver adapter (`@prisma/adapter-better-sqlite3` for SQLite; no Rust engine). npm 11 blocks lifecycle scripts by default → `npm approve-scripts <pkg>` + `npm rebuild` once per repo (prisma engines, better-sqlite3, sharp). Copying `lib/db.ts` from todo/journal (Prisma 6) fails at runtime. | every new Node repo from now on | `sakubun/docs/decisions.md` (2026-07-06); `sakubun/lib/db.ts` |
| 2026-07-06 | **Windows console mangles UTF-8 in command-line args BEFORE the program sees them** — generalization of the PowerShell→git commit trap: `curl -d '{…勉強…}'` from Git Bash/PowerShell sent literal `?` to the server (data corrupted at input, looks like a display issue). For any non-ASCII payload on Windows: write a UTF-8 file and pass `--data-binary @file` / `-F file=…`, and verify by reading code points back from the store, never by eyeballing console output. | every Windows dev box / smoke test with JA/VI text | `sakubun/docs/decisions.md` (2026-07-06); memory `commit-message-windows-encoding` |
| 2026-07-06 | **Runtime verification that WRITES must use a SACRIFICIAL record, never real user data** — a live `submit_review` test wrote a fake rating onto the user's real SRS item, corrupting its schedule + analytics. Recovery was only possible because the log is APPEND-ONLY: delete the injected rows, replay the remaining history through the engine, overwrite derived state. Design corollary: keep event logs append-only precisely so derived state is always rebuildable. | every app with user state / any live verification | `sakubun/docs/decisions.md` (2026-07-06 sacrificial-item) |
| 2026-07-06 | **Docker build snapshots the context at `build` START — file edits made while a build runs silently miss the image** (happened twice in one day: guide-page edits absent from the "fresh" container, looked like a deploy that didn't take). Batch ALL edits → gates → ONE `compose build`. Also: before recreating a locally-used container, check activity (`docker logs --timestamps`) so you don't cut a live session. | every dockerized repo / local containers | `sakubun/docs/00-map.md` §How to run; this session |
| 2026-07-09 | **A killed subagent's work is recoverable from its transcript** — session-limit terminated agents mid-task TWICE; the generated content survived in the `.output` JSONL (parse `type:"assistant"` text blocks). Caveats: a synchronously-run agent's `.output` is EMPTY (only background/SendMessage-resumed agents persist one); recover, don't regenerate. Corollary: for content meant to be saved, have the agent Write files itself or parse its transcript — never ask it to re-emit long output. | any subagent fan-out workflow | `nuc-platform/log/2026-07-09.md` (2026-07-09-02) |
| 2026-07-09 | **MCP display contracts are MODEL behavior — enforce in-result, test with a model-in-the-loop eval.** The client model paraphrased a server-rendered block (dropped the reference translation) despite protocol rules. Escalation ladder that worked: required field in the tool SCHEMA → content rendered server-side → imperative display order INSIDE the tool result (the text the model reads right before replying — strongest placement). Unit tests can't catch this; a subagent role-playing the client, graded on explicit pass criteria, can (first run caught a json-payload leak nobody predicted). | every MCP server (yakudoku, sakubun, future) | `sakubun/eval/display-contract-eval.md`; `sakubun/docs/decisions.md` (2026-07-09) |
| 2026-06-14 | **Automated the "fetch before you trust local" rule (#34/#40) as a SessionStart hook.** A startup `git pull` can report "up to date" yet the remote moves minutes later (another machine pushes); `git-sync-check.mjs` fetches + scans ahead/behind/dirty across the root repo + every sibling repo at session start, warns the user (`systemMessage`) + injects `additionalContext` so the agent pulls stale repos before editing. Silent when clean; always exit 0 (SessionStart can't block); non-interactive git (`GIT_TERMINAL_PROMPT=0`) + per-repo timeout so it can't hang. Caught a real 3-commit drift the moment it shipped. | every repo / multi-machine workflow | `.claude/hooks/git-sync-check.mjs`; `.claude/hooks/README.md`; ties to #34/#40 |
| 2026-06-14 | **Planning spine is now 3 layers: `/idea` (candidate backlog `10-idea-queue.md`) → `/brainstorming`+proposal → `/project-plan` (accepted)** — realizes autonomy Layer C (#48). Idea ranking GATES feasibility+fit first (MoSCoW/Kano), then coarse RICE/WSJF, then an interest bonus **capped ≤15%** (never leapfrogs value). Self-critique on ideas MUST anchor to the supervisor's accept/reject **oracle** (Reflexion) — a closed self-scoring loop degrades ([Huang 2310.01798](https://arxiv.org/abs/2310.01798)/CRITIC/Reflexion). Separate axis from `07-SKILL-CANDIDATES` (external skills). Per-task token forecasting is unreliable (self-corr r≈0.39, ~30× variance) → size by p99/`cost:` + a runtime hard cap, don't predict. | platform / agent capability / planning | `nuc-platform/10-idea-queue.md`; `plans/2026-06-14-phase1-idea-queue-proposal.md`; `plans/2026-06-14-agent-os-evolution.md`; ties to #48/#50 |
| 2026-06-14 | **Agent memory is THREE tiers (MemGPT) — pick the right one or it drifts/bloats.** *core* = `00-map`+thin `CLAUDE.md` (always auto-loaded) · *recall* = dated `nuc-platform/log/YYYY-MM-DD.md` session digests (the "what happened when", **NEVER auto-loaded** — read on demand by date/`milestone_id`) · *archival* = `decisions.md`+ledger (distilled "why", append-only). One write flow: `/session-wrap` writes the recall digest, then distills the durable *why* UPWARD (link, don't copy). Day-log frontmatter is RAG-ready (`embedding: null` ⇒ idea-0002's pgvector build is additive, no migration); design grounded in Park (recency+importance+relevance) + event-sourcing immutability; RAG deferred under ~200K tokens (full-context still wins). Writing to `log/` is T2 (outside governance-locked `.claude/memory/`) so the unattended worker may persist its digest. | platform / agent capability / memory | `nuc-platform/log/README.md`; `plans/2026-06-14-phase3-daylog-memory-{proposal,build}.md`; ties to #48/#52/#54 |
| 2026-06-14 | **Before building a PLANNED skill, check whether a SHIPPED one already realizes it (dedup the roadmap).** Dogfooding `/idea` surfaced that autonomy Layer C's planned `/feature-proposal` (C1) + bounded-backlog/Reflexion (C2) were *already* built as `/idea` (gap-analysis + analyze→proposal + WIP cap + `outcome:` oracle). Folded: marked C1/C2 done-by-/idea, did NOT build the duplicate skill, kept only the distinct residue (C3 = the *unattended* orchestrator→gap-analysis integration). A plan written before a sibling feature shipped can silently spec a dup. | platform / agent capability / planning | `plans/2026-06-14-autonomous-agent.md` (Layer C); `10-idea-queue.md` (idea-0009); ties to #52/#54 |
| 2026-06-14 | **An idea-ranking interest signal needs TWO bias guards, not one — the 15% cap is necessary but NOT sufficient.** The cap stops a single leapfrog; it does NOT stop a feedback loop, because the bias **compounds per re-sort** ([Mansoury CIKM'20](https://arxiv.org/abs/2007.13019)) and homogenizes the backlog toward past accepts, starving novel ideas. Pair the cap with an **exploration floor** (≥1 "wildcard" surfaced per sort, ranked on `base` only). Also: `interest` must be **DERIVED from human signals only** (the accept/reject oracle + tagged prefs in `user-profile.md`), confidence-weighted (Hu 2008 — sparse data ⇒ low confidence ⇒ *why* the cap is small), never agent self-scored (Reflexion). Dogfooding the skill on itself caught two real things at once: queue **drift** (idea-0004 had an accepted plan but its block still read `active/null`) and a **homogeneous backlog** (first exploration-floor check found NO wildcard — all active ideas were autonomy/Knowledge-OS). | platform / agent capability / planning | `nuc-platform/10-idea-queue.md` §Rules; `plans/2026-06-14-phase2-interest-model-proposal.md`; `.claude/skills/idea/SKILL.md`; ties to #52/#48 |
| 2026-06-14 | **Two-way HITL approval via a SIGNED gate-token** — bot signs (RSA-SHA256), agent + `autonomy-gate.mjs` verify with the PUBLIC key; bounded-enum button (never free-text→command), `gate_id`+`exp`+single-use `jti`. Even a valid token unlocks ONLY a non-main `auto/*` push + its PR — T4 stays hard-blocked. Channel = a private "gates" git repo (bot via GitHub API, orchestrator via git). **Corollary: Claude Code's auto-mode classifier blocks even SUPERVISED edits to the live gate → don't fight it; propose the change in a sandbox + human installs.** Cross-language sign/verify MUST be interop-tested. | platform / autonomy / any HITL gate | `nuc-ops-bot/gate_approval.py` + its decisions.md; `plans/2026-06-14-discord-control-plane.md`; `.claude/scripts/gate-verify.mjs` |
| 2026-06-14 | **A self-improving agent may DRAFT its own skills, but never INSTALL them — propose-don't-install, made physical by path.** Adapted Hermes' detect+draft (a process repeated ≥3× → a drafted `SKILL.md`) + Voyager self-verify + a Curator anti-sprawl pass, but DROPPED Hermes' dangerous half (auto-install/use/patch in a closed loop). The drafter writes to `nuc-platform/skill-proposals/` — **outside** the gate's `\.claude\/skills\/` lock, so drafting is a normal **T2** write; installing = a **human** moving the file into `.claude/skills/` (**T4**, hard-blocked for the agent). Grounding: ADAS (2408.08435) warns auto-generated artifacts need human-checked safety/interpretability; Anthropic mandates review-before-install; even Hermes gates *risky* changes behind PR review. The Proposer pattern generalizes: `/idea`→features, `/skill-proposer`→skills (same spine/oracle/gate). Guards: rule-of-three minimum, WIP cap ≤1–2/scan, dedup, "nothing worth proposing" first-class. | platform / agent capability / self-improvement / governance | `.claude/skills/skill-proposer/`; `nuc-platform/skill-proposals/README.md`; `plans/2026-06-14-skill-proposer-{induction-proposal,build}.md`; `09-autonomy-contract §2`; ties to #48/#52/#54 |
| 2026-06-14 | **Testing is TIERED + spec-anchored, not "TDD everything".** Mandate test-first ONLY for *pure logic* (no I/O, deterministic) where Nagappan's evidence (defect ↓40–90% at +15–35% time) and lowest cost coincide; *test-alongside* for actions/components; *consumer-driven contract tests* (Zod-contract, fixtures-first, broker deferred to ≥2 teams) for cross-repo HTTP seams = the real multi-team failure mode; Playwright E2E sparse at the top. Spec→test bridge = **acceptance criteria (Given/When/Then, 1 AC→1 test)** written ON the existing `/idea→proposal→/project-plan` spine (SDD-lite, NO parallel Spec-Kit/Kiro tool — extend-don't-rebuild). Whole standard is **tiered + trigger-gated** so a multi-user/multi-repo future is served without taxing a solo-now operator with blanket ceremony. Routed by skill `/testing-standard`. | every repo / testing / multi-user scaling | `nuc-platform/11-testing-standard.md`; `.claude/skills/testing-standard/`; `plans/2026-06-14-testing-spec-discipline-{proposal,build}.md`; ties to #50/#52 |
| 2026-06-15 | **Deliver a NUC app's env/secrets via a local mirror → ssh STDIN (never argv/chat); inspect secret files by COUNT/length only.** A diagnostic `grep -oE '^KEY='` over a *malformed* `.env` printed a private key into the transcript → forced a **key rotation** (the transcript is unsafe for secrets — invariant #4). The `/nuc-set-env` skill: `~/.nuc-env/<app>.env` → ssh **STDIN** → idempotent upsert into `/opt/apps/<app>/.env` (atomic · chmod 600 · `.env.bak` · force-recreate), with **auto-heal** (drop orphan / non-`KEY=` lines) — because an upsert that replaces only the FIRST line of a multi-line legacy value leaves orphan fragments. Generate machine secrets file→file (never to stdout). Also added `.gitattributes` `*.sh eol=lf` (autocrlf was CRLF-corrupting committed shell scripts → `$'\r'` under bash). | any NUC app needing env set / secret hygiene | `.claude/skills/nuc-set-env/`; `[[never-print-secret-file-contents]]` memory; ties to #56 |
| 2026-06-15 | **First LIVE exercise of a HITL/security path finds what offline tests + a "deployed" log miss — "logs ON" ≠ "the path works".** B4 was marked DEPLOYED (bot logged `gate-approval ON`) but the approval button had NEVER been clicked; the first real B4b.3 click surfaced 4 defects: (1) `gate_approval` reuses `guards.user_allowed`, which hard-binds `OPS_CHANNEL_ID` → a click in a *separate* `GATE_APPROVAL_CHANNEL_ID` is always rejected ("⛔ không có quyền"); (2) the autonomous worker had no permission-allowlist entry for `node gate-cli` / `gh pr create` (headless prompt = auto-deny) → it could never run the control-plane; (3) the orchestrator's `count_unchecked` EXCLUDES `(GATE)` lines → when the gate is the last step the loop sees 0 work and never spawns the crossing batch (auto-pilot can't autonomously cross an *approved* gate); (4) the Sonnet worker mis-orchestrated (mis-diagnosed permissions w/o retrying, dropped PR-title quotes → bash parse error). The signed-token HOOK itself held EXACTLY as the 24/24 offline suite predicted — allowed precisely one `auto/*` push + one `gh pr create` with the live bot token, single-use enforced (2nd push blocked: "no current-gate state"), `git push main` blocked, **zero T4**. Also: a "names-only" env listing (`grep -oE '^...='`) LEAKS a value on a malformed `.env` (leaked a DEAD key here — pubkey ≠ committed verifier, no rotation; fix = bound key-name length) — refines #59. Quick fixes applied live (worker allowlist + bot channel collapse); deeper fixes prepared as a **sandbox** (propose-don't-install) for human commit. | platform / autonomy / any HITL gate / verification | `plans/2026-06-14-autonomous-agent.md` (B4b.3 findings); `nuc-ops-bot/docs/decisions.md` (2026-06-15 channel-authz); `plans/b4b3-fixes-sandbox/`; ties to #56/#48/#59; `[[verify-end-state-not-upload]]` |
| 2026-06-15 | **A CRLF↔LF mismatch when `cp`-installing a sandbox file over a live one makes `git diff` show the WHOLE file changed — that's noise, not the real edit.** The live `.claude/scripts/*` were CRLF, the b4b.3 sandbox copies LF, so `diff -u` flagged every line though the true delta was ~5 lines. See the real change with `git diff --stat` or `diff --strip-trailing-cr` (both compare NORMALIZED blobs), and re-normalize the working-tree EOL per `.gitattributes` AFTER the cp (`*.sh`=LF, `*.ps1`=CRLF — a CRLF `.sh` breaks bash with `$'\r'`). Verify a governance install by re-running its checks from the LIVE location, never the sandbox. Refines #59. | platform / any sandbox→live install / governance | `plans/2026-06-14-autonomous-agent.md` (B4b.3 fixes installed); `.gitattributes`; ties to #59/#60 |
| 2026-06-16 | **A HITL/autonomy loop that passes OFFLINE and passes a ONE-CLICK live test is still not LOOP-reliable — the residual risk is the worker MODEL over-thinking deterministic tool output, and the fix is to FORBID re-verification, not add more checks.** B5 ran the full unattended auto-pilot loop end-to-end (3 fresh-context Sonnet batches: subagent-delegated wide read → park → **approve-from-phone** → cross exactly one `auto/*` push + PR → `consume`; **zero T4**, `main` untouched, verified independently of the worker's digest). But finding #4 reproduced on the cross step: a fresh Sonnet worker got a valid `approve` from `gate-cli check`, then "double-checked" by hand against the WRONG relative path (`agent-gates/gates/` vs `~/.claude/agent-gates/`), overrode it to `none`, and stranded an approved gate; a 2nd fresh worker crossed. The signed-token HOOK held throughout — judgment, not the gate, is the weak link. Fix proposed (sandbox, human-installs): `/auto-pilot` Step 1.5 must TRUST the CLI's one-word output verbatim and NOT hand-re-verify (the CLI already checked sig/gate_id/exp/jti); escalate just the crossing batch to Opus if it persists (R3). Also: RE-HIT #60's `2>&1`-on-native-exe trap on the first launch (terminating error silently kills the worker) — run the orchestrator plainly/foreground, and READ the ledger traps before re-running. | platform / autonomy / any HITL loop / verification | `plans/2026-06-14-autonomous-agent.md` (B5 + finding #4); `nuc-platform/skill-proposals/auto-pilot-trust-gate-cli.md`; `log/2026-06-16.md`; ties to #56/#60/#48; `[[verify-end-state-not-upload]]` |
| 2026-06-16 | **A self-running agent = three ADDITIVE parts, none of which the agent may self-install: a scheduled TRIGGER, self-SOURCED work, and async Q&A — each with a silent-failure trap.** The "ignition" (a Windows Task Scheduler task) was the missing piece, not the loop (Layer B already ran *when launched*). Researched traps: (a) the task MUST be **"run only when user is logged on"** — Session 0 can't decrypt the DPAPI-protected `~/.claude` auth → `claude` fails **silently**; (b) log via **`Start-Transcript`**, never `2>&1` on the native exe (re-hit of #60); (c) C3 self-sourcing is **idle-gated + once/day + propose-only** (balanced objective, anti-churn); (d) async Discord answers are **signed** (authenticity, reusing the B4 RS256 gate) but the worker treats the answer as **DATA, never a command** (authenticity != authority — `kind:'answer'` token is disjoint-by-construction from an approval token). Built **ADDITIVELY** — a NEW `auto-pilot-scheduled.ps1` wrapper + NEW `gate-answer.mjs`/`ask-cli.mjs` (26/26), leaving the 24/24-verified orchestrator + gate **byte-identical** (additive install beats editing a verified critical file). Trigger/scripts/SKILL/bot are governance → drafted to a sandbox, **human installs** (propose-don't-install is what MAKES unattended self-running safe; the agent also cannot "flip itself autonomous" inside a chat — autonomy is a separate launched process by design). | platform / autonomy / self-running agent | `plans/2026-06-16-self-running-agent.md`; `plans/self-running-agent-sandbox/` (sandbox retired 2026-06-17 — see plan `2026-06-17-retire-self-running-sandbox.md`); ties to #48/#52/#56/#60; `[[route-questions-via-discord-not-blocking]]` |
| 2026-06-17 | **Arming a Windows Task Scheduler trigger needs admin (confirmed) — and once it fires, TWO silent traps kill `claude` on the Task-Scheduler→claude path, both invisible until you capture the child's output.** `Register-ScheduledTask` is denied (`0x80070005`) from a non-elevated shell even for a per-user logged-on task → the agent CANNOT self-arm the trigger (a no-admin HKCU `Run` autostart is technically possible but the safety classifier rightly blocks agent-chosen persistence — surface the fork, let the human pick; confirms #63). A human runs `register-task.ps1` elevated (idempotent; pin run-as to the real logged-on user, not the elevating id). THEN: ① a console child inherits the task's console group and dies the instant `claude.exe` starts with `STATUS_CONTROL_C_EXIT` (`0xC000013A`), zero output → launch it in its OWN console via `Start-Process` (NOT `& claude`, NOT `-NoNewWindow`); ② `Start-Process -ArgumentList` (PS 5.1) MANGLES space-bearing args — `Bash(git merge:*)` split at the space + the prompt garbled → claude got rubbish and **no-op'd with exit 0** (textbook "intermediate green ≠ done") → pass via a generated runner script that reads the prompt from a file and SPLATS to claude (`Start-Process` then sees only `powershell -File <runner>`). Found both by REDIRECTING the child's stdout/stderr to files (safe from #60's in-pipeline `2>&1` trap) and reading them — a 12 s exit-0 with no artifact was the tell. With both fixed the armed task self-sourced a real grounded idea (idea-0012), gate holding (zero T4). | platform / autonomy / any Windows-scheduled headless CLI | `plans/2026-06-16-self-running-agent.md` (T2/T3/C3.3); `log/2026-06-17.md`; `register-task.ps1`; confirms #63, ties to #60; `[[verify-end-state-not-upload]]` |
| 2026-06-17 | **An async-Q&A control plane is "done" only once a real round-trip click is verified — and a button+Modal beats an `on_message` reader (no privileged intent, no boot-crash, async-safe).** Phase 3 lets the autonomous worker ask a free-form question via the gates repo; the bot posts it + signs the supervisor's reply (RS256, REUSING the gate-approval key — a `kind:'answer'` token, disjoint-by-construction from approvals, single-use jti, treated as DATA). KEY choices: (a) prefer **button+Modal** to `on_message` — `on_message` needs the privileged `message_content` intent, which if declared-in-code-but-not-granted in the Dev Portal makes `discord.py` raise `PrivilegedIntentsRequired` and crash the WHOLE bot on boot; a persistent button's click is a FRESH interaction so the 15-min Modal window starts at the click → async-safe (the supervisor may answer hours/days later). (b) Offer **preset option buttons + a free-text "Khác"** so the ask is one-click + legible. Verified LIVE twice (typed "Giữ 4h"; then a 4-option one-click) — `ask-cli check` printed the signed answer, gate held. A separate-repo prod push (`nuc-ops-bot` main → Watchtower deploy) is correctly classifier-blocked from the agent — the human pushes. | platform / autonomy / any Discord HITL or async Q&A | `nuc-ops-bot/ask_answer.py` + its `docs/decisions.md` (2026-06-17); `.claude/scripts/ask-cli.mjs` (`--options`)/`gate-answer.mjs`; `plans/2026-06-16-self-running-agent.md` (D1-D5); `log/2026-06-17.md`; ties to #62/#63/#64; `[[ask-with-options-not-open-ended]]`, `[[verify-end-state-not-upload]]` |
| 2026-06-17 | **Monitor app *liveness* by READING Docker's `HEALTHCHECK` status over the docker.sock you already mount — never by network-PROBING.** A container can be `running` while the app inside is wedged or its DB is down; `docker ps`-style checks miss it. Reading each container's `State.Health.Status` (via `containers.list()` → `c.attrs['State']['Health']`) needs ZERO network change — crucial when the monitor has a "no edge / no exposed port" isolation invariant (joining `edge` to HTTP-probe would *violate* it = a governance change, not a feature). Push the DEEP "is the DB reachable?" check INTO each app's own container `HEALTHCHECK` (a deep `/api/ready`·`/ready` running `SELECT 1`), where it can reach its own DB — so "healthy" means "app+DB serving" and a dead Postgres surfaces too. Split liveness (`/api/health`, shallow) from readiness (`/api/ready`, deep); plain-Docker `unhealthy` only *reports* (no auto-restart) so a deep healthcheck is safe (unlike a K8s liveness probe). **Process lesson:** read the TARGET component's local `CLAUDE.md`/invariants BEFORE recommending an option — the first recommendation here (join `edge`) was wrong until nuc-monitor's invariant was read. | nuc-monitor + any monitored app (journal/yakudoku) / option-proposals | `nuc-monitor/docs/decisions.md` + `journal`/`yakudoku` `docs/decisions.md` (all 2026-06-17); `nuc-platform/plans/2026-06-17-nuc-monitor-app-health-{proposal,build}.md`; INVENTORY §1; `[[research-before-design]]`, `[[honest-critique]]` |
| 2026-06-17 | **A headless auto-pilot worker's self-DIAGNOSIS is unreliable — verify its claim with a direct PROBE, not its word; and two claude sessions sharing one working tree + branch CONTEND on git.** Closing the self-running loop, the FIRST real plan-advancement ran clean (Sonnet worker advanced S1–S3, parked at the destructive S4 gate, `main` untouched, zero T4) — but a batch with no work left logged a FALSE open-thread ("`gate-cli`/`ask-cli` permission-blocked", recommending an allowlist entry that already existed). A headless `claude -p --permission-mode acceptEdits` PROBE running `ask-cli check` printed `none` (executed fine) → disproved it; did NOT act on the bogus rec (refines #62/finding #4 — `acceptEdits` auto-accepts EDITS, not Bash, which is governed by the allowlist, which headless DOES honor). Separately, the full worker-initiated Discord round-trip is now verified end-to-end (ask→bot 3-button card→click→RS256 answer→`ask-cli check` reads it as DATA→consume), closing the Phase-3 loop. **Concurrency hazard:** a parallel session in the SAME working tree committed to the SAME `auto/*` branch — one session's commit swept the other's uncommitted files; coordinate (one commits before the other does git ops), and isolate per-app work in its own nested repo. | platform / autonomy / multi-session ops / verification | `plans/2026-06-16-self-running-agent.md` (done) + `2026-06-17-retire-self-running-sandbox.md`; `log/2026-06-17.md` (-06); ties to #62/#63/#65; `[[verify-end-state-not-upload]]` |
| 2026-06-18 | **A "24/7 self-improving agent loop" asked for fresh is usually ~85% ALREADY built — audit before building; the real gap is CHAINING the verified pieces + automating the human-decision handoffs between them, NOT a rebuild.** A 3-Explore + 1-research-subagent audit found executor (#49), Discord approve/ask (#56/#65), scheduler (#63/#64), day-log (#53), interest model (#52/#55) all live; only the OUTER driver (idle→propose→accept→plan→execute→wrap→retro→re-rank) + 4 human-decision auto-handoffs were missing. Grounded externally (anti-bias #50): **fresh-context-per-cycle is the right answer to "context >60% degrades"** — Anthropic recommends a fresh window OVER compaction (Claude rediscovers state from the filesystem), so there is no long session to auto-clear; **reject the long-lived daemon** (AutoGPT/BabyAGI goal-drift/runaway-cost/hallucinated-completion lineage) for bounded fresh cycles + retry-cap + budget-cap + default-deny-on-timeout; **a retro step must score against EXTERNAL signal** (tests/lint/gate/diff — Reflexion only helps when grounded; closed self-grading hits a "coherence trap" + degrades). Phase 0 (executor hardening) shipped + proven live same session. | platform / autonomy / closed-loop / planning | `plans/2026-06-18-closed-loop-driver.md`; ties to #48/#49/#50/#52/#53/#56/#63 |
| 2026-06-18 | **Editing the agent's OWN governance is TWO-tier, not a blanket "always propose" — UNATTENDED must propose + human-installs (the hook hard-blocks self-modification), but INTERACTIVE with explicit live supervisor approval, APPLY the edit directly.** Corrects #56's corollary ("strict sandbox even when supervised"): the auto-mode classifier blocks the first attempt, but the supervisor's live approval satisfies the human-in-the-loop intent of the self-modification prohibition (CVE-2025-53773), so the heavy propose-then-install ceremony is only for the unattended (`CLAUDE_AUTONOMOUS=1`) path. The user explicitly asked for this (preserves the "auto" feel, saves manual re-typing); still review + commit governance, commit only when asked. | platform / autonomy / governance / agent workflow | `[[sandbox-propose-governance]]`; `09-autonomy-contract §2`; corrects #56; ties to #60/#63 |
| 2026-06-19 | **Automating the idea→plan→execute loop keeps propose-don't-execute by graduating an accepted idea to a DRAFT plan, never to a running one — and only a SIGNED Discord answer arms it.** On `outcome: accept` the wrapper auto-writes a `status: draft` + `auto_pilot: false` plan (the plan-advance pass requires `active` + `auto_pilot: true`, so a draft can't self-run); a separate **enrol gate** then asks the supervisor and, only on a signed `enrol` answer, flips `auto_pilot: true`. Mechanism choice: use **`ask-cli`** (answer = DATA, no hook side-effect) for binary-ish decisions with NO push — reserve **`gate-cli`** for push-releasing approvals (its `approve` is wired in `autonomy-gate.mjs` to unlock exactly one `git push`+PR, so reusing it for enrol would conflate "approve push" with "approve arm"). The enrol batch reuses the live ask infra → **no bot change**. Arming is prompt-enforced today; gate-level enforcement (block `auto_pilot: true` writes without a valid enrol answer) is PROPOSED for human commit (defense-in-depth, CVE-2025-53773 standard). | platform / autonomy / closed-loop / HITL | `plans/2026-06-18-closed-loop-driver.md` (Phase 1); `proposals/2026-06-19-enrol-gate-hook-hardening.md`; ties to #56/#65/#68/#69 |
| 2026-06-19 | **Wiring-verified ≠ runtime-verified — parse + dry-run + detection unit-tests prove the scaffolding, NOT the behaviour; the live run is a separate milestone.** Phases 1+2 of the closed loop (graduation, planning Q&A, enrol, reflection) are all parse/dry-run/detection-green, but no autonomous batch has actually RUN — don't declare the loop "done" or pile more phases on an unrun foundation; the live e2e (S3.3) needs the gate-clone provisioned + the supervisor watching. Concrete sub-lessons: (a) when DETECTING a structured field/divider in a markdown queue, **anchor the regex to LINE START** — TWO separate prose-false-positives bit here: a loose `outcome:.*accept` matched the header PROSE, AND a `SimpleMatch '## Done'` matched the Rules' backtick'd `## Done` reference, collapsing the scan region to the header so **graduation would NEVER have fired in production**; (b) keep verified critical code (the C3 launch) **byte-stable** and add a parallel helper rather than refactoring it; (c) prompts passed via a PowerShell double-quoted string must avoid backticks/`$`/embedded `"` (use single quotes + plain command text). **VINDICATED same day:** a LIVE graduation smoke test (synthetic idea → throwaway branch) caught the `## Done` bug that parse + dry-run + unit-test ALL passed over (the unit-test used synthetic line arrays lacking the real prose; the dry-run "skip" was a false negative) — the cheap live run found in minutes what three layers of wiring-checks missed, and confirmed the enrol safety invariant (no signed answer ⇒ plan stays un-armed). | platform / autonomy / verification / Windows PowerShell | `log/2026-06-19.md`; `plans/2026-06-18-closed-loop-driver.md`; smoke-test fix `1dd928c`; `[[verify-end-state-not-upload]]`; ties to #60/#64/#67 |
| 2026-06-19 | **An autonomy watchdog must DISCRIMINATE "stuck" from "correctly waiting on a human" — or it false-alarms on every legitimately-parked gate; and "human decided but worker didn't pick it up" is its OWN, stronger stall class.** Measuring progress as a fingerprint of all branch tips (no tip moved in a "did-work" cycle = no progress) is necessary but not sufficient: a cycle parked on an undecided human gate ALSO moves no tip, yet is expected (the supervisor was already pinged) — detect it via the live state files (`current-ask.json`/`current-gate.json` exists AND `*-cli check`==`none`) and RESET, don't escalate. Conversely an ANSWERED ask / `approve`d gate still sitting unconsumed = the worker failed to cross a decision the human already made (the AC's "gate approved but not crossed") = escalate. Escalate only after N≥2 consecutive bad cycles and ONCE (an `escalated` flag, set only if the Discord publish-push actually succeeded), so the safety net never spams. Also: in PowerShell `git push` FAILS WITHOUT THROWING (sets `$LASTEXITCODE`, no exception) so a blanket `catch {}` never catches it — check the exit code + retry-after-`pull --ff-only`; and never `2>&1` a native git under Start-Transcript (ledger #60 turns benign stderr into a terminating ErrorRecord). | platform / autonomy / closed-loop / reliability / Windows PowerShell | `log/2026-06-19.md`; `plans/2026-06-18-closed-loop-driver.md` (S3.2/S3.4); ties to #60/#67/#72 |
| 2026-06-19 | **The closed-loop enrol gate works end-to-end LIVE through Discord — arming a plan is gated on a cryptographically-signed human click, and a cheap live demo both proved it and exposed a silent-failure.** Full chain verified on a throwaway branch: synthetic accepted idea → graduation draft plan → enrol batch mints `ASK-enrol-*` + pushes to the private gates repo → bot (`nuc-ops-bot`, polls 25s via GitHub API) posts the Discord card → supervisor clicks **enrol** → bot writes the RS256-signed answer → `ask-cli check` verifies → next cycle sets `status: active` + `auto_pilot: true` + consumes (single-use). The agent NEVER self-arms: no signed answer ⇒ plan stays `auto_pilot: false` (confirmed in the smoke test). **Reliability gap found:** `Invoke-GatePush` wraps `git push` in `catch {}` → a failed push is SILENT, so the ask commits locally but never reaches Discord and the worker would park forever (fixed by hand here; queued as plan S3.4 + ties to the S3.2 watchdog). Demo cleaned up to zero residue (authoritative dry-run opted-in=0). | platform / autonomy / closed-loop / HITL / Discord | `log/2026-06-19.md`; `plans/2026-06-18-closed-loop-driver.md` (S3.3/S3.4); `nuc-ops-bot`; ties to #56/#65/#70/#71 |
| 2026-06-20 | **Don't migrate the hand-built harness — it already sits on Anthropic-native primitives, and no "better harness" exists to move to.** A deep-research run (migrate-vs-keep) confirmed: skills (SKILL.md = the Agent-Skills open standard), the PreToolUse gate hook, subagents, thin CLAUDE.md, and the fresh-`claude -p`-worker model are all Anthropic's *recommended* patterns, not bespoke tooling — so the effort is forward-compatible, not wasted. The orchestration frameworks (LangGraph/CrewAI/OpenAI-Agents-SDK) are a DIFFERENT category (programmatic agents in code), a downgrade for a CLI dev-harness + a 4–12wk switching tax. Native betas (server-side compaction `compact_20260112`, context-editing `clear_tool_uses_20250919`, memory-tool `memory_20250818`, Managed Agent Memory) are absorbing the generic plumbing layer — but Anthropic explicitly keeps project-specific scaffolding (progress files, git-as-state, custom gates) as the developer's job. KEEP: project rules/autonomy-contract/idea-queue/git-synced 2-tier memory (no native equivalent). DROP/avoid: any custom generic compaction code. Re-evaluate KEEP-vs-native each major Claude Code release. | the platform itself / harness strategy | `plans/2026-06-14-autonomous-agent.md` "Out of scope" (Agent-SDK REJECTED 2026-06-20); `log/2026-06-20.md`; ties to #49/#68 |
| 2026-06-20 | **A long `Workflow` (deep-research etc.) DIES on session-suspend — resume needs `args` re-passed, and it's cheaper to salvage than re-run.** The script's arg-check runs BEFORE the resume cache, so `resumeFromRunId` WITHOUT `args` exits 13ms/0-agents ("no question provided"); re-pass the same `args`. When synthesis keeps getting cut, don't keep re-running the expensive final phase — the completed agents' results sit in `journal.jsonl` + `agent-*.jsonl`; dispatch ONE cheap subagent to mine + digest them (citations preserved) and synthesize in the main loop. Watch token burn: a multi-day-stalled workflow is a sunk cost, not progress. | any deep-research / multi-agent Workflow run | `log/2026-06-20.md`; ties to #49 |
| 2026-06-19 | **full-S3.3 proven LIVE — and it exposed that REMOVING a blanket `catch {}` can swap a silent failure for a WORSE crash. Under `Start-Transcript` + `$ErrorActionPreference='Stop'`, a native `git` that writes to stderr (a FAILED push) becomes a TERMINATING `NativeCommandError` (ledger #60) — so the explicit-`$LASTEXITCODE`-check retry added in S3.4a was UNREACHABLE: the wrapper crashed at the push line before it ran.** Corrects the prior row's claim that "`git push` fails without throwing" — that holds OUTSIDE a transcript, but the wrapper runs inside one. Two-part fix (`fix/gate-push-transcript-trap` `3270250`): (1) set `$ErrorActionPreference='Continue'` *inside* `Invoke-GatePush` so native errors are non-terminating and the exit-code branch/retry actually runs; (2) the retry must `pull --rebase` not `--ff-only` — the real failure is a DIVERGENCE (the bot pushed an answer concurrently → local 1-ahead/1-behind), which ff-only refuses; rebase replays our disjoint ask/report commit onto the bot's. **Verified by REPRODUCING the trap** in a sandbox under real `Start-Transcript`+`Stop` with a diverged origin (old crashes, patched survives+rebases+delivers) — you cannot catch this with a unit test that runs git outside a transcript (which is why S3.4a's sandbox test missed it). General rule: when a script sets `Stop` + `Start-Transcript`, guard every native-command call site (try/catch like `Invoke-GatePull`, or local `Continue`) — not just avoid `2>&1`. | platform / autonomy / reliability / Windows PowerShell | `log/2026-06-19.md`; `plans/2026-06-18-closed-loop-driver.md` (S3.3/S3.4); refines #72; ties to #60/#71 |
| 2026-06-19 | **The auto-mode classifier enforces the governance boundary (`.claude/hooks|skills/**`) even in an INTERACTIVE session — a vague "handle everything" is blocked, but a SPECIFIC, explicit user instruction crosses it.** So "interactive + supervisor-approved = edit governance directly" (#69) holds, but the *form* of approval matters: prepare every governance change as a VERIFIED drop-in in a non-governance dir (`proposals/`, `skill-proposals/staged/`) and let the human (or an explicit "install this") do the final move. The agent must NEVER circumvent a denial via an alternate tool (e.g. `cp` after a blocked Write = bypassing intent — forbidden). | platform / autonomy / governance | `log/2026-06-19.md`; ties to #69; `nuc-platform/proposals/`, `09-autonomy-contract.md` |
| 2026-06-19 | **Arming a plan for unattended execution (`auto_pilot: true` into `nuc-platform/plans/*.md`) is now GATE-ENFORCED in `autonomy-gate.mjs`, not prompt-promised.** A PreToolUse Write/Edit that introduces the flag is allowed iff a valid, signed, unconsumed `ASK-enrol-*` answer (text enrol/yes) authorizes it (via the shared `verifyAnswerToken` core), else hard-blocked fail-closed — closing the S1.3 gap where a buggy/injected worker could self-arm. Security-critical actions belong in the deterministic gate, not a prompt rule (CVE-2025-53773). Verified 34/34 e2e at the install location; documented residual gap: a raw Bash redirection writing the flag is not caught (Edit/Write is the worker's normal write path). | platform / autonomy / security | `log/2026-06-19.md`; `.claude/hooks/autonomy-gate.mjs`; `plans/2026-06-18-closed-loop-driver.md` (S1.3); ties to #73 |
| 2026-06-20 | **auto-pilot can't run on a sibling APP repo as-built — its governance `.claude/` (gate hook, skill, gate-cli) lives in the platform control-plane repo, but `todo`/`yakudoku` are SEPARATE nested git repos with no `.claude` gate.** cwd=app-repo ⇒ a `CLAUDE_AUTONOMOUS` worker has NO gate (violates invariant #5); cwd=platform ⇒ `git checkout -b`/commit hit the WRONG repo. Running it on yakudoku was BLOCKED at wiring, so slice F was done MANUALLY by Opus instead. Making auto-pilot app-repo-capable needs real cross-repo plumbing (orchestrator `--target-repo` + `git -C`, or a per-repo gate install) = "build more" the 2026-06-20 freeze deferred. **This cost IS the adopt-vs-shelve data: auto-pilot's only clean home today is the platform repo itself** (which rarely has clean app-feature T2 work) ⇒ leans shelve unless used across repos often. | platform / autonomy / auto-pilot | `log/2026-06-20.md`; `plans/2026-06-14-autonomous-agent.md` (FROZEN); `yakudoku#1`; ties to #74 |
| 2026-06-20 | **2nd use of `todo`'s semantic-tone-tokens + score/progress-ring UI pattern (now in `yakudoku`) — copy-in, NOT yet a shared package (rule-of-three: extract on the 3rd).** todo's UI-renovation gave `--ok/--warn/--alert` OKLCH tokens via `@theme inline` + a reusable ring primitive replacing ad-hoc conic rings; yakudoku slice F copied the shape (`--ok/--warn`, `ScoreRing`, tone-tinted badge variants). A 3rd app needing it ⇒ extract to a `ui-kit` copy-in source / `@thiengthb/*`. No-dep discipline that kept it T2-safe: CSS animation via `tw-animate-css` (no `motion`), pure-logic tests (no extra test dep). | todo / yakudoku / ui-kit | `08-SHARED-ASSETS.md` (log as DUPLICATED); `todo/docs/decisions.md` (2026-06-14 UI-renovation); `yakudoku#1` / `yakudoku/docs/plans/2026-06-20-practice-result-ui.md` |
| 2026-06-19 | **An Opus main-loop review of a Sonnet research subagent caught two FACTUAL errors before they shipped — the review step is load-bearing, not ceremony.** Building the idea-0014 backup proposal, the sonnet researcher assumed "3 Postgres (authentik/journal/n8n)"; cross-checking INVENTORY §1 showed `n8n_data` + `yakudoku_data` are **SQLite** (need `.backup`, not `pg_dump`) and the idea undercounted volumes (6→8). Rule for delegated research: verify the subagent's concrete claims against the in-repo source of truth before acting. Durable backup lesson: **app-consistent dumps are per-ENGINE (Postgres `pg_dump` / SQLite `.backup`), never a live file copy** (a live DB file copy is a corrupt/crash-state backup). | platform / model-routing / data-safety | `log/2026-06-19.md`; `plans/2026-06-19-idea-0014-nuc-backup*.md`; ties to #71 |
| 2026-06-20 | **For an MCP server consumed by claude.ai, the ONLY reliable "rich output" lever is markdown that Claude RESTATES — not widgets/MCP-Apps (broken) or elicitation (Claude Code CLI-only).** So make machine-facing tools return a markdown summary + keep the raw payload in a fenced ```json``` block so Claude retains exact IDs/tokens (`okWithMarkdown` pattern). And **personalize server-light**: store prefs in the DB, SURFACE them to Claude (in the tool's read payload) + instruct via the MCP prompts — do NOT push behavior into the grading/scoring engine. Discoverability of settings = enum tool params + options spelled out in the tool description. | yakudoku / todo (both expose MCP) | `yakudoku/docs/decisions.md` (2026-06-20 MCP-UX personalization); `yakudoku/lib/mcp/render.ts`; PRs `yakudoku#4`/`#5` |
| 2026-06-20 | **Match process weight to stakes (P-tiers P1–P3, mirroring autonomy T1–T4) + thin-slice-first — a harness built FOR token efficiency over-spends when EVERY task pays the full spine (idea→brainstorm→research→plan→docs) regardless of stakes.** P1 (trivial/reversible) = `/coding-convention` only; only P3 (architecture/dep/security/schema) gets research-before-design. Build a running END-TO-END slice BEFORE governance/docs ("verified but never used" is how a feature dies). Practice-first, THEN cheaper. | every project / platform / process | `CLAUDE.md` §Thinking & process; `plans/2026-06-20-token-optimization.md` |
| 2026-06-20 | **"Built to completion but never run on real work" = over-engineering — and the auto-mode self-mod guard correctly blocks the agent editing `.claude/skills/**` even under verbal user approval (skill change = human move).** Auto-pilot passed contrived smoke-tests but never advanced a real feature + the Sonnet worker is ~50% flaky on the gate step ⇒ "verified" yet unusable from the operator's seat. A verbal "just do it" does NOT override an autonomy-contract human-move gate — propose, the human runs it. | platform / autonomy / process | `plans/2026-06-14-autonomous-agent.md` (FROZEN R5); `plans/2026-06-20-token-optimization.md` (#6b) |
| 2026-06-20 | **Don't fan-out subagents for internal investigation you already hold context for** — delegation has overhead + failure modes (2 Sonnet subagents hit the session limit, returned 0 usable tokens; a `wc`/`grep`/targeted Read then produced the whole answer). Reserve subagents for genuinely WIDE+UNKNOWN+isolatable reads — the caveat to the "real token lever". | platform / model-routing | memory `direct-over-subagent-for-known-context`; `log/2026-06-20.md` |
| 2026-07-10 | **Split several features tangled across shared files into feature-atomic commits by RECONSTRUCTING each intermediate state, not by staging hunks blindly.** When 3 features touched the same 8 files (interleaved edits in `select.ts`/`render.ts`/`server.ts`/tests), `git add -p` per hunk is error-prone and can't split a single rewritten function. Reliable recipe: (1) back up all final files to a scratchpad, (2) revert the tree to feature-1's state (undo 2+3's edits), stage+commit, (3) restore feature-2's state, commit, (4) restore all final files, commit the rest. Each commit is the exact snapshot that passed gates at that layer; verify with `git show --stat` per commit + a clean final `git status`. Slower than one lump commit but the user explicitly values reviewable history. | any repo / clean git history | `log/2026-07-10.md`; memory `[[feature-atomic-commits]]`; sakubun commits `7059179`/`c4d0d27`/`1b60364` |
| 2026-07-10 | **Reusable visual controls belong in `ui-kit` (shadcn registry, copy-in) — build there, `shadcn build`, copy into the app, register in `08-SHARED-ASSETS.md`.** New `data-pagination` (controlled page/pageCount/onPageChange + `…` window) built canonically in ui-kit on first use (user asked for platform-wide reuse — a deliberate deviation from rule-of-three, justified because ui-kit IS the platform's shared-visual mechanism). Client-side paginate: clamp the page DURING RENDER (`Math.min(page,pageCount)`), never `setState` in an effect (React 19 `set-state-in-effect` lint). Fetch-all + paginate suits a single-user store; cap generously (10 000) as a runaway guard, never a silent truncation like the old `take: 200` that hid 300+ rows. | every web-app with lists | `08-SHARED-ASSETS.md` (data-pagination row); `ui-kit/registry/thiengthb/data-pagination.tsx`; `sakubun/docs/decisions.md` 2026-07-10 |
| 2026-07-10 | **In a long instruction prompt, SECTION ORDER is load-bearing for model behavior** — a model reads top-down and acts on the first applicable block. sakubun's session-start menu was silently skipped because "Bắt đầu buổi" sat BELOW the per-sentence loop; moving it to the top ("LÀM ĐẦU TIÊN") fixed it (eval 3/3). The thing that must happen first must be WRITTEN first. Corollary: bugs that live in *model behavior* (ordering, verbatim-paste, pre-commit sequencing) need a model-in-the-loop eval — unit tests can't see them. | any long agent/tutor prompt | `sakubun/eval/display-contract-eval.md` (scenarios F/G); `sakubun/docs/decisions.md` 2026-07-10 |

---

## B. Pointers to each project's knowledge log

> Each project has its own `docs/decisions.md` (if established). This table is just a table of contents — read the detail in that file.

| Project | Knowledge log | Notes |
|---------|-------------|--------|
| todo | `todo/docs/decisions.md` | **Established 2026-06-13** — seeded with the foundational whys (behavioral invariant, dynamic-compute, rolling-roadmap, in-process MCP + sync rules, stateless OAuth shim, schedule trust boundary, tab-role naming). Full Knowledge OS doc-set in place (00-map + 01–04). |
| journal | `journal/docs/decisions.md` | **Established 2026-06-14** — pgvector via Prisma `Unsupported` column (raw SQL only), Authentik email identity (no self-coded auth), fail-soft AI + private-entry exclusion + non-diagnostic tone, in-process node-cron. Full doc-set (00-map + 01/02/03 + README) in place; in-app `/guide` deferred. |
| yakudoku | `yakudoku/docs/decisions.md` | **Established 2026-06-13** — multi-user (shared catalog + per-user `userKey`, service-token trust, MCP OAuth `sub=email`, Discord `/link`, wipe-migration); grading/judge design, MCP expansion, JP word-lookup (Word/Kanji/DictEntry); + thin CLAUDE.md added 2026-06-13. |
| jobhunter-bot | `jobhunter-bot/docs/decisions.md` | **Established 2026-06-14** — retry-with-backoff on the n8n webhook (transient only, never 4xx), health-server readiness in a headless bot, the silent discord.js `MessageContent`/`Partials.Channel` traps, per-channel session. 00-map + thin CLAUDE.md + eslint flat config added. |
| nuc-monitor | `nuc-monitor/docs/decisions.md` | **Established 2026-06-14** — runs as root by design (non-root needs brittle host-gid matching for docker.sock/auth.log), HEALTHCHECK via loop heartbeat file, in-memory state, edge-triggered Docker + absolute-delta SSH counting. 00-map + thin CLAUDE.md + pyproject(ruff/mypy) added. |
| nuc-ops-bot | `nuc-ops-bot/docs/decisions.md` | **Established 2026-06-14** — verb-bound socket-proxies over raw docker.sock, LLM-suggests/human-confirms + post-LLM re-validation, the `ct.image`→ops-proxy 403 trap, `asyncio.to_thread` for blocking calls. 00-map + thin CLAUDE.md + pyproject(ruff/mypy) added. |
| authentik | `authentik/docs/decisions.md` | **Established 2026-06-14** (infra) — no-Watchtower/pinned-tag (Postgres migrations need a human), email-key identity + machine-endpoint exemption + XFP trap, Redis dropped. Live app registry in `auth-apps.md`. |
| n8n | `n8n/docs/decisions.md` | **Established 2026-06-14** (infra) — immortal `N8N_ENCRYPTION_KEY`, SQLite-not-Postgres, not-behind-Authentik (webhooks public), workflow source in `src/*.js`. Workflow detail in `workflows/README.md`. |
| ui-kit | `ui-kit/docs/decisions.md` | **Established 2026-06-14** (meta) — copy-in shadcn registry over npm package, only product-agnostic/stable/≥3×-reused components. |
| sakubun | `sakubun/docs/decisions.md` | **Established 2026-07-06, pivoted 2026-07-09, learning-depth upgrade 2026-07-10** (23 entries) — LOCAL-only **grammar-driven** JA↔VI translation trainer (vocab dropped 2026-07-07, learn in Anki), Claude Desktop = generator+grader+rating source (inverted trust model vs yakudoku); Prisma 7 driver-adapter mechanics; okWithMarkdown display contract → v2 (required `suggested`, verbatimEnvelope in-result order, model-in-the-loop eval `eval/display-contract-eval.md`, scenarios A–E); enrichment-as-curriculum (507-pattern library, 356 agent-seeded); **focused practice** (session menu + focusLevels/focusPatterns, explicit ask overrides gates not quotas); **deep learning loop** (research-grounded: conversational-only retry, direct+rule feedback, weak-spot-targeted composition, daily sessionGoal); sacrificial-item verification + replay recovery; `docker cp` root-ownership trap. Plans v1 + learning-v2 + drop-vocab + deep-learning-loop all done. |

> When a project establishes its `decisions.md`, change "_(to be established…)_" to a real note + the date. A project
> without a line here that already has a `decisions.md` → add the line (the job of `/session-wrap`).
