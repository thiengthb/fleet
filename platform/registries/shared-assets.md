# 08 — Shared assets catalog (reusable building blocks)

> **The cheap index of "what reusable thing already exists, where its canonical copy lives, and how to reuse it."** Read
> this BEFORE building a feature so you reuse instead of reinventing (saving tokens + dev time). Owned + kept in sync by
> the skill **`/code-reuse`** — every reuse / extraction / newly-noticed duplication MUST update this file in the same
> change (anti-drift, same discipline as `inventory.md`).
>
> Established 2026-06-13. The platform is **independent repos** — reuse is never free; see `/code-reuse` for the
> rule-of-three gate and the hybrid share model, and `references/extraction-playbook.md` for how to extract.

---

> **Renamed 2026-07-28: `ui-kit` → `commons`** (`thiengthb/commons`). The old name said "components"; the thing is
> the reusable essence of every project — components, scripts, tests, helpers. It stays a **separate git repo**, not a
> folder under `platform/`, because it works precisely by being independently versioned and installable
> (`npx shadcn add …`); folding it in would break the one mechanism that makes it useful. Rules and patterns do NOT
> move into it — they are read, not installed, and they live in `platform/`. Entries in `ledger/` and the ledger index
> still say `ui-kit`: that is what the folder was called on the day, and the record is not rewritten.

## Maturity / status legend

- **PATTERN** — a recurring shape worth copying by hand; no single canonical artifact yet (or not worth extracting).
- **TEMPLATE** — a canonical scaffold to copy from (config/CI/Dockerfile/hooks).
- **REGISTRY** — copy-in shared code with a canonical home (the `commons` model).
- **DUPLICATED — extract candidate** — built ≥2×, near-identical; flagged for extraction once it hits 3× stable.
- **SHARED (package)** — extracted to a published `@thiengthb/*` package; consumed at CI build time.

> Reminder (rule of three): 1× = build local · 2× = log here as DUPLICATED · 3× same-shape + stable = extract.

---

## A. Catalog

| Asset | What it is | Canonical / where | Reused in | Mechanism | Status |
|------|-----------|-----------|-----------|-----------|--------|
| **MCP self-issued OAuth shim (glue)** | OAuth authorize/token/register + issuer/validation for the MCP endpoint — *glue only*, NOT the tool definitions | `todo/lib/mcp/{auth,oauth}.ts` + `todo/app/api/oauth/*` | `todo`, `yakudoku/web` (near-identical: auth 38≈39, oauth 86≈89, token 63≈67, authorize 124≈129, register 32=32) | → **published package** when extracted (heavy + security-sensitive) | **DUPLICATED — still 2×, and the expected 3rd never arrived.** `idea-0023`'s MCP server was to be the 3rd consumer, which is why `idea-0013` was armed as its prerequisite. **Phase 3 (2026-07-29) cancelled hosting**, so that server runs on localhost with no auth at all and needs none of this glue. The count stays 2 and `idea-0013` returns to ARMED-not-fired — a *planned* consumer is not a consumer |
| **MCP `server.ts` (tool definitions)** | The app-specific MCP tools | per app | — | **stays local** (this is the *feature*, not glue) | PATTERN (do NOT share) |
| **Forward-auth header reader** | Read `X-authentik-email` / `X-authentik-groups` to identify/authorize the user | `todo`, `journal`, `yakudoku/web` (`lib/api.ts`) | 3 apps | copy-in or tiny package | **DUPLICATED — extract candidate** (3 apps; verify same shape before extracting) |
| **Public-router split** | Splitting health/MCP/OAuth/webhook endpoints into their own Traefik router, **exempt from forward-auth** (invariant #8) | `todo`, `journal`, `yakudoku` | 3 apps | PATTERN (compose labels) — see `coding-convention §9`, `authentik/docs/auth-apps.md` | PATTERN (well-documented; no artifact to extract) |
| **Prisma client singleton** | `lib/db.ts` global-cached PrismaClient (avoids hot-reload connection storms) | `commons/registry/lib/db.ts` (`shadcn add lib-db`) | `todo` (verified byte-identical 2026-07-29), `journal` (1 comment apart — adopt on next touch) | **REGISTRY (copy-in)** | **REGISTRY** (2026-07-29, the first non-UI item). ⚠️ **Never blind-install into `sakubun`** — its `lib/db.ts` is 92 lines carrying the fail-closed tenancy guard (its invariant #5), not the 8-line singleton |
| **Rebuild-and-verify (local container)** | Rebuild → **bounded** health wait → route sweep, so the exit code reflects the ROUTES not the build; `--gates` runs lint/test/format first, `--verify-only` touches docker not at all (for an already-serving/public app). Per-repo values live in `scripts/verify.env`, so the script stays byte-identical everywhere | `commons/registry/script/rebuild-and-verify.sh` (`shadcn add script-rebuild-and-verify`) | `todo` (2026-07-29); the richer sakubun original (`sakubun/scripts/rebuild-and-verify.sh`, +MCP/dynamic-route checks) stays local | **REGISTRY (copy-in, UNIVERSAL item** — installs into a repo with no `components.json`, framework-agnostic) | **REGISTRY** (2026-07-29; generalized from sakubun's, which was itself born of running the sequence ~14× by hand and getting it wrong twice) |
| **shadcn UI components** | Visual component stock | `commons` (`thiengthb/commons`) | every web-app | **REGISTRY (copy-in)** — already established | REGISTRY |
| **The registry itself, widened** | `commons` is no longer UI-only: a **universal item** (type `registry:item`, every file carrying an explicit `~/`-rooted `target`) installs ANY file into ANY repo — proven 2026-07-29 into a directory with no `components.json`/`package.json`. So configs, ops scripts, CI files and rule-enforcing tests can ride the same `shadcn add` as components. Scope line = **installed → `commons`, read → `platform/`, enforced-at-generation → `rulebook`** | `commons/registry/<layer>/registry.json` composed via `include` in the root manifest | `todo` (2 items) | **REGISTRY** | Roadmap + gates: `commons/docs/plans/2026-07-29-commons-install-surface.md` (`status: active`, usage check-in **2026-08-26** whose 0-install branch stops further extraction) |
| **Data pagination control** | Controlled pagination UI (`page`/`pageCount`/`onPageChange` + `…` window, wraps on narrow) + optional rows-per-page selector (`pageSize`/`onPageSizeChange`, 2026-07-12 → now deps `select`); client- or server-side paging | `commons/registry/thiengthb/data-pagination.tsx` (`shadcn add data-pagination`) | `sakubun` (`/resources` grammar/vocab/groups, `/history` mine+community) | **REGISTRY (copy-in)** | REGISTRY (built 1×; canonical in commons from the start per user directive — reuse platform-wide) |
| **Viewport-fit page size** | Auto page-size hook: reserves the MEASURED footer + real row height so the list + pagination bar never overflow the viewport (no page scroll). Pure math `lib/fit-rows.ts` (unit-tested) + DOM hook `components/ui/use-fit-rows.ts`; card lists wrap it in `usePagedList`, tables inline it in `data-table` | `sakubun/components/ui/use-fit-rows.ts` | `sakubun` (every paginated surface) | copy-in (pairs with `data-pagination`) | PATTERN (built 2026-07-23; promote to the `data-pagination` registry entry if a 2nd app paginates) |
| **DataTable (generic CRUD table, TanStack)** | Config-driven table on `@tanstack/react-table`: multi-column sort (Shift-click to add + priority badges), draggable column widths, show/hide columns, global search, an app **filter slot** + **action slot**, DataPagination — whole view (sort/visible-cols/page/size/search) persisted to sessionStorage + mirrored to the URL (widths session-only). App supplies `columns` (accessor + `cell` renderers → any custom cell incl. edit/delete row-actions) and pre-filters `data`. ⚠️ Needs `@tanstack/react-table` + shadcn `table`/`button`/`dropdown-menu`/`input` + `data-pagination` | `commons/registry/thiengthb/data-table.tsx` (`shadcn add data-table`) | `sakubun` (`items-table` + `vocab-table` are now thin column-configs over it, 2026-07-18) | **REGISTRY (copy-in)** | REGISTRY (extracted from the 2× duplicated SRS-item tables — the prior "extract candidate", now realized) |
| **App sidebar (config-driven nav)** | Navigation sidebar on the shadcn `sidebar` primitive — collapses to an icon rail (⌘/Ctrl+B) + mobile Sheet + cookie-persisted state + per-item tooltip when collapsed; takes a `brand` + grouped `items` (lucide icons) + `footer` config so every app wires the SAME component. ⚠️ Next-only (`next/link` + `next/navigation`) | `commons/registry/thiengthb/app-sidebar.tsx` (`shadcn add app-sidebar`) | `sakubun` (replaced the top navbar, 2026-07-12) | **REGISTRY (copy-in)** | REGISTRY (built 1×; canonical in commons from the start per user directive — reuse platform-wide) |
| **Page header + field (minimal-UI, ⓘ tooltip)** | `page-header` (eyebrow/h1/description-as-hover-tooltip/action/back) + `field` (label+control+ⓘ) + `info-tooltip` (hover/focus Radix tooltip, wrap+`line-clamp`) + `truncate`. `info-tooltip` = hover sibling of the click-Popover `info-hint`; `page-header`/`field` point at it | `commons/registry/thiengthb/{page-header,field,info-tooltip,truncate}.tsx` (`shadcn add page-header`) | `sakubun` (all pages, 2026-07-10) | **REGISTRY (copy-in)** | REGISTRY (info-tooltip added 2026-07-10; needs the shadcn `tooltip` primitive, not `popover`) |
| **Empty-state box** | The one "nothing here yet" placeholder: dashed border + icon/title/description/action, **or** free-form `children` for rich copy (inline `<code>`, an embedded dialog). `className` tunes padding and turns it into a stack (`flex flex-col items-center gap-4`). Only dep = `cn` + `lucide-react` | `commons/registry/thiengthb/empty-state.tsx` | `todo` (8 sites) · `journal` · `sakubun` (7 sites) | **`commons` copy-in** (`npx shadcn add empty-state`) | **SHARED.** 2026-07-19: sakubun had forked a `children`-only twin without noticing the registry entry → canonical gained an **additive** `children` slot (`title` now optional) and sakubun adopted it. Existing copies render identically, no re-sync needed. Default padding is `px-6 py-16` — a site relying on the old `p-8` must say so explicitly |
| **Server-action result → toast** | `toastResult(result, success)` — success toast (string or built from the payload) on ok, the action's own `error` otherwise; a **type predicate** so the ok-branch narrows (`if (toastResult(r,'Đã lưu')) onDone()`). Guarantees no action failure is silently swallowed | `sakubun/lib/action-toast.ts` | `sakubun` (8 call sites 2026-07-19: settings save, skip sentence, row suspend/delete, bulk delete, item edit, 2× import) | PATTERN (copy-in; ~20 lines, coupled to each app's own `ActionResult<T>` shape) | **PATTERN** (worth copying to any app with `ActionResult`-style Server Actions — todo/journal; don't package, the type is app-local) |
| **Breadcrumbs (config-driven trail)** | Generic renderer taking an ordered `Crumb[]` (each = label + href + lucide icon + description-as-tooltip); intermediate crumbs link, the last marks the current page (aria-current). App owns the route→crumb map (`sakubun/components/app-breadcrumbs.tsx`: server pages pass a string `section`, so icon components never cross the RSC boundary; detail pages get the parent chain e.g. Tài nguyên → Ngữ pháp → item). ⚠️ Next-only (`next/link`) + needs `TooltipProvider` + the `tooltip` primitive | `commons/registry/thiengthb/breadcrumbs.tsx` (`shadcn add breadcrumbs`) | `sakubun` (all pages, 2026-07-18) | **REGISTRY (copy-in)** | REGISTRY (built 1×; canonical in commons from the start per user directive — reuse platform-wide) |
| **Date / Time picker** | `date-picker` (Popover + shadcn `Calendar`, value = local `"YYYY-MM-DD"` string, optional `clearable`) + `time-picker` (typed `Input` + Popover of `step`′ presets, value = local `"HH:MM"` string, manual typing still valid). Both `w-full`, self-contained (inlined date/time helpers, no util dep); replace raw `<input type="date"/"time">` | `commons/registry/thiengthb/{date-picker,time-picker}.tsx` (`shadcn add date-picker` / `time-picker`; deps: `date-fns`+`react-day-picker`+`calendar`/`popover` for date, `popover`+`scroll-area` for time) | `todo` (per its §12 UI convention), `sakubun` (settings "Giờ nhắc học" → TimePicker, 2026-07-17; date-picker installed but not yet used) | **REGISTRY (copy-in)** | REGISTRY (canonical in commons from the start per user directive — reuse platform-wide) |
| **Semantic-tone tokens + score/progress ring** | OKLCH `--ok/--warn/--alert` tokens via `@theme inline` + a reusable SVG ring primitive; color-codes state (a11y carried by emoji+label, not color alone); CSS-only animation (no `motion` dep) | `todo` (UI-renovation 2026-06-14: `progress-ring`/`progress-bar` + tokens) | `todo`, `yakudoku/web` (`ScoreRing` + `--ok/--warn`, slice F 2026-06-20), `sakubun` (tokens only, 2026-07-06) | copy-in (→ `commons` at the 3rd app) | **DUPLICATED — 3× reached (tokens)** — extract the token block to `commons` next time any of the three touches theming (ring still 2×) |
| **Optimistic like/upvote button (Heart + count)** | A `useOptimistic` toggle: instant fill + count bump, re-syncs from props after revalidate; server action returns `{liked,count}`; stops click propagation. Same shape twice inside sakubun — the sentence heart and the item heart differ only in which action they call | `sakubun/components/community-feed.tsx` (inline `LikeButton`, sentence), `sakubun/components/library-like-button.tsx` (item) | `sakubun` (2× — /history feed + /library, 2026-07-23) | copy-in (→ a generic `<LikeButton onToggle=…>` at the 3rd use) | **DUPLICATED — 2×** — extract a generic `LikeButton` (props: count/liked + async `onToggle`) at the 3rd occurrence; the two current copies are ~40 lines each, action-only difference |
| **MCP tool guard/ok/mcpError wrapper** | The per-tool wrapper (duration log, ZodError → readable soft error, no raw -32603) around `createMcpHandler` registrations | `yakudoku/web/lib/mcp/server.ts` | `todo`, `yakudoku/web`, `sakubun` (2026-07-06, copied verbatim minus auth) | copy-in (tiny, stable) | **DUPLICATED — 3×** but ~30 lines; extraction cost > drift risk, keep copy-in |
| **`deploy.yml` (GitHub Actions)** | Build → push `ghcr.io/thiengthb/<repo>` (`:latest` + `:sha`) | "copy from a living app" — `todo/.github/workflows/deploy.yml`, `yakudoku/.github/workflows/deploy.yml` | every deployed repo | **TEMPLATE** | TEMPLATE |
| **Dockerfile (node multi-stage / python)** | Standard build with `EXPOSE` + `HEALTHCHECK`, base `node:22` | living apps | every repo | TEMPLATE (see `/docker-expert`, `/app-onboard`) | TEMPLATE |
| **commit-msg + pre-commit hooks** | Conventional Commits enforcement + docs reminder | `.claude/skills/coding-convention/hooks/` | every repo | **TEMPLATE** (installed at repo init) | TEMPLATE |
| **Prettier config** | Shared formatting (`semi`, singleQuote, printWidth 100…) | `.claude/skills/coding-convention/templates/` | every repo | **TEMPLATE** | TEMPLATE |
| **Token / size counter (`tokens.sh`)** | "How many chars + Claude tokens is this file / folder / project?" — wraps `npx repomix --token-count-tree` (per-file + per-dir tree, respects `.gitignore`) and **multiplies by a calibration factor**, because repomix counts with OpenAI's `o200k_base`, which undercuts Claude. Measured on this repo against Claude Code's own `/context`: `CLAUDE.md` 5,845→9,100 (×1.56), `MEMORY.md` 1,543→2,300 (×1.49) ⇒ default `CALIBRATION=1.5` (VN prose + md tables, worst case; use ~1.2 for English-only code). Reports % of the context window. Exact counting needs `POST /v1/messages/count_tokens` + an API key — this is the free local estimate, biased high not low. **`/context` still wins for the per-session tax** (real tokenizer); this is for files NOT yet loaded | `.claude/scripts/tokens.sh` | agent-side, every repo (sizing a read, spotting a context hog before loading it) | **TEMPLATE** (run in place; `npx`, nothing to install) | TEMPLATE (built 2026-07-26) |
| **Discord bot bootstrap** | Gateway connect + command/handler scaffold + allowlist | node: `jobhunter-bot`, `yakudoku-bot`; python: `nuc-monitor`, `nuc-ops-bot` | 4 workers (2 node + 2 python) | PATTERN per language | **DUPLICATED — watch** (different langs; extract only within a language if 3× same-shape) |
| **Consumer-driven contract test (cross-repo HTTP seam)** | Zod-contract + consumer fixture test + provider real-response verify — guards a repo↔repo API from silent drift (fixtures-first, no Pact broker) | `.claude/skills/testing-standard/templates/contract-test.example.ts` | (seams: todo↔core, web↔core, MCP) | **TEMPLATE** (copy-in; broker deferred to ≥2 teams) — standard `standards/testing.md §4` | TEMPLATE |

---

## B. How to use this file

- **Building a feature?** Scan column 1 here first. If it's listed → reuse via its mechanism (don't grep, don't rebuild).
- **Found a 2nd copy of something?** Add a row (or flip a row to DUPLICATED) — same change as the code.
- **Extracting?** Follow `/code-reuse` → `references/extraction-playbook.md`; then flip the row to SHARED/REGISTRY with the
  canonical location + consuming repos.
- **Auditing?** `/code-reuse` audit mode reconciles this table against a grep sweep of the projects; `/host-audit`
  treats a drifted catalog as a finding.
