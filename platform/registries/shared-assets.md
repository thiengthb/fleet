# 08 — Shared assets catalog (reusable building blocks)

> **The cheap index of "what reusable thing already exists, where its canonical copy lives, and how to reuse it."** Read
> this BEFORE building a feature so you reuse instead of reinventing (saving tokens + dev time). Owned + kept in sync by
> the skill **`/code-reuse`** — every reuse / extraction / newly-noticed duplication MUST update this file in the same
> change (anti-drift, same discipline as `inventory.md`).
>
> Established 2026-06-13. The platform is **independent repos** — reuse is never free; see `/code-reuse` for the
> rule-of-three gate and the hybrid share model, and `references/extraction-playbook.md` for how to extract.

---

> **Renamed 2026-07-28: `ui-kit` → `commons`** (`thiengthb/commons`). The old name said "components"; the thing is
> the reusable essence of every project — components, scripts, tests, helpers. It stays a **separate git repo**, not a
> folder under `platform/`, because it works precisely by being independently versioned and installable
> (`npx shadcn add …`); folding it in would break the one mechanism that makes it useful. Rules and patterns do NOT
> move into it — they are read, not installed, and they live in `platform/`. Entries in `ledger/` and the ledger index
> still say `ui-kit`: that is what the folder was called on the day, and the record is not rewritten.

## Maturity / status legend

- **PATTERN** — a recurring shape worth copying by hand; no single canonical artifact yet (or not worth extracting).
- **TEMPLATE** — a canonical scaffold to copy from (config/CI/Dockerfile/hooks).
- **REGISTRY** — copy-in shared code with a canonical home (the `commons` model).
- **DUPLICATED — extract candidate** — built ≥2×, near-identical; flagged for extraction once it hits 3× stable.
- **SHARED (package)** — extracted to a published `@thiengthb/*` package; consumed at CI build time.

> Reminder (rule of three): 1× = build local · 2× = log here as DUPLICATED · 3× same-shape + stable = extract.

---

## A. Catalog

| Asset | What it is | Canonical / where | Reused in | Mechanism | Status |
|------|-----------|-----------|-----------|-----------|--------|
| **MCP self-issued OAuth shim (glue)** | OAuth authorize/token/register + issuer/validation for the MCP endpoint — *glue only*, NOT the tool definitions | `projects/todo/lib/mcp/{auth,oauth}.ts` + `projects/todo/app/api/oauth/*` | `todo`, `projects/yakudoku/web` (near-identical: auth 38≈39, oauth 86≈89, token 63≈67, authorize 124≈129, register 32=32) | → **published package** when extracted (heavy + security-sensitive) | **DUPLICATED — still 2×, and the expected 3rd never arrived.** `idea-0023`'s MCP server was to be the 3rd consumer, which is why `idea-0013` was armed as its prerequisite. **Phase 3 (2026-07-29) cancelled hosting**, so that server runs on localhost with no auth at all and needs none of this glue. The count stays 2 and `idea-0013` returns to ARMED-not-fired — a *planned* consumer is not a consumer |
| **MCP `server.ts` (tool definitions)** | The app-specific MCP tools | per app | — | **stays local** (this is the *feature*, not glue) | PATTERN (do NOT share) |
| **Forward-auth header reader** | Read `X-authentik-email` / `X-authentik-groups` to identify/authorize the user | `todo`, `journal`, `projects/yakudoku/web` (`lib/api.ts`) | 3 apps | copy-in or tiny package | **DUPLICATED — extract candidate** (3 apps; verify same shape before extracting) |
| **Public-router split** | Splitting health/MCP/OAuth/webhook endpoints into their own Traefik router, **exempt from forward-auth** (invariant #8) | `todo`, `journal`, `yakudoku` | 3 apps | PATTERN (compose labels) — see `coding-convention §9`, `projects/authentik/docs/auth-apps.md` | PATTERN (well-documented; no artifact to extract) |
| **Prisma client singleton** | `lib/db.ts` global-cached PrismaClient (avoids hot-reload connection storms) | `commons/registry/lib/db.ts` (`shadcn add lib-db`) | `todo` (verified byte-identical 2026-07-29), `journal` (1 comment apart — adopt on next touch) | **REGISTRY (copy-in)** | **REGISTRY** (2026-07-29, the first non-UI item). ⚠️ **Never blind-install into `sakubun`** — its `lib/db.ts` is 92 lines carrying the fail-closed tenancy guard (its invariant #5), not the 8-line singleton |
| **Rebuild-and-verify (local container)** | Rebuild → **bounded** health wait → route sweep, so the exit code reflects the ROUTES not the build; `--gates` runs lint/test/format first, `--verify-only` touches docker not at all (for an already-serving/public app). Per-repo values live in `scripts/verify.env`, so the script stays byte-identical everywhere | `commons/registry/script/rebuild-and-verify.sh` (`shadcn add script-rebuild-and-verify`) | `todo` (2026-07-29); the richer sakubun original (`projects/sakubun/scripts/rebuild-and-verify.sh`, +MCP/dynamic-route checks) stays local | **REGISTRY (copy-in, UNIVERSAL item** — installs into a repo with no `components.json`, framework-agnostic) | **REGISTRY** (2026-07-29; generalized from sakubun's, which was itself born of running the sequence ~14× by hand and getting it wrong twice) |
| **shadcn UI components** | Visual component stock | `commons` (`thiengthb/commons`) | every web-app | **REGISTRY (copy-in)** — already established | REGISTRY |
| **The registry itself, widened** | `commons` is no longer UI-only: a **universal item** (type `registry:item`, every file carrying an explicit `~/`-rooted `target`) installs ANY file into ANY repo — proven 2026-07-29 into a directory with no `components.json`/`package.json`. So configs, ops scripts, CI files and rule-enforcing tests can ride the same `shadcn add` as components. Scope line = **installed → `commons`, read → `platform/`, enforced-at-generation → `rulebook`** | `commons/registry/<layer>/registry.json` composed via `include` in the root manifest | `todo` (2 items) | **REGISTRY** | Roadmap + gates: `commons/docs/plans/2026-07-29-commons-install-surface.md` (`status: active`, usage check-in **2026-08-26** whose 0-install branch stops further extraction) |
| **Data pagination control** | Controlled pagination UI (`page`/`pageCount`/`onPageChange` + `…` window, wraps on narrow) + optional rows-per-page selector (`pageSize`/`onPageSizeChange`, 2026-07-12 → now deps `select`); client- or server-side paging | `commons/registry/thiengthb/data-pagination.tsx` (`shadcn add data-pagination`) | `sakubun` (`/resources` grammar/vocab/groups, `/history` mine+community) | **REGISTRY (copy-in)** | REGISTRY (built 1×; canonical in commons from the start per user directive — reuse platform-wide) |
| **Viewport-fit page size** | Auto page-size hook: reserves the MEASURED footer + real row height so the list + pagination bar never overflow the viewport (no page scroll). Pure math `lib/fit-rows.ts` (unit-tested) + DOM hook `components/ui/use-fit-rows.ts`; card lists wrap it in `usePagedList`, tables inline it in `data-table` | `projects/sakubun/components/ui/use-fit-rows.ts` | `sakubun` (every paginated surface) | copy-in (pairs with `data-pagination`) | PATTERN (built 2026-07-23; promote to the `data-pagination` registry entry if a 2nd app paginates) |
| **DataTable (generic CRUD table, TanStack)** | Config-driven table on `@tanstack/react-table`: multi-column sort (Shift-click to add + priority badges), draggable column widths, show/hide columns, global search, an app **filter slot** + **action slot**, DataPagination — whole view (sort/visible-cols/page/size/search) persisted to sessionStorage + mirrored to the URL (widths session-only). App supplies `columns` (accessor + `cell` renderers → any custom cell incl. edit/delete row-actions) and pre-filters `data`. ⚠️ Needs `@tanstack/react-table` + shadcn `table`/`button`/`dropdown-menu`/`input` + `data-pagination` | `commons/registry/thiengthb/data-table.tsx` (`shadcn add data-table`) | `sakubun` (`items-table` + `vocab-table` are now thin column-configs over it, 2026-07-18) | **REGISTRY (copy-in)** | REGISTRY (extracted from the 2× duplicated SRS-item tables — the prior "extract candidate", now realized) |
| **App sidebar (config-driven nav)** | Navigation sidebar on the shadcn `sidebar` primitive — collapses to an icon rail (⌘/Ctrl+B) + mobile Sheet + cookie-persisted state + per-item tooltip when collapsed; takes a `brand` + grouped `items` (lucide icons) + `footer` config so every app wires the SAME component. ⚠️ Next-only (`next/link` + `next/navigation`) | `commons/registry/thiengthb/app-sidebar.tsx` (`shadcn add app-sidebar`) | `sakubun` (replaced the top navbar, 2026-07-12) | **REGISTRY (copy-in)** | REGISTRY (built 1×; canonical in commons from the start per user directive — reuse platform-wide) |
| **Page header + field (minimal-UI, ⓘ tooltip)** | `page-header` (eyebrow/h1/description-as-hover-tooltip/action/back) + `field` (label+control+ⓘ) + `info-tooltip` (hover/focus Radix tooltip, wrap+`line-clamp`) + `truncate`. `info-tooltip` = hover sibling of the click-Popover `info-hint`; `page-header`/`field` point at it | `commons/registry/thiengthb/{page-header,field,info-tooltip,truncate}.tsx` (`shadcn add page-header`) | `sakubun` (all pages, 2026-07-10) | **REGISTRY (copy-in)** | REGISTRY (info-tooltip added 2026-07-10; needs the shadcn `tooltip` primitive, not `popover`) |
| **Empty-state box** | The one "nothing here yet" placeholder: dashed border + icon/title/description/action, **or** free-form `children` for rich copy (inline `<code>`, an embedded dialog). `className` tunes padding and turns it into a stack (`flex flex-col items-center gap-4`). Only dep = `cn` + `lucide-react` | `commons/registry/thiengthb/empty-state.tsx` | `todo` (8 sites) · `journal` · `sakubun` (7 sites) | **`commons` copy-in** (`npx shadcn add empty-state`) | **SHARED.** 2026-07-19: sakubun had forked a `children`-only twin without noticing the registry entry → canonical gained an **additive** `children` slot (`title` now optional) and sakubun adopted it. Existing copies render identically, no re-sync needed. Default padding is `px-6 py-16` — a site relying on the old `p-8` must say so explicitly |
| **Server-action result → toast** | `toastResult(result, success)` — success toast (string or built from the payload) on ok, the action's own `error` otherwise; a **type predicate** so the ok-branch narrows (`if (toastResult(r,'Đã lưu')) onDone()`). Guarantees no action failure is silently swallowed | `projects/sakubun/lib/action-toast.ts` | `sakubun` (8 call sites 2026-07-19: settings save, skip sentence, row suspend/delete, bulk delete, item edit, 2× import) | PATTERN (copy-in; ~20 lines, coupled to each app's own `ActionResult<T>` shape) | **PATTERN** (worth copying to any app with `ActionResult`-style Server Actions — todo/journal; don't package, the type is app-local) |
| **Breadcrumbs (config-driven trail)** | Generic renderer taking an ordered `Crumb[]` (each = label + href + lucide icon + description-as-tooltip); intermediate crumbs link, the last marks the current page (aria-current). App owns the route→crumb map (`projects/sakubun/components/shell/app-breadcrumbs.tsx`: server pages pass a string `section`, so icon components never cross the RSC boundary; detail pages get the parent chain e.g. Tài nguyên → Ngữ pháp → item). ⚠️ Next-only (`next/link`) + needs `TooltipProvider` + the `tooltip` primitive | `commons/registry/thiengthb/breadcrumbs.tsx` (`shadcn add breadcrumbs`) | `sakubun` (all pages, 2026-07-18) | **REGISTRY (copy-in)** | REGISTRY (built 1×; canonical in commons from the start per user directive — reuse platform-wide) |
| **Date / Time picker** | `date-picker` (Popover + shadcn `Calendar`, value = local `"YYYY-MM-DD"` string, optional `clearable`) + `time-picker` (typed `Input` + Popover of `step`′ presets, value = local `"HH:MM"` string, manual typing still valid). Both `w-full`, self-contained (inlined date/time helpers, no util dep); replace raw `<input type="date"/"time">` | `commons/registry/thiengthb/{date-picker,time-picker}.tsx` (`shadcn add date-picker` / `time-picker`; deps: `date-fns`+`react-day-picker`+`calendar`/`popover` for date, `popover`+`scroll-area` for time) | `todo` (per its §12 UI convention), `sakubun` (settings "Giờ nhắc học" → TimePicker, 2026-07-17; date-picker installed but not yet used) | **REGISTRY (copy-in)** | REGISTRY (canonical in commons from the start per user directive — reuse platform-wide) |
| **Semantic-tone tokens + score/progress ring** | OKLCH `--ok/--warn/--alert` tokens via `@theme inline` + a reusable SVG ring primitive; color-codes state (a11y carried by emoji+label, not color alone); CSS-only animation (no `motion` dep) | `todo` (UI-renovation 2026-06-14: `progress-ring`/`progress-bar` + tokens) | `todo`, `projects/yakudoku/web` (`ScoreRing` + `--ok/--warn`, slice F 2026-06-20), `sakubun` (tokens only, 2026-07-06) | copy-in (→ `commons` at the 3rd app) | **DUPLICATED — 3× reached (tokens)** — extract the token block to `commons` next time any of the three touches theming (ring still 2×) |
| **Optimistic like/upvote button (Heart + count)** | A `useOptimistic` toggle: instant fill + count bump, re-syncs from props after revalidate; server action returns `{liked,count}`; stops click propagation. Same shape twice inside sakubun — the sentence heart and the item heart differ only in which action they call | `projects/sakubun/components/library/community-feed.tsx` (inline `LikeButton`, sentence), `projects/sakubun/components/library/library-like-button.tsx` (item) | `sakubun` (2× — /history feed + /library, 2026-07-23) | copy-in (→ a generic `<LikeButton onToggle=…>` at the 3rd use) | **DUPLICATED — 2×** — extract a generic `LikeButton` (props: count/liked + async `onToggle`) at the 3rd occurrence; the two current copies are ~40 lines each, action-only difference |
| **MCP tool guard/ok/mcpError wrapper** | The per-tool wrapper (duration log, ZodError → readable soft error, no raw -32603) around `createMcpHandler` registrations | `projects/yakudoku/web/lib/mcp/server.ts` | `todo`, `projects/yakudoku/web`, `sakubun` (2026-07-06, copied verbatim minus auth) | copy-in (tiny, stable) | **DUPLICATED — 3×** but ~30 lines; extraction cost > drift risk, keep copy-in |
| **Web-app config set (vitest · eslint · prettier)** | `vitest.config.ts`+`vitest.setup.ts` (node env by default, jsdom per file, `@/*` alias, jest-dom), the flat Next eslint config, and the formatting contract | `commons/registry/config/*` (`shadcn add config-vitest` / `config-eslint` / `config-prettier`) | vitest+eslint were **identical in `todo`, `journal`, `projects/yakudoku/web`**; prettier identical in all 4 | **REGISTRY (copy-in, UNIVERSAL items)** | **REGISTRY** (2026-07-29). The prettier duplication is **closed** (2026-07-30): the two byte-identical `.claude/skills/coding-convention/templates/` files were removed and the skill now points at `shadcn add @thiengthb/config-prettier`. The skill keeps the settings *table* — that is the rule; the registry is the installer. `config-editorconfig` is a separate row and must stay in agreement with this one |
| **PageShell (the page frame)** | Vertical rhythm + body-size baseline + width tier (`full`/`wide`/`narrow`/`form`) + a header row carrying breadcrumb, tabs and page actions. Every app-specific piece is a SLOT and there is no `next/*` import, so the file is identical in every repo | `commons/registry/block/page-shell.tsx` (`shadcn add page-shell`) | generalized from `projects/sakubun/components/shell/page-shell.tsx`; **not yet adopted elsewhere** (todo keeps its own `py-8` frame per its §12 — adoption is a UI migration, not a copy) | **REGISTRY (copy-in)** | **REGISTRY** (2026-07-29). Extracted at 1× under the written-standard exemption: it implements `platform/standards/ui-layout.md`, which is MANDATORY for every app |
| **ThemeToggle** | Light/dark control in the shapes the apps use: ghost icon button, labelled Switch row, or both (the shadcn sidebar's collapsed state picks). Icon = CURRENT theme, aria-label = the ACTION; copy is a prop | `commons/registry/block/theme-toggle.tsx` (`shadcn add theme-toggle`) | `todo` (adopted 2026-07-29, behaviour-preserving); `journal`, `projects/yakudoku/web`, `sakubun` still on their own copies | **REGISTRY (copy-in)** | **REGISTRY** (2026-07-29 — replaced **4 copies in 3 different shapes**, one of which had its icon contradicting its own aria-label) |
| **Gate: no emoji** | The platform no-emoji rule as a failing vitest that names every `file:line`; roots + deliberate exceptions live in the consumer's `docs/gates.json`, so the test never forks | `commons/registry/test/no-emoji.test.ts` (`shadcn add test-no-emoji`) | `todo` (2026-07-29, green on a 30-site burn-down list, red on anything new); `sakubun` keeps its own richer original | **REGISTRY (copy-in, UNIVERSAL item)** | **REGISTRY**. ⚠️ Installing a gate into a living app is a **MIGRATION** — measured drift at install time: `todo` 6 files, `journal` 1, `projects/yakudoku/web` 7. The other three sakubun gates (`layout-standard`, `type-scale`, `ui-pattern-lock`) are **deliberately NOT extracted**: they assume PageShell + a `ui-patterns.json` registry that only sakubun has, so they would ship un-passable |
| **Starter: whole web-app spine** | One command lands 15 files: multi-stage Dockerfile (EXPOSE + HEALTHCHECK, Prisma-free base with the three Prisma edits documented in-file), local `docker-compose.yml` with a named volume, the ghcr build workflow gated on tests, an open `/api/health`, `next.config.ts` with `output: 'standalone'`, `scripts/verify.conf`, and the doc set (`00-map` · `decisions` · `gates.json`). Pulls in config-vitest/eslint/prettier + rebuild-and-verify + the no-emoji gate | `commons/registry/starter/*` (`shadcn add @thiengthb/starter-web-app --overwrite`) | **measured 2026-07-29 on a throwaway app: empty dir → healthy container + HTTP 200 in under 3 minutes** (init 37s, add ~1s, build 5s, container 44s). No production consumer yet — the next new repo is the first | **REGISTRY (copy-in, UNIVERSAL item)** | **REGISTRY**. Three constraints found by running it: `--overwrite` is correct on a fresh scaffold and **wrong on a living app** (it refetches shadcn primitives and undoes local formatting); a multi-item bundle needs **Approach B** (a declared registry URL — a local JSON path cannot resolve `@thiengthb/*` deps, and `file://` is unimplemented); and a registry item **cannot merge npm scripts into `package.json`**, so shipped commands use `npx` |
| **External component registries (reference, never vendor)** | The shadcn CLI resolves several community registries with **no `components.json` config at all** — measured 2026-07-30 with `shadcn search`: `@shadcn` 471 items, `@reui` 1596 (MIT), `@animate-ui` 580, `@aceternity` 270, `@magicui` 247 (MIT), `@ai-elements` 136, `@basecn` 56, `@prompt-kit` 23. So an external component is one command away *on demand* and keeps its author's provenance | not a file — a **mechanism** + the verdicts in `commons/docs/external-patterns.md` | none yet (by design) | **REFERENCE on demand** (`npx shadcn add @reui/x`) — explicitly **NOT** copy-in to `commons` | **POLICY** (2026-07-30). A web pattern has **zero** uses in the fleet, so vendoring it inverts the rule of three and would corrupt the 2026-08-26 usage check-in. Four pre-install gates (`external-patterns.md §2`): read `dependencies` first, licence must be named, never `--overwrite` a living app, record the row here. ⚠️ Of the eight, **one** is a real candidate and only per-item — `@reui/data-grid` pulls `@base-ui/react`, a second primitive library beside Radix. The three biggest are animation/marketing stock, which the platform's minimal-UI rule would require arguing back out |
| **Gate: CI hardening** | Four GitHub Actions rules as a failing vitest naming every `file:line`: top-level `permissions`, third-party actions pinned to a full 40-hex commit SHA (the `tj-actions/changed-files` CVE-2025-30066 tag-repointing class), no `pull_request_target`, and no attacker-controlled context interpolated into a `run:` block. `trustedOwners` + `strict` live in `docs/gates.json` | `commons/registry/test/ci-hardening.test.ts` (`shadcn add @thiengthb/test-ci-hardening`) | none yet — **the finding is open on all 8 fleet workflows** | **REGISTRY (copy-in, UNIVERSAL item)** | **REGISTRY** (2026-07-30, imported from published guidance not self-critique). Measured before it existed: **0 of 8** workflows declared top-level `permissions`, **0 of 8** pinned any action by SHA across 44 `uses:` sites. Its first finding was in `starter-web-app`'s own `deploy.yml` — the `test` job ran `npm ci` (arbitrary postinstall) with the repo-default token scope. ⚠️ Fixing the 8 repos is a **human** commit: `.github/workflows/**` is governance the agent may not edit |
| **Validated environment (`lib/env.ts`)** | Fail-fast zod env validation: `import { env } from '@/lib/env'` and a missing/malformed variable stops the process **at startup** listing every problem, instead of surfacing later as an `undefined` in a URL or a 500 on one route. Server-only key read from client code throws; a `NEXT_PUBLIC_` variable declared without its literal `clientRuntime` entry fails to **compile** (Next inlines those textually, so a computed key is silently `undefined` in the browser); `SKIP_ENV_VALIDATION=1` keeps a `docker build` with no `.env` working | `commons/registry/lib/env.ts` (`shadcn add @thiengthb/lib-env`) | none yet — `todo` is the natural first (adopting it is a real migration: every `process.env.X` read becomes `env.X`, plus the build-stage escape in the Dockerfile) | **REGISTRY (copy-in)** | **REGISTRY** (2026-07-30). The t3-env pattern on **plain zod** — `@t3-oss/env-nextjs` was rejected as a dependency for ~90 lines, and is named in-file as the drop-in upgrade. Measured gap: **0 of 10** repos validated their environment, and every one depends on at least one required variable |
| **EditorConfig** | The half of the formatting contract Prettier does not cover — what the editor does as you type, in every file type including the ones Prettier never sees (`.env`, `Dockerfile`, `.sql`, `.conf`) | `commons/registry/config/editorconfig.txt` (`shadcn add @thiengthb/config-editorconfig`) | none yet — **0 of 10** repos had one | **REGISTRY (copy-in, UNIVERSAL item)** | **REGISTRY** (2026-07-30). ⚠️ Not the trivial file it looks like: **Prettier READS `.editorconfig`** for `endOfLine`/`tabWidth`/`useTabs`/`printWidth`, filling in whatever `.prettierrc` omits. Values that disagree make formatting depend on whether the editor or `prettier --write` touched the file last, so a plausible-but-different EditorConfig is worse than none. This one restates `config-prettier` exactly |
| **Container runtime hardening** | `cap_drop: ALL` + `security_opt: no-new-privileges` active in the starter compose, with `read_only: true` + tmpfs documented as the verified opt-in next step | `commons/registry/starter/docker-compose.yml` | ships with `starter-web-app` | **REGISTRY (copy-in)** | **REGISTRY** (2026-07-30, verified on a real image: healthy, `/` + `/api/health` 200, `CapEff: 0000000000000000`, and `SKIP_ENV_VALIDATION` absent from the runtime env — the build-stage scoping holds). `read_only` also serves 200 with tmpfs on `/tmp` + `/app/.next/cache`, but stays opt-in because that was measured on a **bare** app and the starter's next documented step is Prisma, whose native modules may write outside `/tmp` |
| **Theme provider (next-themes wrapper)** | The 6-line `<ThemeProvider>` wrapper around next-themes | `projects/journal/components/theme-provider.tsx`, `projects/yakudoku/web/components/theme-provider.tsx` (**1.00 similarity**) | 2 apps | copy-in | **DUPLICATED — 2x.** Found by `reuse-scan` 2026-07-29, not by anyone looking. Pairs with the `theme-toggle` registry item; extract both together at the 3rd app |
| **`instrumentation.ts` (in-process cron bootstrap)** | Next's instrumentation hook used to start an in-process node-cron scheduler on boot | `projects/todo/instrumentation.ts`, `projects/journal/instrumentation.ts` (0.86) | 2 apps | copy-in | **DUPLICATED — 2x** (found by `reuse-scan` 2026-07-29). Tiny, and the *scheduler* it starts is app-specific — share the bootstrap only, if ever |
| **Public site header** | The unauthenticated top bar (brand + theme toggle + sign-in) | `projects/journal/components/site-header.tsx`, `projects/yakudoku/web/components/site-header.tsx` (0.79) | 2 apps | copy-in | **DUPLICATED — 2x** (found by `reuse-scan` 2026-07-29). Below the 3x gate, and 0.79 means the shapes are already drifting — re-check they are one asset before extracting |
| **`reuse-scan` (the counter behind the rule of three)** | Finds the same ARTIFACT built in multiple PROJECTS and applies the ladder: 2 projects = CANDIDATE, 3+ = EXTRACT. k=5 k-grams + Jaccard >= 0.72, calibrated on this fleet's known pairs (clones score 1.000, non-clones 0.004-0.075). Classifies away the noise that made the first run useless: `UPSTREAM` (vendored shadcn primitives, and files `shadcn init`/`create-next-app` generate — extracting `cn()` would be the over-engineering it exists to prevent) and cross-references both the registry and this catalog | `.claude/scripts/reuse-scan.mjs` — `node .claude/scripts/reuse-scan.mjs [project] [--new\|--all\|--json\|--calibrate]` | agent-side, whole fleet (~1.9s over 715 files) | **TEMPLATE** (run in place) | **TEMPLATE** (2026-07-29). Complements `/code-reuse` Step 1, which until now needed a keyword the agent had to invent — which is why six rows here sat at "2x, and the 3rd never arrived". First run: **0 EXTRACT** (phase 2 covered everything at 3+), 8 CANDIDATE, and it independently rediscovered the MCP OAuth shim the catalog only recorded in prose. The per-write half is the proposed `reuse-guard` hook (`platform/proposals/2026-07-29-reuse-guard-hook.mjs.proposed`) — a human installs it |
| **`deploy.yml` (GitHub Actions)** | Build → push `ghcr.io/thiengthb/<repo>` (`:latest` + `:sha`) | "copy from a living app" — `projects/todo/.github/workflows/deploy.yml`, `projects/yakudoku/.github/workflows/deploy.yml` | every deployed repo | **TEMPLATE** | TEMPLATE |
| **Dockerfile (node multi-stage / python)** | Standard build with `EXPOSE` + `HEALTHCHECK`, base `node:22` | living apps | every repo | TEMPLATE (see `/docker-expert`, `/app-onboard`) | TEMPLATE |
| **commit-msg + pre-commit hooks** | Conventional Commits enforcement + docs reminder | `.claude/skills/coding-convention/hooks/` | every repo | **TEMPLATE** (installed at repo init) | TEMPLATE |
| **Prettier config** | Shared formatting (`semi`, singleQuote, printWidth 100…) | `commons/registry/config/` — **one installer** (`shadcn add @thiengthb/config-prettier`); the `/coding-convention` skill holds the settings table and points here | every repo | **REGISTRY (copy-in)** | REGISTRY (moved off TEMPLATE 2026-07-30 — see the config-set row above) |
| **Token / size counter (`tokens.sh`)** | "How many chars + Claude tokens is this file / folder / project?" — wraps `npx repomix --token-count-tree` (per-file + per-dir tree, respects `.gitignore`) and **multiplies by a calibration factor**, because repomix counts with OpenAI's `o200k_base`, which undercuts Claude. Measured on this repo against Claude Code's own `/context`: `CLAUDE.md` 5,845→9,100 (×1.56), `MEMORY.md` 1,543→2,300 (×1.49) ⇒ default `CALIBRATION=1.5` (VN prose + md tables, worst case; use ~1.2 for English-only code). Reports % of the context window. Exact counting needs `POST /v1/messages/count_tokens` + an API key — this is the free local estimate, biased high not low. **`/context` still wins for the per-session tax** (real tokenizer); this is for files NOT yet loaded | `.claude/scripts/tokens.sh` | agent-side, every repo (sizing a read, spotting a context hog before loading it) | **TEMPLATE** (run in place; `npx`, nothing to install) | TEMPLATE (built 2026-07-26) |
| **Discord bot bootstrap** | Gateway connect + command/handler scaffold + allowlist | node: `jobhunter-bot`, `yakudoku-bot`; python: `nuc-monitor`, `nuc-ops-bot` | 4 workers (2 node + 2 python) | PATTERN per language | **DUPLICATED — watch** (different langs; extract only within a language if 3× same-shape) |
| **Consumer-driven contract test (cross-repo HTTP seam)** | Zod-contract + consumer fixture test + provider real-response verify — guards a repo↔repo API from silent drift (fixtures-first, no Pact broker) | `.claude/skills/testing-standard/templates/contract-test.example.ts` | (seams: todo↔core, web↔core, MCP) | **TEMPLATE** (copy-in; broker deferred to ≥2 teams) — standard `standards/testing.md §4` | TEMPLATE |

---

## B. How to use this file

- **Building a feature?** Scan column 1 here first. If it's listed → reuse via its mechanism (don't grep, don't rebuild).
- **Found a 2nd copy of something?** Add a row (or flip a row to DUPLICATED) — same change as the code.
- **Extracting?** Follow `/code-reuse` → `references/extraction-playbook.md`; then flip the row to SHARED/REGISTRY with the
  canonical location + consuming repos.
- **Auditing?** `/code-reuse` audit mode reconciles this table against a grep sweep of the projects; `/host-audit`
  treats a drifted catalog as a finding.
