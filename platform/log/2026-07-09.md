<!-- Daily recall log — schema in log/README.md -->

## sakubun grammar-pivot shipped: vocab dropped, 507-pattern library, display contract v2 — 2026-07-09

```yaml
id: 2026-07-09-01
type: reflection
created: 2026-07-09T16:30:00+07:00
last_accessed: 2026-07-09
importance: 8
milestone_id: sakubun-grammar-pivot
related_ids:
  [
    { id: 2026-07-09-02, why: 'content-fill episodic of the same pass' },
    { id: 2026-07-09-03, why: 'display-contract v2 episodic of the same pass' },
    { id: 2026-07-06-01, why: 'previous milestone sakubun-v1 this builds on' },
  ]
embedding: null
```

**What happened** — 3-day pass (07-07 → 07-09) reshaping sakubun from vocab+grammar SRS into a
**grammar-driven translation trainer**:

- Vocab dropped by user decision (learn in Anki): FSRS schedules ONLY grammar patterns; sentence
  model inverted (grammar = TARGET, vocab = free scaffolding). One-time WORD teardown (51 items +
  logs) as the documented exception to append-only; `kind` column kept. Plan
  `sakubun/docs/plans/2026-07-07-drop-vocab-grammar-focus.md` (done). Commit f6e074f.
- Empty-batch legibility: `newBeyondLevels` count + protocol branch so "filtered by level" is never
  misread as "done" (real case: 38 N5 patterns vs levels=[N3,N4]). Commit 769d129.
- Store auto-filled to **507 patterns** (N5 153 · N4 102 · N3 135 · N2 117, 46 tagged `spoken`) +
  **356 enrichment cards** — all via the real MCP write path. levels now [N2..N5], dailyNewGrammar 20.
- Display contract v2 after live user feedback ("Claude chỉ nói tốt/không tốt, không đưa đáp án"):
  `suggested` REQUIRED in submit_review; 📋 block = đề · bạn dịch · bản dịch tham khảo · bảng chấm ·
  menu "Tiếp theo?" 1–5; `verbatimEnvelope` puts the display order INSIDE the tool result; new
  model-in-the-loop eval `sakubun/eval/display-contract-eval.md`. Commits 6118307, 8261651.

**Decisions made** — distilled to `sakubun/docs/decisions.md` (entries dated 2026-07-07 ×3,
2026-07-09 ×2, + content-seeding entry added at wrap) and 2 ledger lines (transcript recovery;
model-in-the-loop eval).

**Open threads** —

- User should practice a full session in a NEW Claude Desktop conversation to confirm the display
  contract holds in the real client (eval passed 5/5 + 1B, but Desktop is the ground truth).
- N5 backlog: 150 user-imported N5 patterns still have no enrichment cards (deliberately untouched).
- N1 grammar + more N3 tail patterns not yet added; user hasn't asked.
- Platform repo edits (this log, ledger, INVENTORY from 07-07) still uncommitted for user review.
- sakubun still has no GitHub remote; NUC deploy deferred (server down).

## content-fill: 356 patterns + 356 enrichment cards via subagent fan-out — 2026-07-09

```yaml
id: 2026-07-09-02
type: episodic
created: 2026-07-09T16:30:00+07:00
last_accessed: 2026-07-09
importance: 7
milestone_id: sakubun-grammar-pivot
related_ids: []
embedding: null
```

**What happened** —

- Reused yakudoku's human-reviewed N4/N3 catalog (27 entries verbatim) + Sonnet subagents drafted
  the rest (N4 66, N3 110, N2 ~120, spoken ~48); Opus reviewed; deduped by expression vs store;
  imported through `add_grammar` (never SQL) so dedup + FSRS init are exercised.
- Enrichment: 9 Sonnet subagents over chunk files → 356 cards (usage/collocations/example/pitfalls/
  contrasts/register; spoken cards name the full form they contract from + mandatory register).
- **Session-limit killed subagents mid-write TWICE** (resets 2:30am, 9:30am) — recovered 100% both
  times by parsing the agents' `.output` transcript JSONL (assistant text blocks survive the kill).
  Caveat learned: a SYNCHRONOUSLY-run agent's `.output` is EMPTY; only background/resumed agents
  persist a transcript — resume-from-transcript (SendMessage) is what materialized the files.

**Decisions made** — `sakubun/docs/decisions.md` "content was LLM-drafted, seeded via the real MCP
write path" (wrap entry); ledger line "subagent output is recoverable from transcript".

**Open threads** — none beyond the reflection's.

## display-contract v2: enforce at schema + in-result order + model eval — 2026-07-09

```yaml
id: 2026-07-09-03
type: episodic
created: 2026-07-09T16:30:00+07:00
last_accessed: 2026-07-09
importance: 8
milestone_id: sakubun-grammar-pivot
related_ids: []
embedding: null
```

**What happened** —

- User's live transcript showed Claude Desktop paraphrasing the 📋 block ("tốt lắm 👍"), dropping
  the reference translation, and hand-writing the menu. Escalation ladder applied: (1) `suggested`
  made REQUIRED in the tool schema (rejected live without it), (2) comparison + menu rendered
  server-side into the block, (3) `verbatimEnvelope` — imperative display order INSIDE the tool
  result, the text the client model reads right before replying.
- Built `eval/display-contract-eval.md` (model-in-the-loop: Sonnet subagents role-play Claude
  Desktop against production-format tool results, 7 pass criteria). First run: 3/3 showed the
  reference (bug fixed) but 2/3 leaked the ```json``` payload → envelope gained a json-ban →
  retrials clean. Eval caught a bug unit tests never could.
- Honest UX note to user: Claude Desktop has no native clickable menu — "Tiếp theo?" is text,
  answered with a digit 1–5.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-09 ×2 (schema+menu enforcement; in-result
display order + eval); ledger line "MCP display contracts need model-in-the-loop evals".

**Open threads** — if Desktop still misbehaves, tighten envelope wording first (eval doc's grading
rule), not the renderer.
