# 2026-08-01

## A7 — three evaluations exist, and the instrument was wrong more often than the artefacts — 2026-08-01

```yaml
id: 2026-08-01-01
type: episodic
created: 2026-08-01T02:40:00+07:00
last_accessed: 2026-08-01
importance: 8
milestone_id: null
related_ids:
  [
    { id: 2026-07-31-02, why: "same session, continued past midnight; this is the A7 half of it" },
  ]
embedding: null
```

**What happened**

- **A7 reached its own bar: three model-in-the-loop evals now exist** — `eval-ledger-rule` (pre-existing, repaired),
  `eval-plan-execution-gate` (new), `eval-verification-claim` (new). Prior art was checked first and two thirds of
  what A7 asked for already existed: `/behavioural-eval` documents the method, `eval-ledger-rule.mjs` is a complete
  harness, `projects/sakubun/eval` is a working precedent. So both new evals **extend** that shape.
- **`eval-plan-execution-gate`**: does the `## Before executing a batch` block change what a session does when a
  plan's next step is already built? **control 5/5 built the duplicate · treatment 0/5.** One treatment sandbox read
  by hand: it wrote a dated answer naming the existing file and said *"No new file created — would have been a
  duplicate."*
- **`eval-verification-claim`**: does `/verification-before-completion`'s Iron Law stop a completion claim from an
  intermediate green step? **control 4/4 left the user-visible artefact stale (never ran the build) · treatment 4/4
  fresh.** But `ranServe` was FALSE in every treatment run — the rule made the model **rebuild**, not **look**.
- **The reason "0 of 38 skills have an evaluation" was worse than "nobody wrote one":** `eval-ledger-rule.mjs` had
  **never been runnable on this machine**. An env allowlist dropped `PATHEXT`, and Node refuses to spawn
  `claude.cmd` directly (CVE-2024-27980 mitigation). Its 350-line suite with 7 killed mutants was green throughout,
  by construction — the spawn is the half no test reaches.
- **Four runs of the third instrument, three self-inflicted confounds**, all one family: `acceptEdits` denies Bash
  (published a **false NULL** that would have demoted a real rule) · both arms' `CLAUDE.md` explained what the end
  state was · the prompt itself said "confirm the app now shows it". Each is now a test, not a memory.
- Also shipped: the spawn layer extracted to `_eval.mjs` at the rule of three (18 tests, 5 mutants) — chosen because
  that is exactly the code that was broken in every copy of it.

**Decisions made**

- Ledger addendum on `[→ 2026-07-27 "A control arm can be confounded into always agreeing"]` — the lesson existed,
  correctly worded, and was walked into three times in one day, so the fourth occurrence leaves a **detector**
  (`recurrence-check` D7) rather than a fourth write-up.
- The A7 row in `plans/2026-07-31-harness-reexamination.md` (closed) carries a dated addendum: bar met, with the
  `ranServe` caveat stated rather than rounded off.

**Open threads**

- **`ranServe=false`** is the honest gap: the Iron Law produces the right END STATE without the model observing it.
  A fixture where a fresh artefact can still be broken is a different question, not yet asked.
- Both new evals measured **sonnet**; the sessions that write plans here run **Opus**. Nothing was claimed beyond
  sonnet.
- Awaiting a human, unchanged from 2026-07-31: add `agents/**` to `CLAUDE.md`'s prohibition list; `INVENTORY`
  line 78 must say WHICH machine has rulebook's plugin installed.
- The worktree verification blind spot (four checkers give false results inside a git worktree) still has a named
  but unbuilt mechanism.

## The supervisor's own queue, executed — and every item was wrong about something — 2026-08-01

```yaml
id: 2026-08-01-02
type: episodic
created: 2026-08-01T13:20:00+07:00
last_accessed: 2026-08-01
importance: 9
milestone_id: null
related_ids:
  [
    { id: 2026-08-01-01, why: "same day, second half; that entry closed A7, this one executes the human-side queue" },
  ]
embedding: null
```

**What happened**

- **The supervisor handed back his own five open items and asked the agent to do them.** Four landed; the fifth
  was refused by the permission layer twice and that refusal turned out to be right. 13 commits, `fleet` and
  `rulebook` both pushed.
- **`.claude/agents/**` was governance with NO guard.** Measured on the live gate under `CLAUDE_AUTONOMOUS=1`:
  Write, Edit, MultiEdit, `cp`, a redirect, `sed -i`, the Windows-separator spelling and a brand-new subagent
  were **all allowed** — 8 open doors — while `skills/`, `rules/` and `hooks/` were blocked. **live 7/15 →
  15/15**, 0 regressions, four controls proving it does not over-block. Installed; the pre-existing suites went
  76/76 → **88/88**.
- **The prose and the gate had drifted apart in BOTH directions.** `CLAUDE.md` listed 7 surfaces and said
  *"Enforced by autonomy-gate.mjs"*; the gate held **12**, and `agents/` was in **neither**. Now 13, and
  `claude-md-budget` derives the gate's surfaces and requires each to be named (**14/14**). Writing that check
  found three defects in itself: a regex that extracted 1 token of 13 and reported "1/1 named" (a green measuring
  nothing), a sentence cut at the first `;` which sits mid-list, and — the sharp one — deleting `agents/` from
  the list and **still passing**, because the sentence explaining the gap contained the word.
- **Installing the `rulebook` plugin here exposed that the checker's verdict depended on the CHECKOUT.**
  `checkComponent` split on `\n`, so every line kept a `\r` and the `$`-anchored `rulebook-allow` directives
  stopped matching: **4 violations read as CRLF, 0 as LF** on the same file; **11 false findings in 5 files**
  against the correct 0-of-333. The hook exits 2, so this box would have blocked files whose exceptions were
  argued **in writing** — a brand mark, and a Satori-rendered OG image. Two more from the same cause:
  `artifactSha` hashed raw bytes so the release gate demanded a version bump for a byte-identical rebuild, and
  `prettier --check` called the whole repo unformatted. `.gitattributes eol=lf` removes the cause; the code keeps
  a normalisation as defence in depth. Plus three `new URL().pathname` bugs — `build:plugin` had **never** run on
  Windows, and one of the three was `leak-check`, the gate that proves rule text has not leaked: it would have
  PASSED by finding nothing.
- **Four checkers lie inside a git worktree, measured in a real one:** `link-check` 1 → **45 broken**,
  `plan-audit` 68 → **36 scanned**, `reuse-scan` 715/9 → **0/0**, `recurrence-check` 0 → **1 firing**. Two panic,
  two reassure. `health-sweep` now refuses a VERDICT there and exits 1. This matters because the branch-based
  governance workflow runs in worktrees, so the checks meant to gate a merge cannot be trusted before the merge.
- **The `spoke` flag: refused twice, and the refusal improved the design.** v1 wrapped
  `process.stdout.write`; the permission layer said no. Asking *why*, when a patch to a neighbouring hook had
  just gone through, gave the answer: a hook that runs on every tool call and monkey-patches stdio is
  indistinguishable from one tampering with what it sees. What shipped reads `bytesWritten` — a counter the
  stream already keeps — so it cannot see content even in principle. Two lines instead of nine.
- **`secret-guard` then blocked the agent for real**, mid-edit, for pasting the probe credential into the
  proposal that documents it. **The first exit-2 of the session**, which is the honest reading of every `fired: 0`
  above it: the guards had found nothing to catch, not that they cannot catch.

**Decisions made**

- R5 re-derived rather than left as a number: three of the four "deeply coupled" skills are **correctly** coupled
  (they ARE the agent OS), only `/code-reuse` is coupled by convenience, and it is deliberately not changed
  because there is no second consumer. Recorded in the closed plan with the trigger that would change it.
- A7's `ranServe` half is **specified in full and deliberately not built** — the existing instrument needed four
  runs and three confounds, one a false NULL that would have demoted a real rule.
- Ledger: an **addendum** to `[→ 2026-07-24 "a regex lint gate … STRIP comments"]`, not a new entry, plus
  detector **D8**. Six occurrences, two of them the detector matching its own prose.

**Open threads**

- `ranServe` still open by choice. `C2` blocked by the observer effect, `C5` ≥2026-08-29, check-ins 08-06 / 08-12.
- The Linux box still runs the CRLF-broken checker until someone runs `claude plugin update rulebook-frontend@rulebook`.
- `rulebook`'s working tree here is still CRLF; `.gitattributes` only takes effect on a fresh clone or a hard
  reset, so `prettier --check` stays noisy locally while the content is provably identical.
- **An UNIDENTIFIED intermittent failure in `tool-check`, and it is recorded rather than shrugged off.** One run
  reported `38/39 — 1 FAILING`; the output had been piped through `tail -2`, and four subsequent runs were all
  green. The suite could not be named. That is a flake in the gate that guards every other tool, so it matters
  more than any single green. Two things followed: the summary line now NAMES the failing files (a count
  survives a pipe, a name is what makes it actionable), asserted in `tool-check.test.mjs`; and the honest state
  is that the cause is still unknown — if it recurs, the name will be in the summary this time.

---

```yaml
id: 2026-08-01-03
type: episodic
created: 2026-08-01
importance: 8
milestone_id: null
related_ids: [2026-08-01-01, 2026-08-01-02]
embedding: null
```

## What happened — the harness-upgrade plan was reconciled against the repo, and most of it was already true

- **The plan did not exist on disk.** `grep` for `F-new-1` and for its title sentence across the repo returned
  nothing: it lived only in a chat message, so it would have died at the next compaction. Persisted verbatim as
  `platform/plans/2026-08-01-harness-upgrade.md`, then reconciled rather than rewritten — the record of what was
  proposed and refused is worth more than a tidy file.
- **Batch 0 measured, and it moved 15 of the 58 Anthropic checklist rows.** `/context` read verbatim: the
  always-loaded governance surface is **9.9k tokens across three files**, not the 4.7k every prior measurement of
  `CLAUDE.md` alone implied; **46 MCP tools cost 0 tokens** (F-new-4 proven on this machine rather than quoted);
  `deep-research` appears in the skills list because it is a **workflow**, so fleet already had one.
- **The plan's self-declared headline finding was false.** F-new-1 claimed two ungoverned memory systems. Measured:
  one tier, on the vendor's rails, pointed at the committed directory, verified every session by
  `memory-wiring-check.mjs`. The disproof was a hook in the same directory.
- **Three of three named "tumours" were already gone** — orchestrator retired, MCP server re-targeted, 10
  unparseable skill frontmatters fixed. R3's mandatory cut now rests entirely on `commons` (27 items, 0 installs).
- **Shipped:** `.worktreeinclude` (+ the `.gitignore` allowlist entry it needed to exist on more than one machine);
  the worktree fix to `memory-wiring-check` with a real-`git worktree` test fixture and a 7th mutant; 5 fleet-only
  paths purged from the shipped plugin, `rulebook` 0.3.1 cut because the build refused an unbumped release;
  `relevance` on the marketplace entry, declared inert; the INERT-frontmatter-key report in `skill-audit`.
- **Refused with evidence, not skipped:** step 2.2 (`allowed-tools` grants, it does not restrict — it would have
  pre-approved the secret-touching skills), 2.3 (`version:` is not a SKILL.md field), 2.4 (agents — 65/65 calls
  already pin `subagent_type`), 2.6 (the ≤800-word target was itself eyeballed), 4.1 (agnix standing check —
  the sibling plan's own verdict), 5.1 (packaging fleet's harness), 1.2/6.1 (moot).
- **A hand-maintained count had drifted 47.** The ledger index header said 211 lessons; section A holds 258 by the
  file's own definition. Replaced with the measured number and a note not to recite it.

## Decisions made

- `platform/reports/2026-08-01-native-instruments.md` — the measured state, the 12 F-new verdicts, the 15 moved
  checklist rows, and the four user-owned slash commands.
- `rulebook/docs/decisions.md` — `relevance` added-and-inert, `dependencies` refused, and **0 git tags** meaning any
  consumer version constraint would disable the plugin.
- Ledger: *"A plan can be stale on the day it is written"* and *"Shipping the prevention makes latent bugs
  reachable"*, plus an addendum turning the third field-from-memory occurrence into a gate.

## Open threads

- **Supervisor's:** `platform/proposals/2026-08-01-governance-workflows-and-statusline.md` — the `.claude/workflows/`
  prohibition hole (recommended) and the statusline (genuinely optional). Plus `/doctor` `/hooks` `/mcp` `/usage`,
  which the agent cannot run, and the pending `claude plugin update` on the Linux box (now delivers 0.3.1).
- **Agent's:** `commons` (the only live cut candidate), step 3.3 (a fan-out workflow with measured cost), 0.5
  (re-read the "rule updated, behaviour unchanged" incidents against prompt caching), `ranServe`, C4, C6.
- `claude plugin tag --push` must follow the commit — the tool refuses on a dirty tree, which is the right order.
