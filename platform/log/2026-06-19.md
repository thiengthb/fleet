<!-- Recall tier (temporal). Schema: log/README.md. NEVER auto-loaded; durable lessons are distilled UP into the ledger. -->

## Closed-loop driver — Phases 1+2 wired (idea→plan→arm bridge + reflection) — 2026-06-19

```yaml
id: 2026-06-19-01
type: episodic
created: 2026-06-19T07:20:00+07:00
last_accessed: 2026-06-19
importance: 8
milestone_id: closed-loop-driver
related_ids: [{id: 2026-06-18-01, why: "Phase 0 + capstone plan this continues"}]
embedding: null
```

**What happened**
- Continued the capstone `plans/2026-06-18-closed-loop-driver.md` after a compact. Built + committed **Phase 1** and **Phase 2**, all in the scheduled wrapper `auto-pilot-scheduled.ps1` (interactive, supervisor-approved direct edits per #69):
  - **S1.1** (`fac1242`) — graduation: an accepted-but-ungraduated idea (`outcome: accept` ABOVE `## Done`) → one bounded batch writes a **draft** plan (`auto_pilot: false`), idempotent, parks for the enrol gate. `Test-HasUngraduatedAccept` + `Invoke-AutonomousClaude` helper.
  - **S1.2** (`33928e2`) — planning Q&A: gate-repo pull/push wrap so the graduation batch can ask via `ask-cli` and resume with the answer next cycle; step-0 RESUME + pending-ask guard (no duplicate asks/plans).
  - **S1.3** (`4368104`) — enrol gate: graduation marks the draft `enrol: pending`; a new enrol phase asks Discord (`enrol|not yet|reject`) and applies the SIGNED answer (enrol⇒`active`+`auto_pilot:true`). Chose `ask-cli` (no hook side-effect, no bot change) over `gate-cli`. Hook-level enforcement of arming PROPOSED for human commit (`proposals/2026-06-19-enrol-gate-hook-hardening.md`).
  - **Phase 2** (`7f94c8a`) — one end-of-cycle `reflect` batch (only when the cycle did work): `/session-wrap` distil + EXTERNALLY-grounded retro filing propose-only inbox ideas + `/idea sort` + one Discord digest with the next-action.
- Also committed `ab9f05a`: the overnight C3 autonomous sort (2026-06-19, no new gaps) — the loop is alive.

**Decisions made** — distilled up into `06-knowledge-ledger.md` §A (#70 the idea→plan→arm bridge design + ask-cli/gate-cli split; #71 wiring-verified ≠ runtime-verified + line-anchor regex over a markdown queue).

**Runtime verification (added end of session)** — ran a LIVE local graduation smoke test (synthetic accepted idea-9999, sonnet worker, throwaway branch). Result: graduation produced a correct draft plan (`status: draft` / `auto_pilot: false` / `enrol: pending` / `idea_ref`), with NO spurious planning question (scope was clear, so S1.2 correctly stayed silent); the enrol batch then minted `ASK-enrol-*` + PARKED, leaving the plan **un-armed** (no signed answer ⇒ no `auto_pilot: true`) — the core safety invariant held. **Bug caught + fixed (commit `1dd928c`):** `Test-HasUngraduatedAccept` matched the Rules-prose `## Done` substring (added in S1.1 docs) → scan region collapsed to the header → graduation would NEVER have fired in production. Anchored the divider to `^## Done`. The S1.1 dry-run "skipped correctly" had been a FALSE NEGATIVE; the unit-test missed it (synthetic line arrays lacked the prose). Test branches + synthetic idea + stray ask-state all cleaned. ⇒ **Phase 1 is now runtime-verified**, not just wired.

**S3.3 enrol round-trip — PROVEN LIVE THROUGH DISCORD (end of session)** — provisioning was already in place (gate-clone → private `nuc-agent-gates`; bot `nuc-ops-bot` Up+healthy, polls every 25s via GitHub API). Ran a synthetic demo idea on a throwaway branch: graduation → draft plan → enrol batch minted `ASK-enrol-b3e7d2` + pushed → bot posted the Discord card → supervisor clicked **enrol** → bot wrote the RS256-signed answer → `ask-cli check` verified it → the next cycle ARMED the plan (`status: active` + `auto_pilot: true`) + consumed the ask. Full chain `accept → graduate → enrol-ask → Discord click → signed answer → arm` works end-to-end with cryptographic authorization. Cleaned up; authoritative dry-run confirms opted-in=0, zero residue. **Reliability finding (now plan S3.4):** `Invoke-GatePush` silently swallowed a failed `git push` once — the ask was committed locally but didn't reach the remote until pushed by hand; in production a worker would park forever. The bot's old 2026-06-17 `401` was transient (the round-trip proves the token works now).

**S3.4(a) push-reliability — FIXED 2026-06-19.** Vá `Invoke-GatePush`. Root cause sâu hơn `catch {}`: trong PowerShell `git push` FAIL KHÔNG THROW (chỉ set `$LASTEXITCODE`), nên catch chẳng bao giờ bắt → push fail luôn bị nuốt im. Fix: kiểm `$LASTEXITCODE`, retry 1 lần sau `pull --ff-only` (case non-ff khi bot push song song), fail cuối → hét `WARNING: gate push FAILED` + trả `$false` (commit local giữ → cycle sau retry). 3 caller hứng `$null = Invoke-GatePush`. Runtime-verified trong temp-git sandbox (clean→no-op True · good remote→True+reached · broken remote→False+warning+commit preserved). Plan S3.4 → [~] (phần (a) xong; (b) verify-landed còn mở, gộp với S3.2 watchdog).

**S3.2 watchdog — DONE 2026-06-19** + **S3.4(b) — DONE**. Phase-4 block trong `auto-pilot-scheduled.ps1`: progress fingerprint = multiset sorted của TẤT CẢ branch tip (`git for-each-ref refs/heads`); cycle "did work" mà không nhúc nhích tip nào = no-progress. Phân biệt 2 ca KHÔNG phải stall để không báo nhầm: (a) parked đúng trên 1 quyết định human (`current-ask`/`current-gate` tồn tại + `*-cli check`==none) → reset; (b) human ĐÃ trả lời/duyệt mà worker chưa consume (answered ask / `approve` gate chưa cross) → coi là stall mạnh (đúng cái AC gọi "gate approved but not crossed"). Escalate qua `ask-cli report`→Discord chỉ sau `StallThreshold=2` cycle no-progress-không-parked liên tiếp, và CHỈ 1 lần (`escalated` flag, chỉ set nếu publish push thành công → không thì retry cycle sau). State ở `~/.claude/auto-pilot-logs/watchdog-state.json` (machine-bound). S3.4(b): `Invoke-GatePush` giờ phân biệt "chưa provision" (dir vắng → no-op đúng) vs "provision-nhưng-hỏng" (dir có asks/reports chờ nhưng không `.git` → WARN + `$false`, không nuốt im). Cũng sửa 1 lỗi tự gây: `2>&1` trên native git (trap #60 dưới Start-Transcript) → `| Out-Null` + `$LASTEXITCODE`. Verify: unit-test 10 scenario của decision-fn (PASS) + git-for-each-ref/JSON-roundtrip real-I/O + S3.4b glob + dry-run wiring. **Plan: S3.2 [x], S3.4 [x]. Phase 3 còn lại: chỉ S3.1 (retry-cap=3 + escalate) + full-S3.3.** Lưu ý: escalation-publish path (report→GatePush) là cùng plumbing đã proven live ở S3.3, nhưng watchdog với `didWork=true` thật chưa chạy live (cùng hạng wiring≠runtime #71 — sẽ tự kiểm khi cycle thật đầu tiên có việc).

**S3.1 retry-cap — DONE 2026-06-19.** Full-cycle ORDER đã wired sẵn; phần còn thiếu là "retry cap=3 then escalate". Thêm retry-cap registry trong `auto-pilot-scheduled.ps1`: streak fail-liên-tiếp per-target (`retry-state.json` ở `$LogDir`) keyed `advance:<plan>`/`graduate`/`enrol`/`reflect`. Mỗi batch ghi exit qua `Set-RetryOutcome` (exit 0 → xoá streak); lần fail thứ 3 liên tiếp → escalate Discord **1 lần** + BLOCK target đó (`Test-RetryBlocked` skip) tới khi human can thiệp; phase khác vẫn chạy. Bổ trợ watchdog: watchdog = no-progress *toàn cục*; retry-cap = chặn *1 target fail* lặp vô hạn. Batch parked đúng exit 0 → KHÔNG tính fail (đó là việc của watchdog). C3 cố ý loại trừ (propose-only + throttle once/day-on-success riêng). Verify: unit-test 7 scenario (streak/escalate-once/clear-on-clean/independent-keys) ✓ + dry-run wiring ✓. **Phase 3 phần KHÔNG-cần-live giờ XONG hết (S3.1+S3.2+S3.4); chỉ còn full-S3.3 (planning-Q&A round-trip + single-transcript execute→wrap→retro) vốn BẮT BUỘC chạy live có người giám sát.**

**Closing — PR #3 MERGED to `main` + branches cleaned (end of 2026-06-19).** PR #3 (`feat(autonomy): closed-loop driver Phases 0-2 (+ skill-law refactor, app-health, sandbox retirement)`, 40 files, +2553/-1082) merged via merge-commit `a19b95d` — clean fast-forward, 0 conflict. The supervisor reviewed a legible per-stream summary then authorised the merge (governance-into-`main` is the human's act; the agent prepped the review, did not self-merge). After merge, BOTH merged `auto/*` branches deleted (local+remote: `auto/skill-law-refactor`, `auto/retire-sr-sandbox`) → repo down to a single `main`, which **resolves the "branch-state coherence across parallel `auto/*` branches" risk** the plan flagged (no parallel branches left to drift). All Phase-3 non-live work (S3.1+S3.2+S3.4) is now in `main`.

**full-S3.3 — PROVEN LIVE 2026-06-19 (both parts) + a real bug found & fixed.** Ran the two unproven halves on throwaway branches, supervisor live on Discord.
- **Part A — planning-Q&A round-trip (S1.2):** staged a synthetic accepted idea-9997 whose accept deliberately left a real design fork open (where to store metrics: a/b/c). Round 1: graduation correctly detected the ambiguity → minted `ASK-graduate-9997a8` (3 options) → pushed → bot posted the Discord card → supervisor clicked **(a)** → bot wrote the RS256-signed answer. Round 2: graduation `ask-cli check` verified the signature, read **(a)** as DATA, and finalized the draft plan reflecting option (a) (`status: draft`/`auto_pilot: false`/`enrol: pending`), moved idea-9997 under `## Done` with `graduated_plan:`, consumed the ask. ⇒ the planning-Q&A Discord round-trip works end-to-end. (Bonus: the watchdog's "correctly parked awaiting a human" branch ran LIVE here and classified it right — not a stall.)
- **Part B — single-transcript execute→wrap→retro (S2.x):** staged a throwaway opted-in plan (1 trivial safe step). One wrapper run advanced it (created the scratch file, checked the box, plan→done) AND fired the reflection batch (session-wrap + retro filed nothing [correct] + `/idea sort` + composed a Discord digest) — **both batches in ONE transcript**. The execute→reflect chain is confirmed.
- **BUG found live (the demo's real payoff):** the reflection's final `Invoke-GatePush` hit a non-fast-forward (the bot had pushed concurrently) and the wrapper **CRASHED (exit 1) at the push line** instead of retrying — the digest never reached Discord. Root cause: removing the blanket `catch {}` in S3.4a exposed ledger-#60 — under `Start-Transcript` + `$ErrorActionPreference='Stop'`, a failed `git push`'s stderr becomes a **terminating** `NativeCommandError`, aborting the wrapper BEFORE `$LASTEXITCODE`/the retry could run; and even if reached, `pull --ff-only` cannot reconcile a *diverged* (1-ahead/1-behind) gates repo. **Fix (`3270250`, branch `fix/gate-push-transcript-trap`):** force `$ErrorActionPreference='Continue'` inside `Invoke-GatePush` (native errors non-terminating → retry reachable) + change the retry to `pull --rebase` (replays our ask/report onto the bot's commit; disjoint files ⇒ no conflict; `rebase --abort` fallback). **Verified by REPRODUCING the trap:** a sandbox under real `Start-Transcript`+`Stop` with a diverged origin — old logic crashes (`NativeCommandError`), patched logic survives + rebases + delivers (in-sync 0/0). The stuck digest was flushed by hand so the Part-B digest did reach Discord. All synthetic residue cleaned (4 throwaway branches deleted, runtime state reset, dry-run opted-in=0).

**Open threads**
- **`didWork=true` runtime gap (#71) — now PARTLY closed:** the Phase-2 reflect batch + the watchdog "parked-awaiting" branch DID run live this demo. Still unproven live: the watchdog/retry-cap **escalation** paths (no real stall/failure occurred) — they reuse the now-fixed `GatePush` publish plumbing.
- **The transcript-trap fix awaits merge** (`fix/gate-push-transcript-trap`, commit `3270250`). Until merged, `main`'s `auto-pilot-scheduled.ps1` still has the crashing `GatePush`.
- **Human-action items now in `main` (merging did NOT auto-apply them):** apply the governance proposals `proposals/CLAUDE.md.proposed`, `proposals/05-documentation-standard.md.proposed`, `proposals/2026-06-19-enrol-gate-hook-hardening.md`; review+install/reject the skill drafts `skill-proposals/auto-pilot-smoke-test.md` (+ the older `auto-pilot-trust-gate-cli.md`); gate idea-0013/0014.

## Governance finalize + Phase 4 opened + idea-0014 backup graduated — 2026-06-19

```yaml
id: 2026-06-19-02
type: episodic
created: 2026-06-19T17:30:00+07:00
last_accessed: 2026-06-19
importance: 7
milestone_id: closed-loop-driver
related_ids: [{id: 2026-06-19-01, why: "same session continued — closes the human-action items it left open"}]
embedding: null
```

**What happened** — cleared all the human-action items the full-S3.3 session left, then opened Phase 4 (PRs #5–#12 merged to `main`):
- **PR #5** merged — the `fix/gate-push-transcript-trap` (S3.4c) landed; `main`'s `Invoke-GatePush` no longer crashes under `Start-Transcript`.
- **PR #6** — applied the Phase 5 governance docs (`CLAUDE.md` + `05-documentation-standard.md` §7.1: SKILL=procedure / references=LAW); **gated idea-0014 (accept→active) + idea-0013 (defer, revisit when journal/3rd app adds MCP)** — oracle decisions were the supervisor's, not self-scored.
- **PR #8** — **enrol-gate hardening (Option A)** implemented + verified **34/34** then INSTALLED to `.claude/hooks/autonomy-gate.mjs`; **skill `auto-pilot-smoke-test`** installed. Both crossed the governance boundary only on the supervisor's *explicit, specific* "run the install" instruction (a vague "handle all" was blocked by the auto-mode classifier — see ledger).
- **PR #7** — opened closed-loop **Phase 4** with a research-grounded proposal scaffold (`plans/2026-06-19-phase4-memory-harness-proposal.md`); the 2 research subagents hit a session limit, so the 3 NEW mechanisms are framed-only with `research: PENDING`. Scheduled a **cloud one-off agent** (`trig_01NbfRyWaLHNmrUa1oK8w6g9`, 2026-06-19T10:20Z) to re-run the research → a PR.
- **PR #9** — cleaned up the consumed `.proposed`/staged files; kept the enrol-gate design record (marked installed).
- **PRs #10–#12 — idea-0014 full spine:** research-grounded Restic backup **proposal** (#10) → supervisor **accept** → **/project-plan** (active build plan, #11) → authored the in-repo **backup assets** (#12): `nuc-platform/backup/` (config-driven `restic-backup.sh` w/ app-consistent PG/SQLite dumps, restore-test, 5 systemd units, env template, deploy README; `bash -n` clean).

**Decisions made** — distilled up into `06-knowledge-ledger.md` §A (governance-boundary-in-practice; arming-now-gate-enforced; Opus-review-of-subagent caught real factual errors; app-consistent DB dump per engine).

**Open threads**
- **Cloud research PR (Phase 4)** pending (~17:20+) → review → fills the PENDING blocks → proposal `status: proposed` → supervisor accept → `/project-plan`.
- **idea-0014 build plan** active: steps 1,3–7 are human/NUC (verify engines, create B2 bucket+keys, init, restore drill, install timers, wire alert); step 8 (INVENTORY §2/ops docs) deferred to post-deploy (write docs of the LIVE setup, not intent).
- The enrol-gate design record + this whole session's lessons are now distilled (ledger rows below); nothing else outstanding.
