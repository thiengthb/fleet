# 2026-07-31

## Making the platform's own tooling honest on the SECOND machine — 2026-07-31

```yaml
id: 2026-07-31-01
type: episodic
created: 2026-07-31T02:30:00+07:00
last_accessed: 2026-07-31
importance: 8
milestone_id: null
related_ids:
  - { id: 2026-07-30-01, why: "its Open threads named this box's 59 BROKEN as nobody's job yet — this is that job" }
```

**What happened**

- Opened on "is there anything left in fleet" and the honest answer came from the tools, not the plans:
  **59 BROKEN / 178 drift on this box** while the Linux box read far less. The previous session had recorded that
  as "a platform-on-Windows gap"; half of that reading was wrong and it would have kept the box permanently red.
- **15 red test suites were real defects, in three mechanisms.** Mutation patches written with `\n` against a CRLF
  working tree (so `.replace()` matched nothing and the suite reported "the patch is stale" — a real signal firing
  for a fake reason); reports emitting native `\` separators; and three sandboxes setting only `HOME` when
  `os.homedir()` reads `USERPROFILE` on Windows, so those fixtures were measuring the developer's real machine.
  Ended **30/30 suites**, and `eval-ledger-rule`'s exemption was closed rather than re-argued → **29/29 tools
  tested, 0 exempt** (which is the literal reading of the original *không chừa*).
- **Five silent tool defects, four of them not Windows-specific at all.** `platform-report` built its git-log
  marker with bash ANSI-C quoting and `shell: "/bin/bash"` → spawn threw → catch → empty history → **every age in
  the repo unknown**, which is the input to every retirement verdict. `attic` shelled `grep … 2>/dev/null || true`
  through cmd.exe → the mention guard answered "nobody references this file" on every call, and its worst reader is
  `attic verify`, the 2026-08-29 gate. `git-sync-check` measured any directory holding a `.git`, so a corrupt
  project was reported with an ancestor's dirt (this box's home directory is a repo). `reuse-scan` read the
  canonical registry from a hardcoded `commons/` path that did not exist while the repo sat under its old folder
  name → **11 artifacts that already live in commons were reported as EXTRACT**. And extracting
  `eval-ledger-rule`'s deterministic half found that `buildSandbox` never created `platform/registries/`, so every
  arm of that eval died with ENOENT before the model was asked anything.
- **Batch D2 closed:** a shape ERROR on a closed plan is now `LEGACY` — 105 → **25 live + 80 legacy**, and only the
  live count gates. The rename plan (E8/E9) closed for the second time: the supervisor closed the editor, the
  prepared script moved nine repos into `projects/` and renamed `ui-kit` → `commons`, **link-check 44 → 1** and the
  1 is `docgen`, which has no remote to clone.
- **Two machines, two directions.** The Linux box pushed 12 commits mid-session; both boxes independently found and
  fixed the same defect (the trend log could not say which machine a row came from) within the same hour. Kept the
  pushed version, re-applied only what it lacked. Then a **parallel session in THIS working tree** turned out to be
  what made `attic.test.mjs` flaky — and my own new suite was writing mutants inside the repo, the same class that
  session had just fixed in two other files.

**Decisions made** — distilled to `platform/ledger/2026-07.md`, not copied here:
`#2026-07-30-a-tool-that-shells-out-has-assumed-a-shell`,
`#2026-07-30-a-sandbox-must-redirect-every-name-for-home`,
`#2026-07-30-an-exemption-must-name-the-smallest-untestable-part`,
`#2026-07-30-a-count-that-improved-deserves-the-same-scrutiny`,
`#2026-07-31-a-machine-local-measurement-cannot-be-shared`,
`#2026-07-31-a-parallel-session-is-a-second-writer`.
Mechanism added for the third instance of the swallowed-failure class: `recurrence-check` **D6
`silent-empty-on-failure`**, baseline 0.

**Open threads**

- **`docgen` has no remote and exists on the Linux box only** — one copy, no backup, and `link-check`'s last
  BROKEN wire on this box. Recorded in INVENTORY; the supervisor has not decided keep-with-remote vs discard.
- **Two Claude sessions were running in one working tree.** Every tool here reads the real repo's `git status`, so
  concurrent runs make each other red and each false red costs an investigation. If parallel work is wanted, it
  needs `git worktree`, not a second window on the same folder.
- **`sprawl-check`'s baselines are declared for two machines only** (`thien-ubuntu`, `TNT-Laptop`). A third box
  reads as *unratcheted* and fails `--gate` until someone records its line — deliberately, but it is a thing that
  will need doing.
- **A cheap-staffing candidate, recorded so it does not evaporate:** the LF-normalisation of 17 mutation-source
  reads was pure bulk-mechanical work (one identical edit per site, verified by re-running each suite) — the shape
  that belongs on a cheaper subagent under review next time. The five silent-defect diagnoses were the opposite:
  each needed the whole session's context to even form the hypothesis.
- **Date discipline crossed midnight this session:** entries written before ~01:00 local are dated 2026-07-30 and
  the work continued into 2026-07-31. `health-sweep` used to print a UTC date over a locally-dated row; fixed, but
  other tools were not audited for the same split.
