# 2026-07-21

## sakubun — F5 completed live, plan hygiene, CVE triage, enrichment coverage to 508/508 — 2026-07-21

```yaml
id: 2026-07-21-01
type: episodic
created: 2026-07-21T15:30:00+07:00
last_accessed: 2026-07-21
importance: 8
milestone_id: null
related_ids:
  [
    { id: 2026-07-20-01, why: 'same continuous work pass; this is the day-2 continuation across the date boundary' },
  ]
embedding: null
```

**What happened** — one long pass continuing from 2026-07-20, spanning the date boundary. Docker Desktop hung mid-rebuild twice and the session restarted twice; data survived intact each time (verified `integrity_check`).

- **F5 finished and verified LIVE, not just built.** F5.1 backup (`/api/backup` via `VACUUM INTO` + integrity-check) proven by `scripts/verify-restore.sh` restoring into a throwaway container and diffing every table. F5.2 a11y: audited RENDERED HTML, found 0/6 select triggers named, fixed centrally in `Field` (clone + context) to 6/6. F5.3: trimmed the `get_due_items` payload 1054→817 tok by cutting only jaRuby + review-item examples.
- **Restore-in-app resolved by user decision:** keep as-is (docker 3-command restore). Presented three options; user picked "don't build more".
- **Plan hygiene:** audited every `docs/plans/*` against the code. `practice-flexibility-batch` was stuck at in-progress with everything shipped AND claimed `autoLearnSupport` DONE though it was deleted 2026-07-17. `adaptive-vocab-sensory` is BLOCKED on C0 (a user-run voice spike). F0.6/F1.5 surfaced as carried-forward, not buried in a closed plan.
- **CVE triage:** one shipped HIGH (`brace-expansion` via exceljs) fixed with a scoped override; the other 7 are dev-only or false major-downgrade "fixes" (Next 16→9). Documented, not churned.
- **Enrichment coverage closed to 508/508 grammar cards** — wrote all 86 remaining N5 cards by hand in 6 validated batches; a duplicate-key + mixed-script gate caught a nonexistent-item card, a silently-overwriting duplicate key, and a leftover placeholder before any reached the library.
- **Extracted the enrichment toolkit** (`scripts/enrich/`) so the validator that caught those bugs doesn't evaporate with the scratchpad.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-20/21: "A prop handed to a component that renders no DOM disappears silently" · "A backup is a claim until a restore has been performed" (+ restore-in-app kept as-is) · "Grep the protocol before trimming what it reads" · "CVE triage: fix the one that ships" · "Enrichment coverage closed to 508/508". Ledger + `02-known-traps` §10 (compose volume prefix).

**Open threads** — **C0 voice spike is the only open item, and it is USER-run** (speak into Claude Desktop voice mode, report whether it hears + grades correctly); C3/C4 depend on it. `adaptive-vocab-sensory` stays `active`/blocked until then. 86 N5 patterns are now enriched but still UNSTUDIED (FSRS hasn't introduced them). 7 moderate CVEs accepted. Enrichment cards live only in the volume + the local backup file, not git.
```

## sakubun — new direction: multi-user + deployed; Phase 1 LIVE, Phase 2 auth+schema started — 2026-07-21

```yaml
id: 2026-07-21-02
type: episodic
created: 2026-07-21T22:45:00+07:00
last_accessed: 2026-07-21
importance: 9
milestone_id: null
related_ids:
  [{ id: 2026-07-21-01, why: 'same day, separate work stream — that pass was enrichment/CVE; this is the multi-user/deploy direction' }]
embedding: null
```

**What happened** — framed + planned a large new direction and shipped its first phase live.

- **Planned it:** `docs/plans/2026-07-21-multi-user-deploy-sharing.md` (5 phases) — turn the single-user local MCP trainer into a multi-user, deployed app with a shared resource library + shared history/comments.
- **Architecture decided: CENTRALIZED**, not local-first + a thin share hub. Local-first was the better fit on every axis except the one that decided it — the operator needs a hard kill switch (revoke session/token/ban) and self-hosting can't lock a user's own copy. Retires sakubun invariant #5.
- **Deploy target pivoted twice:** NUC dead → VPS → VPS also dead → **THIS machine as a home-server** via Cloudflare Tunnel. Surveyed 2026 free clouds (Fly killed free tier, Render free sleeps+no disk, Railway trial-only; only Oracle Always Free fits, kept as the later ARM upgrade).
- **Phase 1 SHIPPED + verified live:** `https://sakubun.thientnse.site` (cloudflared container → :3789), gated by Cloudflare Access (operator only, verified 302), CI→ghcr green, machine set no-sleep. The running app is unchanged (`sakubun:local`, pre-auth).
- **Phase 2 batch 1** (committed): better-auth (email/password + `admin` ban/revoke + `nextCookies`) + a **custom `McpKey`** token table (rejected `@better-auth/api-key` — its peer deps pull zod v4 vs this app's zod v3). Additive migration (5 tables), build/lint/test green.
- **Phase 2 batch 2 schema** (committed): nullable `userId` on Item/SrsState/ReviewLog/Sentence + Settings; Prisma SQLite RedefineTables, data-preserving; test 134/134.
- **Paused deliberately** before the heavy `userId` threading (the security-sensitive core work).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-21: "centralized, not local-first + hub" · "better-auth + custom McpKey" · "home-server via Cloudflare Tunnel (free-cloud survey)" · "nullable-userId migration". Ledger: 2 cross-project lines (off-NUC free hosting; better-auth adoption pattern + api-key/zod4 trap).

**Open threads** — Phase 2 next = **thread `userId` through `lib/practice.ts`** (2.4, security-sensitive — keep on the main loop, don't delegate) + **fail-closed tenancy guard + a "user A can't read user B" test** (2.5) + **backfill** (create the operator user + claim existing data — GATED, preview first) + auth UI + MCP-token verify + lock `/api/backup` admin-only + retire invariant #5 in `00-map`/`CLAUDE.md`. **Migrations are NOT yet on the live Docker volume** (only dev.db) — the live app is still the pre-auth build, safe behind CF Access. Phase 3 (library) + Phase 4 (history+comments) follow.

## sakubun — UI polish pass (star, furigana, table UX, guide) + verify-image discipline — 2026-07-21

```yaml
id: 2026-07-21-03
type: episodic
created: 2026-07-21T13:00:00+07:00
last_accessed: 2026-07-21
importance: 6
milestone_id: null
related_ids:
  [
    { id: 2026-07-21-02, why: 'same day; the UI polish rode ON TOP of the in-progress multi-user schema, and a rebuild applied its migrations' },
  ]
embedding: null
```

**What happened** — a multi-round UI feature/polish pass driven by the operator, on top of his in-progress multi-user schema work (block -02). Rebuilt + verified the LOCAL container after each round.

- **Round 1 (7 features):** section headings tag→bold+themed-dot (shared `SectionTag`); **star/favourite** on grammar+vocab+history (`Item.starred`/`Sentence.starred`, additive migration `add_star_flags`) + filters; DataTable toolbar controls moved right (action outermost); bulk suspend; detail-page action buttons onto the tab row; **/guide "Tính năng" tab rewritten** around user capabilities (led with luyện dịch, dropped bulk-delete-as-headline).
- **Round 2 (4):** grouped star + quick-look LEFT of the ⋮ row menu (dropped the lone star column + the peek in the term cell); bulk **resume** (contextual when all-selected-suspended); **pageIndex-clamp bug fix**; /guide tool/prompt table gained a Loại column.
- **Round 3 (3):** Thống kê tab persists range+grouping (`useSessionState`); **furigana alignment fix** (`buildFuriganaMarkup`); detail buttons → expand-on-hover style (shared `ExpandingIconButton`).
- **Verify discipline:** built `scripts/verify-image.sh` — verify the image against a COPY of the real 509-item volume on a throwaway port BEFORE rebuilding the live container. 134 tests green each round.
- **DataTable changes synced** to the `ui-kit` registry copy (toolbar + STT toggle + pageIndex clamp).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-21: "Furigana must be ALIGNED onto the kanji" · "A filter OUTSIDE DataTable can strand its pageIndex" · "Verify a rebuilt image against a COPY of the real volume (+ seed chown trap)". Ledger: pageIndex-clamp cross-app line. `02-known-traps` §11 (root-seeded volume → readonly migrate).

**Open threads** — **SIDE-EFFECT to flag:** rebuilding the LOCAL container for this UI work ran `migrate deploy`, which applied the operator's committed **auth+userId migrations** (`add_auth_and_mcp_key`, `add_user_tenant_columns`) to the LOCAL volume — it now has the auth tables + `Item.userId` (nullable, NULL on all 509 rows; data intact). This ADVANCES block -02's "migrations not yet on live volume" for the local box. Backfill still un-run (userId all NULL). Not pushed. Star color left as `warn` (amber) pending operator's call.
```

## sakubun — Phase 2 (multi-user + auth) SHIPPED LIVE end-to-end — 2026-07-21

```yaml
id: 2026-07-21-03
type: episodic
created: 2026-07-21T15:50:00+07:00
last_accessed: 2026-07-21
importance: 10
milestone_id: 2026-07-21-M-multiuser
related_ids:
  [{ id: 2026-07-21-02, why: 'this is the execution of the plan framed in 02 — Phase 2 code + the live cutover' }]
embedding: null
```

**What happened** — turned sakubun from single-user/no-auth into a deployed multi-user app AND cut it over live.

- **Phase 2 code (13 commits, pushed):** userId threaded through `lib/practice.ts` (+ analytics/item-stats/stats); a
  fail-closed Prisma tenancy guard (`lib/db.ts`); a real cross-user SQLite test; better-auth session (web) + a per-user
  `McpKey` bearer for MCP (`withMcpAuth` required:true, `getUserId(extra)` in every tool); login/register/account/admin
  UI (previews approved 4/4); `/api/backup` admin-only; `/guide` + invariants retired #5; a gated backfill script.
- **Verified RUNNING, not just built:** headless smoke (register → first-user auto-admin → MCP 401 no-token / 200 with
  token → add→due loop scoped) BEFORE cutover.
- **Live cutover (home-server = this machine):** backup live volume first (509 items, integrity ok) → build the image from
  a CLEAN git worktree at the pushed commit (to EXCLUDE the operator's uncommitted study-groups WIP) → `docker compose up
  -d --no-build` → operator registered (admin) → backfill `--apply` claimed 1267 rows → verified operator's data flows
  through the authed MCP.
- **Two traps hit + fixed:** better-auth `INVALID_ORIGIN` (baseURL=domain rejects localhost) → `trustedOrigins`; migrations
  were ALREADY on the live volume (a prior rebuild), so G2 was code-only (`migrate deploy` = no-op).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-21: fail-closed tenancy guard · first-user-admin + in-container
raw-SQL backfill. Infra traps → `02-known-traps` §11–§12 (build-from-worktree-excludes-WIP; better-auth origin). Ledger: tenancy-guard pattern.

**Open threads** — G6 not done: Cloudflare Access still blocks friends (remove to open self-registration); the live
container lacks the `trustedOrigins` fix until the next rebuild (localhost login still 403; public URL works). `Settings.id`
still `@default(1)` → latent collision when a 2nd user saves settings (autoincrement deferred). Phase 3 (share library +
fork) + Phase 4 (comments) still ahead in the plan. Operator's own G5 verify (create real token, practice loop) pending.

## MILESTONE — sakubun is multi-user + deployed — 2026-07-21

```yaml
id: 2026-07-21-M-multiuser
type: reflection
created: 2026-07-21T15:55:00+07:00
last_accessed: 2026-07-21
importance: 10
milestone_id: 2026-07-21-M-multiuser
related_ids: [{ id: 2026-07-21-03, why: 'the session that shipped it' }]
embedding: null
```

**Milestone:** sakubun went from a local single-user MCP trainer to a **centrally-deployed multi-user app with revocable
auth** — the operator can now onboard friends (email/password), each with isolated data, connecting Claude Desktop via a
personal revocable MCP token, and can ban anyone instantly (web + MCP). Retired the founding "local-only, no-auth" invariant.
Next arc: Phase 3 sharing (public library + fork) then Phase 4 (shared history + comments).

## sakubun — study-groups Slice 1–2 shipped; MCP endpoint unblocked (CF Access) — 2026-07-21

```yaml
id: 2026-07-21-04
type: episodic
created: 2026-07-21T22:40:00+07:00
last_accessed: 2026-07-21
importance: 7
milestone_id: null
related_ids:
  [{ id: 2026-07-21-02, why: 'builds on the multi-user tenancy shipped in that pass' }]
embedding: null
```

**What happened**

- Diagnosed the operator's Claude Desktop MCP failure ("Unexpected content type: text/html") to **Cloudflare Access gating `/api/mcp`** — a machine endpoint sitting behind interactive SSO. Operator added a CF Access **Bypass** app scoped to path `api/mcp`; verified end to end (curl: 302→CF login BEFORE, then 401 JSON app-gate AFTER; `initialize` with the real token → `serverInfo{name:sakubun-mcp}`). Multi-user plan **G5 satisfied**.
- **Study-groups Slice 1** (data layer): denormalized `GroupMember.userId` (migration `group_member_user_id`) so the fail-closed guard covers the join table like SrsState/ReviewLog; `lib/groups.ts` = CRUD + kind-guard (vocab/grammar/mixed) + `listAddableItems` + `getItemGroupPicker`; `lib/groups.test.ts` (real SQLite) 9/9. Commit `c43b1e5`.
- **Slice 2a** (UI): `/groups` (card grid, create/edit/delete dialogs) + `/groups/[id]` (member list + add picker), 5 server actions, "Nhóm" sidebar entry. Kind enum split to `lib/group-kinds.ts`.
- **Slice 2b**: `listItems` now returns each item's `groupIds`; the grammar+vocab resources tables gain a "Nhóm" filter + bulk "Thêm/Gỡ nhóm" (kind-guarded add targets), and the item-detail page shows removable group chips + an add picker. Commit `3514921`.
- Gate green throughout: `tsc` 0, `eslint` 0, `next build` ok, 148 tests.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-21: "`/api/mcp` carved OUT of Cloudflare Access" (+ infra trap `02-known-traps §14`); "Client component importing a VALUE from a DB-importing module leaks `node:module` into the browser bundle → split client-safe constants". Ledger: the node:module client/server split (all Next apps). Plans: study-groups Slices 1–2 ticked; multi-user G5 done.

**Open threads** — study-groups **Slice 3 (MCP tools list/create/add/remove/suggest_group + `groupId` on the due batch + `catalog.ts`) and Slice 4 (guide + docs + rebuild live) NOT started** — paused for the operator to eyeball the Slice 2 UI + approve the Claude-facing surface change (invariant #3). `/groups` not visually verified by a screenshot (auth-gated; build/tsc/lint/tests are the evidence). Still pending from G6: `Settings.id @default(1)` → autoincrement before a 2nd user saves settings; removing CF Access entirely to open self-registration. The CF-Access §14 trap lives in the OUTER repo (`nuc-platform/`), a separate commit from the sakubun code.
