<!-- One file/day; multiple entries = multiple blocks. Schema: see README.md. -->

## sakubun — UI polish, data re-owning, and optimization-v2 kickoff — 2026-07-25

```yaml
id: 2026-07-25-01
type: episodic
created: 2026-07-25T06:40:00+07:00
last_accessed: 2026-07-25
importance: 7
milestone_id: null
related_ids:
  - { id: 2026-07-24-03, why: 'continues the cross-machine identity-sync + local-public-tunnel session' }
embedding: null
```

**What happened**

- Recovered/relocated study data across accounts: adopted the 64 single-user-era orphan items (+143
  ReviewLog +64 SrsState) to `628`, then on request MOVED the whole original set to `tranngocthien024`
  (45 grammar + 19 vocab, incl. reconstructing 6 merged-dup items from a backup); `628` left as the pure
  publisher (508 public grammar). Every write behind a fresh backup + a self-verifying transaction.
- Two UI-polish batches (settings sliders: division ticks + bigger thumb; data-tab cards → visible
  descriptions + section label/description, icons added then removed per user; history/community cards:
  drop topic + topic filter, direction → `JP → VN`/`VN → JP`, translation gets a `Dịch câu` tag,
  `Hội thoại tình huống`→`Hội thoại`, relative-time-with-hover; resources due → relative + hover, drop
  the "Hiển thị hạn" toggle) and the `/library` 200→5000 limit fix.
- Fixed the **slider thickness bug** (a `data-*` variant out-specifies a plain override) — see decisions.
- Accepted + started **optimization plan v2** (multi-session P3): P0.1 knip + dead-code report, P0.2
  `practice.ts` DB-harness test net (5 tests), P1.5 twin-table dedup (`srs-constants`, `due-cell`,
  `speakable` ×7→1). Grounded by a 4-subagent read-only audit.
- Committed the agent's work in 3 feature-atomic commits; left the user's concurrent WIP untouched.

**Decisions made**

- `sakubun/docs/decisions.md` 2026-07-25 — data-* variant specificity trap (slider) + `/library` limit stopgap.
- Ledger §A — Tailwind v4 `data-*` variant beats a plain override utility (cross-project shadcn gotcha).
- Plan `docs/plans/2026-07-24-optimization-maintainability-v2.md` (active) — full audit + roadmap; P0.1/P0.2/P1.5 ticked.

**Open threads**

- Opt-v2 plan `active`: next P1.1 `withAction` wrapper (~30 actions in `app/actions.ts`), then P1.2/1.4/1.6/1.7,
  P2 components/ re-org (GATED on user's WIP committed), P3 perf (indexes, unbounded `getStreak`-every-10s scan,
  library LEFT JOIN), P4 dev-review compose, P5 dead-code sweep (date-picker/calendar/alert + date-fns/react-day-picker/shadcn), P6 practice.ts split (re-opened, analyze-first + gate).
- User WIP left uncommitted: `lib/practice.ts`, `lib/mcp/{prompts,render,render.test}.ts`, `row-actions.tsx`,
  `row-share-button.tsx` (deleted), `components/_INDEX.md` (needs `srs-constants.ts`+`due-cell.tsx` lines added when it settles).
- P1.4 (`useServerAction`) touches `row-actions.tsx` → wait for the user's row-actions WIP to land first.
```

## sakubun — opt-v2 execution: dead-code, indexes, library query, action helpers — 2026-07-25

```yaml
id: 2026-07-25-02
type: episodic
created: 2026-07-25T18:00:00+07:00
last_accessed: 2026-07-25
importance: 6
milestone_id: null
related_ids:
  - { id: 2026-07-25-01, why: 'same day — executes the optimization plan kicked off in -01' }
embedding: null
```

**What happened** — executed opt-v2 steps in WIP-independent order, each committed + gated (lint/test/build):

- P5.1 dead-code: removed `ui/{date-picker,calendar,alert}.tsx` + dropped `date-fns`/`react-day-picker`. `shadcn` looked dead to knip but is CSS-imported → reinstated + `knip.json ignoreDependencies`.
- P3.1: 4 hot-path DB indexes (migration `20260725083912`). P3.3(a): `listPublicLibrary` correlated subqueries → LEFT JOIN aggregates (b/c deferred: BLOB lazy-load, server-pagination).
- P1.7: single rating-label source + reuse `withinDatePreset`/`DATE_PRESETS`.
- P1.1+P1.2: RESCOPED — pure `lib/action-result.ts` glue (ok/err/parseInput/cleanIds) across ~30 actions instead of a mega-wrapper.
- Committed the user's own WIP snapshot (share-in-⋮-menu + RowShareButton-lock) at their request to unblock P1+.

**Decisions made** — `decisions.md` 2026-07-25 "extract glue not a withAction wrapper" + (last wrap) the data-\* variant trap; ledger §A knip-CSS-import false-positive.

**Open threads** — plan 6→7/25 lines done (see `docs/plans/2026-07-24-optimization-maintainability-v2.md`); remaining now all unblocked: P1.3/1.4/1.6/1.8, P2 re-org, P3.2 (getStreak 10s scan) / P3.4 (N+1) / P3.5, P4 dev-compose, P5.2 knip triage (incl. dead `lib/srs.ts RATING_LABELS`), P6 practice.ts split.
```

## sakubun — opt-v2 big push: P1 dedup complete + P3 perf + P5 dead-code + P0 test net — 2026-07-25

```yaml
id: 2026-07-25-03
type: episodic
created: 2026-07-25T21:30:00+07:00
last_accessed: 2026-07-25
importance: 7
milestone_id: null
related_ids:
  - { id: 2026-07-25-02, why: 'same day — continues executing the optimization plan v2' }
embedding: null
```

**What happened** — one long push, 9 plan lines, each committed + gated (lint + 255 unit + build + knip clean):

- **P1 dedup nhánh COMPLETE:** P1.3 (`ID`/`ITEM_IDS` MCP Zod primitives into tool-kit), P1.4 (`useServerAction`
  hook — one-action-one-pending — migrated 6 clean sites; LEFT row-actions/settings-form/bulk-group/resource-add
  with reasons), P1.6 (`ConfirmDialog` — migrated 3 confirms), P1.8 (finished the ConfirmDialog rollout to ALL 5
  destructive confirms + locked it via a mechanical `docs/ui-patterns.json` gate).
- **P3 perf:** P3.2 (shared cached `studyStreak` — kills the every-10s full ReviewLog scan, busted on write +
  dayKey-guard; `getStats` bounded to an 8-day window), P3.4 (batched `saveEnrichments` N+1; left the two
  intentional non-N+1s with reasons), P3.5 (verified NO-OP — all 3 RSC-boundary audit guesses were wrong).
- **P5.2:** knip report cleared to ZERO (deleted real dead exports + `ignoreExportsUsedInFile` policy). Found a
  bonus gotcha — `lib/import-schema.ts` reads as BINARY to grep/`file` because `itemKey` NUL-joins its key.
- **P0.3/P0.4:** Playwright critical-path smoke GREEN (login → dashboard → settings-persist) on a dedicated port
  3799 + throwaway DB; coverage baseline + how-to-run in `00-map`.
- Three honest-critique SKIPs instead of tick-mind (P3.5, recentContextsFor/forkItems, BulkToggleButton), all
  logged with reasons in the plan.

**Decisions made** — `decisions.md` 2026-07-25: e2e harness traps (Prisma-7 AI-guard / better-auth Origin / Next-16
`127.0.0.1` dev block / seed-via-HTTP+sqlite) + streak-cache-with-write-invalidation + the `itemKey` NUL-binary
gotcha. Ledger §A: the Next-16 localhost + Prisma-7 force-reset e2e traps (cross-project). `ui-patterns.json`
`destructive-confirm-uses-dialog` lock.

**Open threads** — plan 17/25 done + 1 partial; remaining ALL need a user decision or are deferred infra: P2
(components/ folder re-org — needs grouping approval), P6 (practice.ts split — needs seam-boundary approval), P4.1
(dev-compose, fiddly). All 9 commits are LOCAL (unpushed, local-only deploy state). `_INDEX` debt from P1.5's
`srs-constants`/`due-cell` still pending (use-server-action + confirm-dialog were added to `_INDEX` this push).
```

## sakubun — opt-v2 CLOSED: dev-loop rescope + components/ regroup + P6/P3.3 rulings — 2026-07-25

```yaml
id: 2026-07-25-04
type: episodic
created: 2026-07-25T23:15:00+07:00
last_accessed: 2026-07-25
importance: 6
milestone_id: null
related_ids:
  - { id: 2026-07-25-03, why: 'same day — finishes the optimization plan v2 kicked off in -01' }
```

**What happened** — the final opt-v2 pass; the two decision-gated items resolved, plan flipped `done`:

- **P4.1 (rescoped):** `docker-compose.dev.yml` runs VOICEVOX ALONE (exposed 50021); the fast review loop
  is host `npm run dev` (HMR, no image rebuild), NOT a dockerized dev app (would fight better-sqlite3's
  platform-specific native binding for no speed gain). Full-image build stays the pre-deploy verify path.
- **P2 (user-approved grouping):** 93 flat `components/*.tsx` → 10 domain folders (`shell`/`shared`/
  `resources`/`library`/`groups`/`history`/`settings`/`guide`/`dashboard`/`account` + keep `ui`/`landing`).
  Done as ONE atomic codemod (git mv + alias string-rewrite) — safe because every cross-component import is
  the absolute `@/components/<base>` alias (0 relative sibling imports). 99 files, 258 alias groups. Two
  misses fixed by hand (a relative `../components` test import + a non-recursive `readdirSync` gate).
- **P6 (declined, user-confirmed):** don't split `practice.ts` — import-cycle risk at the sole DB layer,
  perf already extracted in P3, payoff is legibility-only. **P3.3(b/c) (deferred):** server-pagination is
  speculative at 508 « 5000-cap; documented a scale trigger (~2–3k patterns).
- All gates green each step (lint · 255 unit · build · knip clean · e2e 1-passed). 3 feature-atomic commits.
- **Verified END-STATE, not just commits:** `rebuild-and-verify.sh` rebuilt the image + recreated the
  container — health OK, all 7 routes 200, MCP auth-gated. The 10-folder reorg runs in the actual image.

**Decisions made** — `decisions.md` 2026-07-25: (1) absolute-alias codebase → atomic folder move + the two
things a `@/`-rewrite misses; (2) the P6-decline / P3.3-defer rationale. Ledger §A: alias-import makes a
components/ reorg atomic (cross-project). Plan `2026-07-24-optimization-maintainability-v2.md` flipped `done`.

**Open threads** — opt-v2 CLOSED (21/25 + P6 declined + P3.3(b/c) deferred-to-trigger). All commits LOCAL
(now 12 unpushed; local-only deploy state — NUC down). No active plan remains. Standing debt: P5.1's
`_INDEX` `srs-constants`/`due-cell` lines; the `itemKey` NUL→printable-key change (own gate).
```

## sakubun — viewport-fit loading skeletons + build-regression fixes — 2026-07-25

<!-- Merged from a PARALLEL machine's session (created 2026-07-25T08:35); its original id was 2026-07-25-01,
     collided with this machine's -01, renumbered to -05 on merge per the day-log id-collision rule
     (ledger §A 2026-07-24). related_ids was [] so nothing needed re-pointing. -->

```yaml
id: 2026-07-25-05
type: episodic # episodic | reflection (milestone anchor)
created: 2026-07-25T08:35:00+07:00
last_accessed: 2026-07-25
importance: 5 # 1–10, Park poignancy at write
milestone_id: null
related_ids: []
embedding: null
```

**What happened**
- Rebuild of sakubun surfaced a broken `main`: the dedup refactor `41d43c5` had dropped `RowShareButton` from both tables and mis-wired `visibility`/`forkedFromId` onto `RowActions` (a plain tsc error committed unbuilt). Fixed (`2bebac8`) + a stray test type error `studyOnce rating` (`60b14c1`).
- Built the feature: loading skeletons now **viewport-fit** to the real row count, reusing the pagination fit engine (`useFitRows` + `lib/fit-rows.ts`). New `components/fit-skeleton.tsx` (`FitList` primitive + auto-fit `Table`/`CardGrid`/`List`), re-exported through `page-skeleton`. Each placeholder mirrors the real row (table columns + header + pagination footer; history card header + play line; group member row) and matches its height. Applied to resources/library/history/group-detail; admin stays fixed. Committed `dec7a20`.
- Verified: tsc + lint clean, 232 tests pass, container rebuilt healthy + HTTP 200.
- Browser-review blocked: the claude-in-chrome extension drives a separate Chrome profile with no app session (`get-session` → null), so the authed pages redirect to /login and can't be viewed there. Pivoted to a faithful static Artifact preview (app tokens + `animate-pulse`) — user approved from it.

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-25 (skeleton viewport-fit; + the dedup-dropped-child pitfall). Cross-project line added to `06-knowledge-ledger.md §A`.

**Open threads**
- A PARALLEL session is mid-flight on a "bulk-action" feature (`components/bulk-action-button.tsx` new + `bulk-*-button.tsx` · `data-table.tsx` · `account-view.tsx` · `ui/input.tsx` · `guide-features.tsx` · `guide-coverage.json` · `ui-patterns.json`) — left untouched.
- `components/_INDEX.md` is entangled: my `fit-skeleton` doc line sits amid their `bulk-*` edits; left unstaged, so my doc line will land with WHOEVER commits `_INDEX.md`. Keep the `fit-skeleton` line when committing the bulk-action batch.
- 3 commits (`2bebac8`, `60b14c1`, `dec7a20`) are unpushed (NUC local-only; awaiting user's push decision).
```
