<!-- One file/day; multiple entries = multiple blocks. Schema: see README.md. -->

## sakubun — group flashcard (Quizlet-style) study tab — 2026-07-23

```yaml
id: 2026-07-23-01
type: episodic # episodic | reflection (milestone anchor)
created: 2026-07-23T17:20:00+07:00
last_accessed: 2026-07-23
importance: 5 # 1–10, Park poignancy at write
milestone_id: null # episodic → point UP to a reflection entry's id
related_ids: []
embedding: null
```

**What happened**

- Added a **Danh sách | Flashcard** tab pair to sakubun `/groups/[id]`. Flashcard = a Quizlet-style flip
  study over the group's members, **pure client-side, never touches FSRS** (group = organisation layer).
  Back face shows nghĩa + cách dùng + ví dụ (audio + furigana); front is the prompt only. Reused
  `SpokenLine` / `ItemDetailDialog` / enrichment; `getGroup` now selects `enrichment/suspended/srs`.
- Iterated on user feedback: removed the page title/member-count; furigana ruby replaces the raw reading
  (follows `furiganaMode`, hover-reveal); an enrich icon on each list row + a corner enrich button on the
  card open the shared detail dialog; the add-item picker terms went through `SpokenLine` (invariant #9).
- Refined controls: direction + shuffle fold into a gear "Cài đặt" **Popover** (shuffle is a persistent
  toggle, direction a labelled segmented control); a **"Mở rộng"** expand button; grade buttons carry
  per-round counts; progress bar tracks card position, not mastery. Widened the add-item dialog.
- **Ran the real app** (dev on port 3790 + a seeded throwaway dev.db + a Playwright login/screenshot
  script, because the Chrome extension wasn't connected). Screenshots confirmed all six states — and the
  run caught a bug that lint AND build reported green: a `<button>` (flip card) nesting `SpokenLine`'s
  play `<button>` → hydration error (Next dev "2 Issues"). Also fixed expand not covering the sidebar.
- Cleaned up: reset dev.db to its pre-seed state (kept the 4 pre-existing items), killed the dev server,
  left the prod container on 3789 untouched. Playwright was installed `--no-save`.

**Decisions made** — distilled into `sakubun/docs/decisions.md` 2026-07-23 (the design + the two
runtime traps: full-screen backdrop-vs-narrow-box + button-in-button hydration). Cross-project line in
`06-knowledge-ledger.md` §A.

**Open threads**

- Three commits are **local only** (`0c6c7b2`, `0e139b5`, `93bbb51`) — NUC down / local-only, not pushed.
- The `sakubun/docs/{decisions,00-map}.md` doc edits sit in the working copy, interleaved with the user's
  own concurrent WIP in those files — to be committed with the user's docs batch, not by me.
- MCP "luyện nhóm X" scoping already exists; a group-scoped SRS review path was explicitly NOT built
  (flashcard stays FSRS-free by design).

## sakubun — /library DataTable + social signals (likes, votes, auto-share) — 2026-07-23

```yaml
id: 2026-07-23-02
type: episodic
created: 2026-07-23T20:10:00+07:00
last_accessed: 2026-07-23
importance: 6
milestone_id: null
related_ids: [{ id: 2026-07-23-01, why: 'same-day sakubun UI/social batch' }]
embedding: null
```

**What happened**

- `/library` rebuilt from a card gallery to the shared `DataTable` (per-row enrich modal via reused
  `ItemDetailDialog` + eager-loaded `enrichment`; download/fork; bulk "Lấy về kho"; checkbox select).
- `listPublicLibrary` now also lists the viewer's OWN public items (`isMine` → "Của bạn"); reversed the
  old "never list your own" rule + updated its test.
- Social signals: sentence likes (`SentenceLike`) + comment up/down votes (`CommentVote`) on the
  community feed, and library item likes (`ItemLike`) — all NON-tenant models; 2 Prisma migrations.
- Auto-share toggle on the /history community tab (Settings flag `autoShareSentences`, honored in
  `submitReview`; no migration). Auto-fit page-size made visible ("Tự động (N)", always shown,
  re-enableable); `useFitRows` reserve 140→180. Shared external "Xóa lọc" on `FilterPopover`.
- Strict `SpokenLine` playable rule → `CLAUDE.md` invariant #9 + `00-map` #8.
- Shipped via local `docker compose up -d --build sakubun` (NUC down → local-only); 5 feature-atomic
  commits (sakubun) + 1 (parent). Not pushed. One transient Docker build OOM (retry succeeded).

**Decisions made** — distilled into `sakubun/docs/decisions.md` 2026-07-23 ("/library as a DataTable +
social signals"); cross-project line in `06-knowledge-ledger.md`; duplication logged in `08-SHARED-ASSETS.md`.

**Open threads**

- The session-wrap doc edits (`decisions.md`, `00-map.md`, this log, ledger) are NOT yet committed —
  they interleave with the user's concurrent group-flashcard WIP in `decisions.md`/`00-map.md`; commit
  with the user's docs batch when asked.
- A 3rd like-button use ⇒ extract a generic `<LikeButton onToggle>` (rule-of-three).

## sakubun — conversation mode (situational production, a 3rd MCP protocol) — 2026-07-23

```yaml
id: 2026-07-23-03
type: episodic
created: 2026-07-23T11:15:00+07:00
last_accessed: 2026-07-23
importance: 7
milestone_id: null
related_ids:
  [{ id: 2026-07-23-01, why: 'same-day sakubun feature batch' }]
embedding: null
```

**What happened**

- Built **conversation mode** (`chế độ hội thoại`), a THIRD practice type beside dịch + quiz: Claude
  designs a real-life SITUATION (who you are, WHO you speak to, where, what happened) + states the
  register + lists the due grammar/vocab to USE; the learner **produces their own Japanese utterance**;
  grading puts **register/appropriateness FIRST**, then each required item, then rates into FSRS.
- **The app needed no new machinery** — it reuses `get_due_items → stage_sentence → submit_review`
  untouched, with `exerciseType='CONVERSATION'` + `direction='VI_JA'` (brief in `sourceText`, model
  utterance in `suggested`, learner's utterance in `userAnswer`). **No migration.** Total code: a Zod
  enum value, one protocol constant, a 3-way `MODE_DISPATCH`, `get_protocol` gaining a mode, a prompt
  registration, a catalog entry, a `/guide` recipe, a history label.
- Three design forks put to the user, all recommended options taken: FSRS-integrated (not free-talk) ·
  single-turn (multi-turn roleplay deferred as a variant) · items from the due queue.
- Research-before-design (feature plan): task-based roleplay (accuracy/complexity/fluency), an arXiv
  precedent for LLM situational dialogues in L2, and JA register being relationship-driven — the last
  one is WHY the brief must name who you speak to and why register is graded first-class.
- **Verified by an independent Sonnet run of the protocol**, not by gates: fed a mocked due batch and a
  deliberately register-wrong utterance (`ごめん…遅れた` to a boss), it reproduced the brief shape, staged
  before showing, graded register first, tagged `register`/`omission`, rated the dodged pattern 1.
- Gates green (lint · 171/171 · build). Committed `4e1c9cc` (7 pure files). Then **rebuilt the local
  Docker container** on user request and verified the protocol/prompt/label are inside the RUNNING
  image (grep in-container) + `/api/health` ok. Not pushed (NUC down → local-only).

**Decisions made** — distilled into `sakubun/docs/decisions.md` 2026-07-23 "Conversation mode: a 3rd
protocol, situational PRODUCTION, zero app machinery" (incl. both traps). Plan
`sakubun/docs/plans/2026-07-23-conversation-mode.md` closed `status: done`. Cross-project line in
`06-knowledge-ledger.md` §A.

**Open threads**

- **NOT yet proven end-to-end:** no real conversation turn has run through the live MCP server, so
  `stage_sentence` writing `exerciseType='CONVERSATION'` → `submit_review` → the "Hội thoại tình huống"
  row in `/history` is unverified. Deliberately NOT self-tested: it is a WRITE against the prod
  container's DB on 3789 (the pollution trap). The user does one real turn = the real acceptance.
- Four files hold conversation changes but stayed uncommitted because they interleave with the user's
  in-flight Anki `.apkg` WIP: `lib/mcp/catalog.ts`, `app/guide/page.tsx`, `docs/00-map.md`,
  `docs/decisions.md`. So commit `4e1c9cc` changes the MCP surface WITHOUT its catalog entry —
  invariant #3 holds in the working tree, not in git history. Closes when the user commits their batch.
- The rebuild bundled that unfinished Anki WIP into the running image (incl. compose dropping
  `extra_hosts`, so AnkiConnect can no longer reach the host).
- Deferred by design: multi-turn in-character roleplay (`conversation_roleplay` variant).

## platform + sakubun — locked UI patterns: enforcement for rules the user keeps repeating — 2026-07-23

```yaml
id: 2026-07-23-04
type: episodic
created: 2026-07-23T12:15:00+07:00
last_accessed: 2026-07-23
importance: 8 # a governance mechanism + it immediately caught a live bug
milestone_id: null
related_ids:
  [
    { id: 2026-07-23-02, why: 'same UI batch; invariant #9 and its first gate came from it' },
    { id: 2026-07-23-03, why: 'same-day sakubun session' },
  ]
embedding: null
```

**What happened**

- The user asked for a rule/skill that FORCES compliance with UI patterns he has had to repeat —
  explicitly because he had been made "quá khó chịu" by re-stating them and re-checking by hand.
  Grounding was already on disk: `no-emoji`, `layout-standard` and `spoken-line` each say in their
  own header that the rule was stated, documented, and broken anyway (rule of three).
- Built a three-layer mechanism, deliberately ordered by how much each actually helps: (1) a
  **PreToolUse hook** that BLOCKS the session's first `.tsx` write and prints the locked list — a doc
  on the JIT read path is read after the UI is already written; (2) `sakubun/docs/ui-patterns.json`,
  where locking a rule is **one JSON entry, not a new test file** (the real reason locks never got
  written); (3) `lib/ui-pattern-lock.test.ts`, one generic runner (`forbid` / `require-with` /
  `manual`) as the durable backstop. The three bespoke gates were NOT migrated — they follow imports
  and branch on `direction`, beyond the runner.
- **Installed:** registry, gate runner, `.claude/skills/ui-pattern-lock/SKILL.md`, a `CLAUDE.md` rule
  ("user corrects a UI pattern ⇒ STOP the edit and lock it FIRST"). **Not installed:** the hook — the
  classifier hard-blocks agent writes to `.claude/hooks/` and offers no approval prompt. Did NOT route
  around it with `cp` (that is the propose-don't-install line); shipped
  `nuc-platform/skill-proposals/{ui-pattern-lock.md,install-ui-pattern-lock.mjs}` for the user to run.
  The installer was dry-run against a scratchpad copy of the repo (creates the hook, patches the right
  `settings.json` group, idempotent on re-run).
- Verified rather than assumed: the gate was made to go RED with a probe file and the probe deleted;
  the hook was tested by extracting its code from the proposal and firing six real payloads (blocks
  first call, passes the retry, blocks again in a new session, ignores `.ts` / non-UI paths / projects
  with no registry, fails open on an empty payload).
- The user then wrote his OWN first real lock (`japanese-three-signals`) into the registry and
  committed it (`40b0737`). Auditing it found it watched 2 of the 11 files rendering `SpokenLine`
  because it keyed on the field NAMES `sourceText|suggested`. Re-keyed on the contradiction
  (`showFurigana` with no `ruby=` in the same site) — which immediately caught
  `components/item-review-log.tsx`: `lib/item-stats.ts` selected `{ sourceText: true }` and dropped
  `sourceRuby`, so the item page's review log could never show furigana. Same bug as the community
  feed (`4cce2d5`), different file. Fixed; proven by reverting the fix and watching the gate name that
  exact file. `userAnswer` / `*.notes` have no ruby column in the schema at all, so they are excluded
  inside the pattern (a file-level `allow` would have exempted two whole files).
- Gates: lint clean, `tsc --noEmit` clean, 183/183. `next build` was NOT run — another `next build`
  process was already holding the lock and was left alone.

**Decisions made** — distilled into `sakubun/docs/decisions.md` 2026-07-23 "Locked UI patterns: make
locking a rule DATA, and key the check on the contradiction". Cross-project line in
`06-knowledge-ledger.md` §A. Memory `enforce-rules-with-gates` extended with the capture trigger.

**Open threads**

- **The hook is not live** until the user runs `node nuc-platform/skill-proposals/install-ui-pattern-lock.mjs`;
  until then only the vitest gate enforces, and the `manual` patterns are enforced by nothing.
- **Not rebuilt** — the furigana fix is in the working tree only, so the running container still shows
  the item page without furigana in its review log (see memory `verify-end-state-not-upload`).
- The 5 seeded patterns came from `CLAUDE.md` + memory, NOT from the user naming them. He was asked to
  name the ones that actually annoy him so they get locked in his own words.
- `userAnswer` and item `notes` carry Japanese with no ruby column anywhere in the schema — a real
  gap, currently papered over as a pattern exception rather than fixed.
- sakubun has 5 unpushed commits; the parent-repo wrap docs + `CLAUDE.md` are uncommitted.

## sakubun — Anki export: AnkiConnect push → downloadable .apkg — 2026-07-23

```yaml
id: 2026-07-23-05
type: episodic
created: 2026-07-23T12:20:00+07:00
last_accessed: 2026-07-23
importance: 6
milestone_id: null
related_ids: [{ id: 2026-07-23-02, why: 'same-day sakubun multi-user surface work' }]
embedding: null
```

**What happened**

- User asked whether the "add vocab to Anki" feature still works on this deploy. Verified empirically
  (not reasoned): container is up, `host.docker.internal` resolves, but port 8765 refuses — Anki was
  installed on the host, AnkiConnect installed, `webBindAddress` was `127.0.0.1`, Anki was closed. So
  the LOCAL path was one config + open-Anki away from working.
- The real question the user then asked: does it work for OTHER accounts hitting the site from THEIR
  machines? No — a server-side push reaches only the container HOST's Anki, never a remote member's
  `localhost`. That is a by-construction break for a multi-user deploy, not a setup issue.
- Chosen fix (user picked): DELETE the AnkiConnect push, replace with a downloadable `.apkg` the member
  imports into their own Anki. Removed `lib/anki.ts` + the `add_vocab_to_anki` MCP tool + compose
  `extra_hosts`; added `lib/anki-apkg.ts` (builds the deck by hand with better-sqlite3 + fflate — no
  sql.js), `app/api/export/anki/route.ts`, and a "Tải bộ thẻ Anki" button in Settings → Dữ liệu.
- Vocab already lives in sakubun (`Item` kind=VOCAB) so no new model, no migration — the export is a
  scoped read + format, reusing `buildBack`. Note guids derive from the Item id so re-export UPDATES
  instead of duplicating; cards export as NEW (FSRS state deliberately not carried).
- Verified against a READ-ONLY copy of the prod DB (deleted after): 4 accounts, each got only their own
  words (2/1/0/0), no duplicate guids, cards render furigana · Hán Việt · nghĩa · ví dụ. The `req` key
  in the note-type JSON is load-bearing — without it Anki imports the notes but generates ZERO cards; a
  test asserts it. Container rebuilt (healthy, /api/export/anki → 401 unauthenticated).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-23 "Anki export: AnkiConnect push replaced by
a downloadable .apkg"; closed `docs/plans/2026-07-11-anki-vocab-push.md` (status carried a `removed:`
line pointing at that decision). Cross-project line in `06-knowledge-ledger.md` §A.

**Open threads**

- **USER live-verify pending** — importing a real `.apkg` into Anki (deck exists + card count > 0) is
  the ground-truth step, not doable here. Sample files were generated for the user to double-click.
- No `userAnswerRuby` / vocab-example ruby column exists, so exported cards + several in-app surfaces
  carry Japanese without furigana until a column is added (shared gap with block 2026-07-23-04).

## sakubun — sync-from-backup (account + system restore) + auth-aware verify script — 2026-07-23

```yaml
id: 2026-07-23-06
type: episodic
created: 2026-07-23T16:45:00+07:00
last_accessed: 2026-07-23
importance: 6
milestone_id: null
related_ids:
  [
    { id: 2026-07-23-05, why: 'the RESTORE counterpart of the same Settings→Dữ liệu export/backup surface' },
    { id: 2026-07-23-02, why: 'built on the same multi-user tenancy guard + raw-SQL cross-tenant pattern' },
  ]
embedding: null
```

**What happened**

- Built **sync-from-backup**, the restore counterpart of the existing export/backup: `POST /api/import/mine`
  (a member re-imports their own JSON dump) and `POST /api/import/system` (admin merges a whole-DB `.db`).
  New: `lib/import-schema.ts` (Zod validate + PURE create/update/skip planner, DB-free), `lib/import.ts`
  (writes), the two routes, UI in `components/settings-data-tab.tsx` (file pick → dry-run preview dialog →
  confirm), `lib/import.test.ts`. Committed **`f39f88b`** (9 files) — reused the built-in
  `rebuild-and-verify.sh` afterward.
- **This reversed the 2026-07-20 "no in-app restore" decision — legitimately**, because that note said
  "do not re-raise UNLESS the app goes multi-user" and it had (2026-07-21). Surfaced the conflict to the
  user first (honest-critique) before building; the user chose all three recommended options: merge (not
  replace), in-app whole-DB merge, dry-run preview.
- **Design = MERGE, never delete**, which is what keeps invariant #2 (append-only ReviewLog) and avoids a
  file-swap: UPSERT by stable id; ReviewLog is INSERT-ONLY; Item's (kind,expression) clash is skipped (not
  fatal). Account path = guarded Prisma with `userId` FORCED to the session user (a file can't write into
  another account). System path = cross-tenant, so RAW SQL `ATTACH` + `INSERT … ON CONFLICT` per table in
  FK order (same escape-hatch rule as `lib/library.ts`), `session`/`verification` skipped, `Settings`
  matched on `userId` not its autoincrement id, FK off only for the merge window with `foreign_key_check`
  before commit.
- **Verified at runtime, not just built:** unit tests (194/194, incl. 11 new planner/validate) + lint +
  `next build` clean. The risky system SQL merge was run on a SCRATCH copy of `dev.db` (deleted 3 items +
  wiped ReviewLog → merge restored all, upsert overwrote a tampered field, no FK breakage, re-merge
  idempotent). The account path was run through the REAL guarded Prisma client + interactive transaction
  (a throwaway vitest, since the committed suite is DB-free; `dev.db` is still a legacy single-user
  snapshot so the test seeds a user first). Then rebuilt the local container and PROVED both routes are in
  the running image (anon `POST` → 401 / 403).
- **The rebuild surfaced a stale gate, not a bug:** `rebuild-and-verify.sh` reported 2 MCP failures — the
  MCP endpoint now requires a per-user bearer token, and the script probed it anonymously. Diagnosed by
  curling the endpoint (`invalid_token`), confirmed the feature commit touched no MCP files, then fixed the
  script to be token-aware (`SAKUBUN_MCP_TOKEN` → deep check; else assert up + auth-gated). Committed
  **`0e964ae`** (script only). Re-ran → "All checks passed".

**Decisions made** — distilled into `sakubun/docs/decisions.md` 2026-07-23 "In-app sync-from-backup: a
MERGE, re-opening the 'no in-app restore' call now multi-user" (committed inside `f39f88b`). Two
cross-project lines in `06-knowledge-ledger.md` §A (merge-restore; auth-aware verify script). `00-map.md` +
`lib/_INDEX.md` updated (in `f39f88b`).

**Open threads**

- Both commits (`f39f88b`, `0e964ae`) are **local only** on `main` — NUC down / local-only, not pushed.
- **USER live-verify pending:** no real backup file has been synced through the running UI (Settings → Dữ
  liệu, needs an admin login). The `.db` and JSON round-trips were proven on scratch/throwaway DBs, not the
  prod container's volume — deliberately (the port-3789 prod-DB pollution trap). The real acceptance is the
  user doing one account sync + one system-sync dry-run in the app.
- The user is mid-flight on a SEPARATE **guide-overhaul + coverage-gate** batch (`app/guide/page.tsx`,
  `components/guide-{features,steps}.tsx`, `lib/mcp/catalog.ts`, new plan
  `docs/plans/2026-07-23-guide-overhaul-and-coverage-gate.md`, `scripts/verify-image.sh`) — left entirely
  untouched/unstaged by me.
- These session-wrap docs (this log block + the 2 ledger lines) are in the parent repo and NOT yet
  committed.

## sakubun — admin last-active + /guide overhaul (5→4 tabs) + a guide-coverage gate — 2026-07-23

```yaml
id: 2026-07-23-07
type: episodic
created: 2026-07-23T17:30:00+07:00
last_accessed: 2026-07-23
importance: 6
milestone_id: null
related_ids:
  [
    { id: 2026-07-23-04, why: 'same "lock a rule as DATA + a gate" shape as ui-pattern-lock/no-emoji/layout-standard' },
    { id: 2026-07-23-03, why: 'the conversation mode this overhaul finally surfaced in the feature tour' },
    { id: 2026-07-23-06, why: 'the guide-overhaul batch that entry left as an open in-flight thread — now done' },
  ]
embedding: null
```

**What happened**

- **Admin last-active** (`/admin`): each user now shows "Hoạt động gần nhất" as relative time
  ("5 phút trước") with the exact datetime on hover. New reusable `components/relative-time.tsx`
  (+ `humanizeAgo` in `lib/format.ts`, `suppressHydrationWarning` for the clock drift). "Last active"
  = `max(last ReviewLog.reviewedAt, last Session.updatedAt)` queried **per-user** — because the
  fail-closed tenancy guard forbids a cross-user ReviewLog aggregate. Committed **`040fb39`**.
- **/guide overhaul, 5→4 tabs.** Merged the "Cách hoạt động" EXPLANATION tab INTO the flagship
  "Luyện dịch" feature topic (loop walkthrough + FSRS signals + why-Claude-grades). Filled ~10
  coverage gaps a feature-inventory (Explore subagent) found: conversation/story modes, enrichment
  cards, JSON export/sync, theme + live-refresh, dashboard charts, store below-level suspend,
  Google/verify/reset auth. Committed **`1e47244`**.
- **Guide-coverage gate (the load-bearing ask).** `docs/guide-coverage.json` registry +
  `lib/guide-coverage.test.ts` (fails when a route/prompt is uncovered, an entry is stale, or a `ref`
  stops resolving) + sakubun invariant #10 + platform PreToolUse hook
  `.claude/hooks/guide-coverage-reminder.mjs` (once-per-session nudge before a route/MCP-catalog edit).
  Committed **`259c403`** (sakubun) + **`62632a8`** (platform governance). User picked the strictest
  enforcement + 4 tabs + cover-all-gaps-except-admin from an options prompt.
- **Verified:** test 199/199, lint, `next build` clean; rebuilt the local container and proved the new
  guide + admin string are in the running `.next` bundle (not just built).

**Decisions made** — `sakubun/docs/decisions.md` 2026-07-23 "/guide: 5→4 tabs + a gate that fails when
the guide falls behind the code" (in `1e47244`/`259c403`); `00-map.md` guide line updated; plan
`docs/plans/2026-07-23-guide-overhaul-and-coverage-gate.md` → done. One `06-knowledge-ledger.md` §A line.

**Open threads**

- All commits are **local only** (NUC down / local-only) — not pushed. The concurrent
  `lib/mcp/catalog.ts` edit is the USER's, left unstaged.
- USER live-verify pending: view `/admin` + `/guide` in the running app (needs admin login).
- Admin panel stays deliberately undocumented in `/guide` (recorded as an `exempt` in the manifest).
